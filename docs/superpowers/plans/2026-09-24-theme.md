# Tema sistema / claro / escuro: plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** dar ao portfólio um tema claro AAA tão bem-acabado quanto o escuro, com um seletor Sistema / Claro / Escuro no painel de preferências, sem piscar o tema errado no carregamento.

**Architecture:** cada cor que muda com o tema vira um token `light-dark(claro, escuro)` no `@theme` do Tailwind. O `color-scheme` do `<html>` decide o lado: `light dark` segue o sistema (só CSS); `data-theme="light"` ou `"dark"` força um lado. Um script inline no `<head>` lê o `localStorage` e grava o `data-theme` antes da primeira pintura. No cliente, um store pequeno (`useSyncExternalStore`) guarda a escolha, e a troca faz um cross-fade com a View Transitions API.

**Tech Stack:** Next.js 16.3.5 (App Router, Turbopack), React 19.2.8, TypeScript 5.9.3, Tailwind CSS 4.3.3 (`@custom-variant`, `light-dark()`), Biome 2.4.2, Node 24 (`node --test` com TypeScript nativo), agent-browser, `sharp` (vem com o Next; usado só em scripts de verificação no scratchpad).

**Spec:** `docs/superpowers/specs/2026-09-24-theme-design.md`

## Global Constraints

- Nenhuma dependência nova em `package.json` (nem de desenvolvimento).
- Temas: `system`, `light`, `dark`. Padrão: `system`. Chave do `localStorage`: `theme`. `"system"` não é gravado (a chave é apagada).
- O `<html>` só recebe `data-theme="light"` ou `data-theme="dark"`. Sem atributo = sistema.
- Cores exatamente como na spec (claro / escuro): `page` `#f4f7fb` / `#070c17`; `surface` `#ffffff` / `#0c1424`; `line` `#d9e1ec` / `#1e2b45`; `fg` `#0b1220` / `#f3f6fb`; `fg-muted` `#455369` / `#93a1b8`; `highlight` `#085975` / `#61dafb`; `gradient-from` `#1a5299` / `#6aa8f5`; `gradient-via` `#085975` / `#61dafb`; `gradient-to` `#265e1d` / `#8cc84b`.
- Cores de marca e do botão iguais nos dois temas: `ts #3178c6`, `ts-light #6aa8f5`, `react #61dafb`, `node #5fa04e`, `node-light #8cc84b`, `node-lighter #a6db6c`, `accent-foreground #04070e`.
- O tema escuro fica **visualmente idêntico** ao de hoje. A única mudança visível no escuro é a altura dos links sociais no desktop (44px, AAA 2.5.5).
- Contraste AAA para todo texto, nos dois temas: 7:1 (normal) e 4.5:1 (grande, 24px+ ou 18.66px+ em negrito).
- `/pt-br` e `/en` continuam páginas estáticas no `pnpm build`.
- A imagem de Open Graph e o `manifest` continuam escuros.
- Arquivos testados com `node --test` (`src/theme/themes.ts`, `theme-script.ts`, `theme-store.ts` e o `src/lib/theme-colors.ts` que o store importa) se importam com `.ts` no final e sem o alias `@/`. O resto do projeto continua com `@/` (ou relativo) sem extensão.
- Um arquivo, uma responsabilidade. Nomes em kebab-case.
- Comentários em português, curtos, explicando o conceito novo (`light-dark()`, `color-scheme`, `@custom-variant`, script inline, `useSyncExternalStore`, View Transitions), no estilo dos que já existem no projeto.
- Mensagens de commit em inglês, no formato do histórico (`feat: ...`), terminando com `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
- `pnpm lint` (Biome) precisa passar em todo commit. Se reclamar só de formatação ou da ordem dos imports, rode `pnpm exec biome check --write` e confira o diff.
- Nunca mexer no servidor da porta 3000 (é do Sinval). Os servidores das tarefas usam a porta 3100.

## Receitas usadas nas tarefas

O estado do shell não persiste entre comandos, então cada bloco repete as variáveis de que precisa. Não use `sleep` solto: para esperar o servidor, use `curl --retry`; para esperar no navegador, `agent-browser wait <ms>`.

**Scratchpad** (saídas temporárias, screenshots, scripts de verificação; nunca dentro do repo):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
```

**Subir o build de produção na porta 3100:**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
cd /home/martins/projects/martinsportfolio
pnpm build
setsid pnpm start --port 3100 > "$SCRATCH/server.log" 2>&1 &
curl -s -o /dev/null --retry 30 --retry-delay 1 --retry-connrefused -w "servidor: %{http_code}\n" http://localhost:3100/en
```

**Subir o servidor de desenvolvimento na porta 3100** (só onde a tarefa pede o modo dev):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
cd /home/martins/projects/martinsportfolio
setsid pnpm dev --port 3100 > "$SCRATCH/dev.log" 2>&1 &
curl -s -o /dev/null --retry 60 --retry-delay 1 --retry-connrefused -w "servidor: %{http_code}\n" http://localhost:3100/en
```

**Parar o servidor da porta 3100** (mata o grupo de processos inteiro: `pnpm`, `next` e `next-server`). O PGID vem de quem escuta a porta, não de `$!`: depois de `setsid ... &`, o `$!` não é o líder do grupo e o `kill` erra em silêncio.

```bash
PID=$(ss -lptnH 'sport = :3100' | grep -o 'pid=[0-9]*' | head -1 | cut -d= -f2)
if [ -n "$PID" ]; then
  PGID=$(ps -o pgid= -p "$PID" | tr -d ' ')
  kill -- -"$PGID"
  timeout 15 tail --pid="$PID" -f /dev/null
fi
ss -ltnH 'sport = :3100' | grep -q . && echo "AINDA RODANDO" || echo "servidor parado"
ps -eo pid,cmd | grep -E "next (start|dev)|next-server" | grep -v grep | grep 3100 || echo "nenhum processo sobrando na 3100"
```

**Sessão do agent-browser** (sempre uma sessão própria; screenshots com caminho absoluto no scratchpad; escala 1, para 1px de CSS = 1px da imagem):

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set viewport 1440 900 1
```

No fim de cada tarefa que usou o navegador: `agent-browser close`.

**Screenshots estáveis:** as duas animações que ficam em loop (o ponto de "disponível" e a seta de rolar) só rodam sem "reduzir movimento". Para comparar pixel a pixel, use `agent-browser set media dark reduced-motion` (ou `light reduced-motion`). Com "reduzir movimento", a intro dura cerca de 1s: `agent-browser wait 4000` depois de abrir basta.

**Script de comparação de imagens** (`$SCRATCH/diff.cjs`, criado na Tarefa 2 e reusado depois):

```js
// Compara dois PNGs do mesmo tamanho: quanto mudou e onde.
// Uso: node diff.cjs antes.png depois.png
const sharp = require(
  require.resolve("sharp", {
    paths: [require.resolve("next", { paths: ["/home/martins/projects/martinsportfolio"] })],
  }),
);
const [a, b] = process.argv.slice(2);
const load = (file) => sharp(file).removeAlpha().raw().toBuffer({ resolveWithObject: true });
(async () => {
  const [A, B] = await Promise.all([load(a), load(b)]);
  if (A.info.width !== B.info.width || A.info.height !== B.info.height) {
    console.log(`tamanhos diferentes: ${A.info.width}x${A.info.height} vs ${B.info.width}x${B.info.height}`);
    process.exit(1);
  }
  const { width, height } = A.info;
  let changed = 0;
  let [minX, minY, maxX, maxY] = [width, height, -1, -1];
  for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
      const i = (y * width + x) * 3;
      const d = Math.max(
        Math.abs(A.data[i] - B.data[i]),
        Math.abs(A.data[i + 1] - B.data[i + 1]),
        Math.abs(A.data[i + 2] - B.data[i + 2]),
      );
      if (d > 10) {
        changed++;
        minX = Math.min(minX, x);
        minY = Math.min(minY, y);
        maxX = Math.max(maxX, x);
        maxY = Math.max(maxY, y);
      }
    }
  }
  const pct = ((changed / (width * height)) * 100).toFixed(3);
  console.log(changed === 0 ? "idênticas" : `${pct}% dos pixels mudaram, na área x ${minX}-${maxX}, y ${minY}-${maxY}`);
})();
```

**Scripts de contraste na tela** (`$SCRATCH/probe-boxes.js` e `$SCRATCH/contrast.cjs`, criados na Tarefa 3 e reusados na 5). O primeiro roda no navegador: esconde os textos medidos e devolve onde eles estavam e a cor de cada um. O segundo lê a screenshot tirada com os textos escondidos e mede o pior contraste entre a cor do texto e os pixels que ficam atrás dele. Esconder o texto também some com a sombra ou o halo dele, então a medida é pessimista: se passar assim, passa de verdade.

`$SCRATCH/probe-boxes.js`:

```js
(() => {
  const light = getComputedStyle(document.body).backgroundColor === "rgb(244, 247, 251)";
  const c = light
    ? { fg: "#0b1220", muted: "#455369", grad: ["#1a5299", "#085975", "#265e1d"] }
    : { fg: "#f3f6fb", muted: "#93a1b8", grad: ["#6aa8f5", "#61dafb", "#8cc84b"] };
  const targets = [
    ["titulo", document.querySelector("h1"), [c.fg], 4.5],
    ["subtitulo", document.querySelector("h1 + p span"), c.grad, 7],
    ["disponivel", document.querySelector("main p span.text-fg-muted"), [c.muted], 7],
    ...[...document.querySelectorAll('main a[target="_blank"]')].map((a, i) => [`social-${i}`, a, [c.muted], 7]),
    ...[...document.querySelectorAll("header nav a")].map((a, i) => [`nav-${i}`, a, [c.muted], 7]),
  ];
  const boxes = [];
  for (const [label, el, colors, goal] of targets) {
    if (!el) continue;
    const r = el.getBoundingClientRect();
    if (r.width === 0 || r.bottom < 0 || r.top > innerHeight) continue;
    boxes.push({ label, x: r.x, y: r.y, w: r.width, h: r.height, colors, goal });
    el.style.visibility = "hidden";
  }
  return JSON.stringify(boxes);
})()
```

`$SCRATCH/contrast.cjs`:

```js
// Pior contraste (WCAG) entre a cor de cada texto e os pixels atrás dele.
// Uso: node contrast.cjs screenshot.png boxes.txt
// boxes.txt é a saída do probe-boxes.js. Para gradiente (3 cores), cada
// terço da caixa é comparado com a sua cor (começo, meio, fim).
const fs = require("node:fs");
const sharp = require(
  require.resolve("sharp", {
    paths: [require.resolve("next", { paths: ["/home/martins/projects/martinsportfolio"] })],
  }),
);
const [shot, boxesFile] = process.argv.slice(2);
let boxes = JSON.parse(fs.readFileSync(boxesFile, "utf8").trim());
if (typeof boxes === "string") boxes = JSON.parse(boxes);
const lin = (v) => (v <= 0.04045 ? v / 12.92 : ((v + 0.055) / 1.055) ** 2.4);
const lum = ([r, g, b]) => 0.2126 * lin(r / 255) + 0.7152 * lin(g / 255) + 0.0722 * lin(b / 255);
const hex = (h) => [1, 3, 5].map((i) => Number.parseInt(h.slice(i, i + 2), 16));
const ratio = (a, b) => {
  const [hi, lo] = [lum(a), lum(b)].sort((m, n) => n - m);
  return (hi + 0.05) / (lo + 0.05);
};
(async () => {
  const { data, info } = await sharp(shot).removeAlpha().raw().toBuffer({ resolveWithObject: true });
  let failed = false;
  for (const box of boxes) {
    let worst = Number.POSITIVE_INFINITY;
    for (let y = Math.round(box.y); y < Math.round(box.y + box.h); y++) {
      for (let x = Math.round(box.x); x < Math.round(box.x + box.w); x++) {
        if (x < 0 || y < 0 || x >= info.width || y >= info.height) continue;
        const i = (y * info.width + x) * 3;
        const part = Math.min(box.colors.length - 1, Math.floor(((x - box.x) / box.w) * box.colors.length));
        worst = Math.min(worst, ratio(hex(box.colors[part]), [data[i], data[i + 1], data[i + 2]]));
      }
    }
    const ok = worst >= box.goal;
    if (!ok) failed = true;
    console.log(`${box.label.padEnd(12)} pior ${worst.toFixed(2)}:1 (meta ${box.goal}:1) ${ok ? "OK" : "FALHOU"}`);
  }
  process.exit(failed ? 1 : 0);
})();
```

**Medir o contraste de uma tela** (com o servidor e a sessão do navegador já abertos; `NOME` identifica a medida, ex.: `claro-1440`):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
NOME=claro-1440
agent-browser eval "$(cat "$SCRATCH/probe-boxes.js")" > "$SCRATCH/boxes-$NOME.txt"
agent-browser screenshot "$SCRATCH/contraste-$NOME.png"
node "$SCRATCH/contrast.cjs" "$SCRATCH/contraste-$NOME.png" "$SCRATCH/boxes-$NOME.txt"
agent-browser reload
agent-browser wait 4000
```

## Ajustes em relação à spec

Decididos ao escrever o plano, depois de conferir as ferramentas instaladas:

1. **O grupo "Tema" some sem JavaScript com a variante `noscript:` do Tailwind** (gera `@media (scripting: none)`), numa classe do próprio componente. O `preferences-menu.module.css` não muda.
2. **Não existe o token `shade`.** Cada efeito (vinheta, sombra do título, halo do subtítulo) usa `light-dark()` com a cor e a opacidade dele, porque as opacidades são diferentes em cada tema. Os valores do escuro ficam idênticos aos de hoje. Como `light-dark()` só troca cores, não tamanhos, as sombras do claro usam os mesmos deslocamentos e desfoques do escuro (a primeira sombra do título fica `0 2px 4px` e a última do subtítulo `0 2px 18px`, em vez de `0 1px 2px` e `22px` do mockup). A diferença não aparece na tela.
3. **A logo ganha a prop `strokeColor` direto.** Conferido no pacote do `next/og`: o Satori não trata `currentColor`. O padrão é `currentColor` (a logo herda o `text-fg` do `<body>`), e a imagem de OG passa o hex.
4. **O `theme-store.ts` também guarda a escolha na memória.** Com o storage bloqueado, a escolha vale até o F5, como o cookie de idioma. Ganha teste próprio (`theme-store.test.ts`), então importa com `.ts` e sem `@/`, inclusive `../lib/theme-colors.ts`.
5. **`themeColors.ink900` vira `pageDark`**, e entra `pageLight`: nome pela função, como no CSS.
6. **O ícone do segmento marcado usa `highlight`, não verde-limão.** O verde sobre o fundo claro teria 1.9:1.
7. **A borda do segmento marcado usa `fg-muted`**, que tem mais de 7:1 contra a faixa nos dois temas (a spec pede pelo menos 3:1).
8. **Holofote no claro:** as camadas começam com metade da força (`ts/18`, `react/12`, `node/18`). A Tarefa 3 confere no navegador.
9. **Tela de erro:** o `error.tsx` desenha o mesmo `StatusPage` do 404, que é conferido nos dois temas. O `global-error` não tem como ser disparado sem mudar código de propósito; o `ThemeSync` dele é conferido por leitura.
10. **Contraste em cima da colagem:** a medida pessimista (texto escondido) some com o halo. Se o subtítulo claro falhar só nela, a Tarefa 3 mede de novo com o halo pintado (passo descrito lá). Se falhar com halo, reforça a vinheta.

## Review Focus

Entradas que a spec implica, mas que é fácil esquecer. Cada uma tem teste ou checagem na tarefa indicada.

1. **Storage bloqueado** (aba anônima antiga, cookies e dados do site desligados): `localStorage` lança erro só de ser tocado. A página carrega no tema do sistema sem quebrar, e escolher um tema funciona na aba (só não fica lembrado). (Tarefa 1, testes do script e do store)
2. **Valor estranho salvo na chave `theme`** (`"Dark"`, `"\"dark\""`, `"blue"`, `"light "`): é tratado como "Sistema", nunca vira `data-theme` inválido. (Tarefa 1, testes)
3. **Trocas rápidas seguidas** (cliques ou setas durante o fade): o tema final é o último escolhido, o rádio marcado bate com ele e o `data-theme-switching` não fica preso no `<html>` (o que desligaria as transições do site para sempre). (Tarefa 4, passo 11)
4. **Trocar de idioma com tema forçado:** a página em inglês continua no tema escolhido, sem piscar. (Tarefa 4, passo 13)
5. **O sistema muda com a página aberta:** em "Sistema", o site acompanha na hora; com escolha forçada, não muda. (Tarefa 2, passo 12)

---

### Task 1: Núcleo do tema (lista, script inline e store), com testes

**Files:**
- Create: `src/theme/themes.ts`
- Create: `src/theme/theme-script.ts`
- Create: `src/theme/theme-store.ts`
- Test: `src/theme/themes.test.ts`
- Test: `src/theme/theme-script.test.ts`
- Test: `src/theme/theme-store.test.ts`
- Modify: `src/lib/theme-colors.ts` (`ink900` → `pageDark`, novo `pageLight`)
- Modify: `src/app/manifest.ts`, `src/app/[lang]/opengraph-image.tsx`, `src/app/[lang]/layout.tsx` (só o nome `ink900` → `pageDark`)

**Interfaces:**
- Consumes: nada.
- Produces:
  - `themes: readonly ["system", "light", "dark"]`
  - `type Theme = "system" | "light" | "dark"`
  - `THEME_STORAGE_KEY: "theme"`
  - `isTheme(value: unknown): value is Theme`
  - `themeScript: string` (o texto do script inline)
  - `readTheme(): Theme`
  - `saveTheme(theme: Theme): void`
  - `subscribeTheme(listener: () => void): () => void`
  - `applyTheme(theme: Theme): void`
  - `themeColors.pageDark` (`"#070c17"`) e `themeColors.pageLight` (`"#f4f7fb"`)

- [ ] **Passo 1: Renomear a cor de fundo em `theme-colors.ts`**

Substitua o arquivo `src/lib/theme-colors.ts` inteiro por:

```ts
/**
 * Cores para onde não existe CSS: a cor da barra do navegador
 * (`viewport.themeColor` e o `applyTheme`), o manifest, a imagem de Open
 * Graph e a logo (que também é desenhada na imagem de Open Graph).
 *
 * São cópias dos tokens do `globals.css`. Se mudar uma cor lá, mude aqui
 * também. Fora `pageLight`, são as cores do tema escuro: a imagem de
 * compartilhamento e o manifest são sempre escuros.
 *
 * O src/theme/theme-store.ts importa este arquivo com ".ts" e sem "@/",
 * porque é testado com `node --test`.
 */
export const themeColors = {
  pageDark: "#070c17",
  pageLight: "#f4f7fb",
  line: "#1e2b45",
  fg: "#f3f6fb",
  fgMuted: "#93a1b8",
  ts: "#3178c6",
  tsLight: "#6aa8f5",
  react: "#61dafb",
  node: "#5fa04e",
  nodeLight: "#8cc84b",
} as const;
```

Troque os três usos de `ink900` (o valor é o mesmo, só muda o nome):

```bash
cd /home/martins/projects/martinsportfolio
sed -i 's/themeColors\.ink900/themeColors.pageDark/g' src/app/manifest.ts "src/app/[lang]/layout.tsx"
sed -i 's/c\.ink900/c.pageDark/g' "src/app/[lang]/opengraph-image.tsx"
grep -rn "ink900" src || echo "nenhum ink900 sobrando"
```

Esperado: `nenhum ink900 sobrando`.

- [ ] **Passo 2: Escrever os testes de `themes.ts` e do script (vão falhar)**

`src/theme/themes.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, test } from "node:test";
import { isTheme } from "./themes.ts";

describe("isTheme", () => {
  test("aceita os três temas", () => {
    for (const value of ["system", "light", "dark"]) {
      assert.equal(isTheme(value), true, value);
    }
  });

  test("recusa qualquer outra coisa", () => {
    for (const value of ["blue", "", "Dark", '"dark"', "light ", null, 1]) {
      assert.equal(isTheme(value), false, String(value));
    }
  });
});
```

`src/theme/theme-script.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, test } from "node:test";
import { runInNewContext } from "node:vm";
import { themeScript } from "./theme-script.ts";

type Stored = string | null | { blocked: true };

/**
 * Roda o script como o navegador rodaria, mas num contexto separado, com
 * um <html> e um localStorage falsos. Devolve o data-theme gravado, ou
 * undefined se nada foi gravado.
 */
function runScript(stored: Stored) {
  const attributes = new Map<string, string>();
  const context = {
    document: {
      documentElement: {
        setAttribute: (name: string, value: string) => {
          attributes.set(name, value);
        },
      },
    },
    localStorage: {
      getItem: () => {
        if (stored !== null && typeof stored === "object") {
          throw new Error("SecurityError");
        }
        return stored;
      },
    },
  };
  runInNewContext(themeScript, context);
  return attributes.get("data-theme");
}

describe("script inline do tema", () => {
  test("claro salvo vira data-theme=light", () => {
    assert.equal(runScript("light"), "light");
  });

  test("escuro salvo vira data-theme=dark", () => {
    assert.equal(runScript("dark"), "dark");
  });

  test("sistema ou nada salvo não grava atributo", () => {
    assert.equal(runScript("system"), undefined);
    assert.equal(runScript(null), undefined);
  });

  test("valor estranho é ignorado", () => {
    for (const value of ["blue", "", "Dark", '"dark"', "light "]) {
      assert.equal(runScript(value), undefined, value);
    }
  });

  test("storage bloqueado não quebra a página", () => {
    assert.doesNotThrow(() => runScript({ blocked: true }));
    assert.equal(runScript({ blocked: true }), undefined);
  });
});
```

- [ ] **Passo 3: Rodar e ver falhar**

Run: `cd /home/martins/projects/martinsportfolio && pnpm test`
Expected: FAIL nos dois arquivos novos, com `ERR_MODULE_NOT_FOUND` para `./themes.ts` e `./theme-script.ts`. Os testes de idioma continuam passando.

- [ ] **Passo 4: Escrever `themes.ts` e `theme-script.ts`**

`src/theme/themes.ts`:

```ts
// Fonte única dos temas. É importado pelo layout (script inline), pelo
// cliente e pelos testes (`pnpm test`).
//
// Como em src/i18n/locales.ts, os arquivos do tema testados com
// `node --test` se importam com ".ts" no final e sem o alias "@/".

/** "system" segue o sistema operacional; os outros forçam um lado. */
export const themes = ["system", "light", "dark"] as const;

export type Theme = (typeof themes)[number];

/** Chave do localStorage com a escolha do painel. "system" não é gravado. */
export const THEME_STORAGE_KEY = "theme";

/** Type guard que aceita qualquer valor: o storage devolve string ou null. */
export function isTheme(value: unknown): value is Theme {
  return (themes as readonly unknown[]).includes(value);
}
```

`src/theme/theme-script.ts`:

```ts
import { THEME_STORAGE_KEY, themes } from "./themes.ts";

// Os temas que forçam um lado. "system" fica de fora: sem atributo, o
// CSS segue o sistema operacional.
const forcedThemes = themes.filter((theme) => theme !== "system");

/**
 * Script que o layout põe no <head>. O navegador roda ele enquanto lê o
 * HTML, antes de pintar a página: a escolha salva já aparece no primeiro
 * quadro, sem piscar o outro tema. (Num useEffect, só rodaria depois de
 * o React carregar, com a página já pintada.)
 *
 * É texto, não função, porque vai direto para o HTML: por isso é JS
 * antigo (var, indexOf) e não usa nada de fora. O try/catch cobre o
 * storage bloqueado; aí vale o sistema.
 */
export const themeScript = `(function(){try{var t=localStorage.getItem(${JSON.stringify(THEME_STORAGE_KEY)});if(${JSON.stringify(forcedThemes)}.indexOf(t)!==-1)document.documentElement.setAttribute("data-theme",t)}catch(e){}})()`;
```

- [ ] **Passo 5: Rodar e ver passar**

Run: `cd /home/martins/projects/martinsportfolio && pnpm test`
Expected: PASS em todos (os de idioma e os dois novos).

- [ ] **Passo 6: Escrever o teste do store (vai falhar)**

`src/theme/theme-store.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, test } from "node:test";

type Store = typeof import("./theme-store.ts");

/** Um localStorage falso, com o que foi gravado à vista em `data`. */
function fakeStorage(initial: Record<string, string> = {}) {
  const data = new Map(Object.entries(initial));
  return {
    data,
    getItem: (key: string) => data.get(key) ?? null,
    setItem: (key: string, value: string) => {
      data.set(key, value);
    },
    removeItem: (key: string) => {
      data.delete(key);
    },
  };
}

let loads = 0;

/**
 * Carrega uma cópia nova do store: a query string muda a URL, e o Node
 * trata como outro módulo (o store guarda estado no módulo). "blocked"
 * imita o navegador com o storage bloqueado: só tocar nele já lança erro.
 */
async function loadStore(
  storage: ReturnType<typeof fakeStorage> | "blocked",
): Promise<Store> {
  Object.defineProperty(globalThis, "localStorage", {
    configurable: true,
    get: () => {
      if (storage === "blocked") throw new Error("SecurityError");
      return storage;
    },
  });
  loads += 1;
  return import(`./theme-store.ts?load=${loads}`);
}

describe("theme-store", () => {
  test("sem nada salvo, o tema é o do sistema", async () => {
    const { readTheme } = await loadStore(fakeStorage());
    assert.equal(readTheme(), "system");
  });

  test("lê a escolha salva", async () => {
    const { readTheme } = await loadStore(fakeStorage({ theme: "dark" }));
    assert.equal(readTheme(), "dark");
  });

  test("valor salvo estranho vira sistema", async () => {
    for (const value of ["blue", "Dark", '"dark"']) {
      const { readTheme } = await loadStore(fakeStorage({ theme: value }));
      assert.equal(readTheme(), "system", value);
    }
  });

  test("salvar claro grava no storage e muda a leitura", async () => {
    const storage = fakeStorage();
    const { readTheme, saveTheme } = await loadStore(storage);
    saveTheme("light");
    assert.equal(storage.data.get("theme"), "light");
    assert.equal(readTheme(), "light");
  });

  test("salvar sistema apaga a chave", async () => {
    const storage = fakeStorage({ theme: "dark" });
    const { readTheme, saveTheme } = await loadStore(storage);
    saveTheme("system");
    assert.equal(storage.data.has("theme"), false);
    assert.equal(readTheme(), "system");
  });

  test("storage bloqueado: começa no sistema e a escolha vale na memória", async () => {
    const { readTheme, saveTheme } = await loadStore("blocked");
    assert.equal(readTheme(), "system");
    assert.doesNotThrow(() => saveTheme("dark"));
    assert.equal(readTheme(), "dark");
  });

  test("avisa quem assinou, até cancelar", async () => {
    const { saveTheme, subscribeTheme } = await loadStore(fakeStorage());
    let calls = 0;
    const unsubscribe = subscribeTheme(() => {
      calls += 1;
    });
    saveTheme("light");
    unsubscribe();
    saveTheme("dark");
    assert.equal(calls, 1);
  });
});
```

Run: `cd /home/martins/projects/martinsportfolio && pnpm test`
Expected: FAIL em `theme-store.test.ts` com `ERR_MODULE_NOT_FOUND` para `./theme-store.ts`.

- [ ] **Passo 7: Escrever o store**

`src/theme/theme-store.ts`:

```ts
import { themeColors } from "../lib/theme-colors.ts";
import { isTheme, THEME_STORAGE_KEY, type Theme } from "./themes.ts";

// Guarda a escolha do tema e aplica no <html>. Roda só no navegador: o
// servidor não sabe o tema (as páginas são estáticas).

/**
 * Última escolha conhecida. Com o storage bloqueado, a escolha continua
 * valendo nesta aba; só não fica lembrada depois do F5.
 */
let current: Theme | undefined;
const listeners = new Set<() => void>();

function readStoredTheme(): Theme {
  try {
    const stored = localStorage.getItem(THEME_STORAGE_KEY);
    return isTheme(stored) ? stored : "system";
  } catch {
    return "system";
  }
}

/** A escolha atual: a da memória ou, na primeira vez, a do storage. */
export function readTheme(): Theme {
  current ??= readStoredTheme();
  return current;
}

/** Guarda a escolha e avisa quem estiver ouvindo (o hook useTheme). */
export function saveTheme(theme: Theme) {
  current = theme;
  try {
    // "system" é a ausência de escolha: apaga em vez de gravar.
    if (theme === "system") localStorage.removeItem(THEME_STORAGE_KEY);
    else localStorage.setItem(THEME_STORAGE_KEY, theme);
  } catch {
    // Storage bloqueado: a escolha fica só na memória.
  }
  for (const listener of listeners) listener();
}

/** O formato que o useSyncExternalStore pede: assina e devolve o cancelamento. */
export function subscribeTheme(listener: () => void) {
  listeners.add(listener);
  return () => {
    listeners.delete(listener);
  };
}

/**
 * Põe o tema no <html> (o CSS faz o resto) e acerta a cor da barra do
 * navegador. O layout gera duas <meta name="theme-color">, uma para cada
 * `prefers-color-scheme`. Com escolha forçada, as duas recebem a cor do
 * tema escolhido, e vale a que o navegador estiver usando.
 */
export function applyTheme(theme: Theme) {
  const root = document.documentElement;
  if (theme === "system") root.removeAttribute("data-theme");
  else root.setAttribute("data-theme", theme);

  const metas = document.querySelectorAll<HTMLMetaElement>(
    'meta[name="theme-color"]',
  );
  for (const meta of metas) {
    const scheme =
      theme === "system"
        ? meta.media.includes("dark")
          ? "dark"
          : "light"
        : theme;
    meta.content =
      scheme === "dark" ? themeColors.pageDark : themeColors.pageLight;
  }
}
```

- [ ] **Passo 8: Rodar os testes, o lint e o build**

```bash
cd /home/martins/projects/martinsportfolio
pnpm test
pnpm lint
pnpm build 2>&1 | tail -25
```

Expected: `pnpm test` com todos passando (`fail 0`); `pnpm lint` sem erros; build termina com `/[lang]` gerando `/pt-br` e `/en` como estáticas (`●`). Se o Biome reclamar só da ordem dos imports ou de formatação, rode `pnpm exec biome check --write` e confira o diff.

- [ ] **Passo 9: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add src/theme src/lib/theme-colors.ts src/app/manifest.ts "src/app/[lang]/opengraph-image.tsx" "src/app/[lang]/layout.tsx"
git commit -m "feat: add the theme list, the inline theme script and the theme store with tests

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 2: Tokens com `light-dark()` e o tema aplicado antes da pintura

**Files:**
- Modify: `src/app/globals.css` (arquivo inteiro)
- Create: `src/theme/theme-sync.tsx`
- Modify: `src/app/[lang]/layout.tsx`
- Modify: `src/app/global-error.tsx`
- Modify: `src/components/hero/hero-grid.tsx` (`bg-ink-800` → `bg-surface`)
- Modify: `src/components/preferences/preferences-menu.tsx` (`bg-ink-800` → `bg-surface`)

**Interfaces:**
- Consumes: `themeScript`, `readTheme()`, `applyTheme(theme)`, `themeColors.pageDark`, `themeColors.pageLight` (Tarefa 1).
- Produces:
  - Classes do Tailwind: `bg-page`, `bg-surface`, `ring-line`, `text-fg`, `text-fg-muted`, `text-highlight`, `outline-highlight`, `from-gradient-from`, `via-gradient-via`, `to-gradient-to` (e as de sempre para as cores de marca).
  - Variantes `light:` e `dark:` (escolha forçada primeiro, sistema depois).
  - Utilities `bg-stack-glow`, `title-glow` (agora com os dois temas) e `subtitle-glow` (nova, usada na Tarefa 3).
  - CSS da troca: `::view-transition-old/new(root)` com 250ms e a regra `[data-theme-switching]` (usada pela Tarefa 4).
  - `<ThemeSync />` (componente cliente, não desenha nada).

- [ ] **Passo 1: Tirar as screenshots de referência do escuro de hoje**

Antes de mexer no CSS. Crie o `$SCRATCH/diff.cjs` com o conteúdo da seção "Receitas" e suba o build de produção (receita). Depois:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media dark reduced-motion
agent-browser set viewport 1440 900 1
agent-browser open http://localhost:3100/pt-br
agent-browser wait 4000
agent-browser screenshot "$SCRATCH/antes-escuro-1440.png"
agent-browser set viewport 375 812 1
agent-browser reload
agent-browser wait 4000
agent-browser screenshot "$SCRATCH/antes-escuro-375.png"
agent-browser open http://localhost:3100/pt-br/nao-existe
agent-browser wait 1500
agent-browser screenshot "$SCRATCH/antes-404-375.png"
OG=$(curl -s http://localhost:3100/en | grep -o 'property="og:image" content="[^"]*"' | head -1 | sed 's/.*content="//;s/"$//;s/&amp;/\&/g')
echo "og: $OG"
curl -s "$OG" -o "$SCRATCH/antes-og-en.png" && file "$SCRATCH/antes-og-en.png"
```

Expected: três PNGs de página e um PNG de 1200x630 da imagem de OG. Pare o servidor (receita).

- [ ] **Passo 2: Reescrever `globals.css`**

Substitua `src/app/globals.css` inteiro por:

```css
@import "tailwindcss";

/* Tema. light-dark(claro, escuro) escolhe um dos dois valores pelo
   color-scheme do elemento. No :root, "light dark" deixa o navegador
   decidir pelo sistema operacional: é o tema "Sistema", só com CSS.
   Com data-theme no <html> (posto pelo script do layout, ver
   src/theme/theme-script.ts), só um lado vale. */
:root {
  color-scheme: light dark;
}

:root[data-theme="light"] {
  color-scheme: light;
}

:root[data-theme="dark"] {
  color-scheme: dark;
}

/* Variantes para o que não é cor (mostrar uma imagem ou outra, trocar
   uma sombra). A dark: de fábrica do Tailwind só olha o sistema; estas
   duas seguem a mesma regra do color-scheme: escolha forçada primeiro,
   sistema depois. @slot é onde entra a classe (ex.: light:hidden). */
@custom-variant dark {
  &:where([data-theme="dark"], [data-theme="dark"] *) {
    @slot;
  }
  @media (prefers-color-scheme: dark) {
    &:where(:root:not([data-theme]), :root:not([data-theme]) *) {
      @slot;
    }
  }
}

@custom-variant light {
  &:where([data-theme="light"], [data-theme="light"] *) {
    @slot;
  }
  @media (prefers-color-scheme: light) {
    &:where(:root:not([data-theme]), :root:not([data-theme]) *) {
      @slot;
    }
  }
}

@theme {
  /* Cores de marca: iguais nos dois temas. */
  --color-ts: #3178c6;
  --color-ts-light: #6aa8f5;
  --color-react: #61dafb;
  --color-node: #5fa04e;
  --color-node-light: #8cc84b;
  --color-node-lighter: #a6db6c;

  /* Cores de papel: light-dark(claro, escuro). Todo texto passa AAA
     (7:1) nos dois temas; os contrastes estão na spec do tema. */
  --color-page: light-dark(#f4f7fb, #070c17);
  --color-surface: light-dark(#ffffff, #0c1424);
  --color-line: light-dark(#d9e1ec, #1e2b45);
  --color-fg: light-dark(#0b1220, #f3f6fb);
  --color-fg-muted: light-dark(#455369, #93a1b8);
  --color-highlight: light-dark(#085975, #61dafb);
  --color-gradient-from: light-dark(#1a5299, #6aa8f5);
  --color-gradient-via: light-dark(#085975, #61dafb);
  --color-gradient-to: light-dark(#265e1d, #8cc84b);

  --animate-nudge: nudge 1.6s ease-in-out infinite;

  @keyframes nudge {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(6px);
    }
  }
}

@theme inline {
  --font-display: var(--font-display-family), Georgia, serif;
  --font-sans: var(--font-body-family), ui-sans-serif, system-ui, sans-serif;
  --font-mono: var(--font-mono-family), ui-monospace, monospace;
  --color-accent: var(--color-node-light);
  --color-accent-strong: var(--color-node-lighter);
  /* Texto do botão: escuro nos dois temas (10:1 sobre o verde). */
  --color-accent-foreground: #04070e;
}

@utility bg-stack-glow {
  background-color: var(--color-page);
  background-image:
    radial-gradient(
      55% 45% at 0% 35%,
      light-dark(
        color-mix(in oklab, var(--color-ts) 14%, transparent),
        color-mix(in oklab, var(--color-ts) 24%, transparent)
      ),
      transparent
    ),
    radial-gradient(
      50% 40% at 100% 75%,
      light-dark(
        color-mix(in oklab, var(--color-node) 12%, transparent),
        color-mix(in oklab, var(--color-node) 16%, transparent)
      ),
      transparent
    ),
    radial-gradient(
      circle at 1px 1px,
      light-dark(rgb(11 18 32 / 0.06), rgb(255 255 255 / 0.05)) 1px,
      transparent 0
    );
  background-size:
    auto,
    auto,
    28px 28px;
}

/* Sombra atrás do título: escura no escuro, halo claro no claro. */
@utility title-glow {
  text-shadow:
    0 2px 4px light-dark(rgb(255 255 255 / 0.7), rgb(4 7 14 / 0.5)),
    0 8px 40px light-dark(rgb(244 247 251 / 0.95), rgb(4 7 14 / 0.85)),
    0 0 90px
      light-dark(
        color-mix(in oklab, var(--color-ts) 22%, transparent),
        color-mix(in oklab, var(--color-ts) 40%, transparent)
      );
}

/* Separa o subtítulo das linhas coloridas dos cards. No escuro é a
   sombra de sempre (as duas primeiras são transparentes); no claro, um
   halo branco. */
@utility subtitle-glow {
  filter: drop-shadow(0 0 1px light-dark(rgb(255 255 255 / 0.9), transparent))
    drop-shadow(0 0 10px light-dark(rgb(255 255 255 / 0.95), transparent))
    drop-shadow(0 2px 18px light-dark(#f4f7fb, rgb(4 7 14 / 0.95)));
}

@layer base {
  :focus-visible {
    outline: 2px solid var(--color-highlight);
    outline-offset: 4px;
  }

  ::selection {
    background-color: color-mix(in oklab, var(--color-ts) 55%, transparent);
    color: var(--color-fg);
  }
}

/* Troca de tema (src/theme/fade-theme-change.ts): a View Transitions API
   tira uma "foto" da página com o tema velho e faz um cross-fade para o
   novo. */
::view-transition-old(root),
::view-transition-new(root) {
  animation-duration: 250ms;
}

/* Durante a troca, nenhuma transição CSS roda. Sem isto, cada link com
   transition-colors desbotaria no seu próprio ritmo. */
[data-theme-switching] *,
[data-theme-switching] *::before,
[data-theme-switching] *::after {
  /* biome-ignore lint/complexity/noImportantStyles: precisa vencer as classes transition-* dos componentes */
  transition: none !important;
}

@media (scripting: enabled) {
  [data-intro]:not([data-intro="preloader"]) {
    visibility: hidden;
  }
}

/* ^= é "começa com": pega o fundo (preloader) e a logo (preloader-logo). */
@media (scripting: none) {
  [data-intro^="preloader"] {
    display: none;
  }
}
```

- [ ] **Passo 3: Trocar as classes de fundo pelos nomes novos**

```bash
cd /home/martins/projects/martinsportfolio
sed -i 's/bg-ink-900/bg-page/g' "src/app/[lang]/layout.tsx" src/app/global-error.tsx
sed -i 's/bg-ink-800/bg-surface/g' src/components/hero/hero-grid.tsx src/components/preferences/preferences-menu.tsx
grep -rn "ink-[0-9]" src || echo "nenhum ink- sobrando"
```

Expected: `nenhum ink- sobrando`.

- [ ] **Passo 4: Criar o `ThemeSync`**

`src/theme/theme-sync.tsx`:

```tsx
"use client";

import { useLayoutEffect } from "react";
import { applyTheme, readTheme } from "./theme-store";

/**
 * Põe o tema salvo no <html> depois que o React assume a página. Não
 * desenha nada.
 *
 * O script do <head> já fez isso antes da primeira pintura, mas:
 * - no modo dev, o Strict Mode monta tudo duas vezes e, na segunda, o
 *   React limpa os atributos do <html> que ele não conhece;
 * - o global-error desenha um <html> novo, onde o script não roda;
 * - a cor da barra do navegador só é acertada aqui.
 * useLayoutEffect roda antes de o navegador pintar, então nada pisca.
 */
export function ThemeSync() {
  useLayoutEffect(() => {
    applyTheme(readTheme());
  }, []);

  return null;
}
```

- [ ] **Passo 5: Script, `ThemeSync` e barra do navegador no layout**

Em `src/app/[lang]/layout.tsx`, depois do import de `themeColors`, acrescente:

```tsx
import { themeScript } from "@/theme/theme-script";
import { ThemeSync } from "@/theme/theme-sync";
```

Troque o `viewport` por:

```tsx
export const viewport: Viewport = {
  // Cor da barra do navegador, uma para cada tema do sistema. Com
  // escolha forçada no painel, o applyTheme (src/theme/theme-store.ts)
  // troca as duas.
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: themeColors.pageLight },
    { media: "(prefers-color-scheme: dark)", color: themeColors.pageDark },
  ],
  // Avisa o navegador, antes de o CSS chegar, que a página tem os dois temas.
  colorScheme: "light dark",
};
```

Troque o `return` do `RootLayout` por:

```tsx
  return (
    // suppressHydrationWarning: o script do <head> põe data-theme no
    // <html> antes de o React carregar. Isto diz ao React para aceitar o
    // que está no DOM em vez de reclamar da diferença.
    <html
      lang={localeInfo[lang].htmlLang}
      className={`${fontVariables} h-full antialiased`}
      suppressHydrationWarning
    >
      <head>
        {/* Roda enquanto o navegador lê o HTML, antes da primeira pintura:
            a escolha salva aparece sem piscar o outro tema. */}
        <script
          // biome-ignore lint/security/noDangerouslySetInnerHtml: texto fixo, montado em theme-script.ts só com constantes do projeto
          dangerouslySetInnerHTML={{ __html: themeScript }}
        />
      </head>
      <body className="flex min-h-full flex-col bg-page font-sans text-fg">
        <ThemeSync />
        {children}
      </body>
    </html>
  );
```

- [ ] **Passo 6: `ThemeSync` no `global-error.tsx`**

Em `src/app/global-error.tsx`, acrescente o import (depois do de `fontVariables`):

```tsx
import { ThemeSync } from "@/theme/theme-sync";
```

E troque o `return` por:

```tsx
  return (
    <html
      lang={localeInfo[locale].htmlLang}
      className={`${fontVariables} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full bg-page font-sans text-fg">
        {/* Esta tela troca o <html> inteiro depois que a página carregou,
            e script inserido pelo React não roda: quem aplica o tema
            salvo aqui é o ThemeSync. */}
        <ThemeSync />
        <title>{errorTexts[locale].title}</title>
        <ErrorScreen {...props} />
      </body>
    </html>
  );
```

- [ ] **Passo 7: Lint, testes e build**

```bash
cd /home/martins/projects/martinsportfolio
pnpm lint
pnpm test
pnpm build 2>&1 | tail -25
```

Expected: lint sem erros; testes passando; `/pt-br` e `/en` estáticas (`●`).

- [ ] **Passo 8: Conferir o CSS gerado**

```bash
cd /home/martins/projects/martinsportfolio
CSS=$(grep -l "color-scheme" $(find .next/static -name "*.css"))
echo "arquivo: $CSS"
grep -o "\-\-color-page:[^;]*" $CSS | head -2
grep -c "light-dark(" $CSS
grep -c "lightningcss" $CSS
grep -o "prefers-color-scheme:light)[^{]*{[^}]*" $CSS | head -2
```

Expected: `--color-page:light-dark(#f4f7fb,#070c17)` (ou equivalente minificado) e várias ocorrências de `light-dark(`. Se aparecer `--lightningcss-light` no lugar, o minificador trocou `light-dark()` por variáveis: funciona igual, mas anote no commit e confira os quatro casos do passo 11 com atenção.

- [ ] **Passo 9: O escuro continua idêntico**

Suba o build de produção (receita). Depois:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media dark reduced-motion
agent-browser set viewport 1440 900 1
agent-browser open http://localhost:3100/pt-br
agent-browser wait 4000
agent-browser screenshot "$SCRATCH/depois2-escuro-1440.png"
agent-browser set viewport 375 812 1
agent-browser reload
agent-browser wait 4000
agent-browser screenshot "$SCRATCH/depois2-escuro-375.png"
node "$SCRATCH/diff.cjs" "$SCRATCH/antes-escuro-1440.png" "$SCRATCH/depois2-escuro-1440.png"
node "$SCRATCH/diff.cjs" "$SCRATCH/antes-escuro-375.png" "$SCRATCH/depois2-escuro-375.png"
```

Expected: `idênticas` nos dois (ou menos de 0.05% de pixels, espalhados, por serrilhado). Se uma área inteira mudou, abra as duas imagens com a ferramenta Read e ache o token com valor errado no escuro.

- [ ] **Passo 10: Sem piscar no carregamento**

Com o servidor ainda de pé:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set viewport 1440 900 1
agent-browser set media dark
agent-browser open http://localhost:3100/en
agent-browser eval "localStorage.setItem('theme', 'light')"
agent-browser record start "$SCRATCH/carrega-claro-com-so-escuro.webm" http://localhost:3100/en --contact-sheet
agent-browser wait 2500
agent-browser record stop
agent-browser set media light
agent-browser eval "localStorage.setItem('theme', 'dark')"
agent-browser record start "$SCRATCH/carrega-escuro-com-so-claro.webm" http://localhost:3100/en --contact-sheet
agent-browser wait 2500
agent-browser record stop
agent-browser eval "localStorage.removeItem('theme')"
agent-browser record start "$SCRATCH/carrega-sistema-claro.webm" http://localhost:3100/en --contact-sheet
agent-browser wait 2500
agent-browser record stop
agent-browser set media dark
agent-browser record start "$SCRATCH/carrega-sistema-escuro.webm" http://localhost:3100/en --contact-sheet
agent-browser wait 2500
agent-browser record stop
```

Abra os quatro contact sheets (o caminho aparece na saída do `record stop`) com a ferramenta Read. Expected: em cada um, **o fundo tem a mesma cor do primeiro ao último quadro**: claro em `carrega-claro-com-so-escuro` e `carrega-sistema-claro`; escuro em `carrega-escuro-com-so-claro` e `carrega-sistema-escuro`. O hero do claro ainda está incompleto (cards escuros, vinheta escura): isso é a Tarefa 3; aqui só importa o fundo.

- [ ] **Passo 11: Os quatro casos de escolha × sistema**

Primeiro, o HTML que sai do servidor não tem `data-theme` (quem põe é o script, no navegador; sem JavaScript, fica o "Sistema"):

```bash
curl -s http://localhost:3100/en | grep -o "<html[^>]*>"
curl -s http://localhost:3100/en | grep -c 'localStorage.getItem("theme")'
```

Expected: a tag `<html>` sem `data-theme`, e `1` (o script inline está na página).

Depois:

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
CHECK="JSON.stringify({ theme: document.documentElement.dataset.theme ?? null, bg: getComputedStyle(document.body).backgroundColor, scheme: getComputedStyle(document.documentElement).colorScheme, metas: [...document.querySelectorAll('meta[name=theme-color]')].map((m) => m.media + ' ' + m.content) })"
for caso in "light dark" "dark light" "none light" "none dark"; do
  set -- $caso
  agent-browser set media "$2"
  if [ "$1" = none ]; then agent-browser eval "localStorage.removeItem('theme')"; else agent-browser eval "localStorage.setItem('theme', '$1')"; fi
  agent-browser reload
  agent-browser wait 2000
  echo "salvo=$1 sistema=$2 -> $(agent-browser eval "$CHECK")"
done
```

Expected:

| salvo | sistema | `theme` | `bg` | `metas` |
| --- | --- | --- | --- | --- |
| light | dark | `light` | `rgb(244, 247, 251)` | as duas com `#f4f7fb` |
| dark | light | `dark` | `rgb(7, 12, 23)` | as duas com `#070c17` |
| none | light | `null` | `rgb(244, 247, 251)` | light `#f4f7fb`, dark `#070c17` |
| none | dark | `null` | `rgb(7, 12, 23)` | light `#f4f7fb`, dark `#070c17` |

- [ ] **Passo 12: O sistema muda com a página aberta (Review Focus 5)**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
BG="getComputedStyle(document.body).backgroundColor"
agent-browser eval "localStorage.removeItem('theme')"
agent-browser set media dark
agent-browser reload
agent-browser wait 2000
echo "sistema, SO escuro: $(agent-browser eval "$BG")"
agent-browser set media light
agent-browser wait 300
echo "sistema, SO mudou para claro sem recarregar: $(agent-browser eval "$BG")"
agent-browser eval "localStorage.setItem('theme', 'dark')"
agent-browser reload
agent-browser wait 2000
agent-browser set media light
agent-browser wait 300
echo "forçado escuro, SO claro: $(agent-browser eval "$BG")"
agent-browser eval "localStorage.removeItem('theme')"
agent-browser close
```

Expected: `rgb(7, 12, 23)`, depois `rgb(244, 247, 251)` (acompanhou sem recarregar), depois `rgb(7, 12, 23)` (escolha forçada não segue o SO). Pare o servidor (receita).

- [ ] **Passo 13: Modo dev (Strict Mode)**

Suba o servidor de desenvolvimento (receita). Depois:

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media dark
agent-browser open http://localhost:3100/en
agent-browser wait 6000
agent-browser eval "localStorage.setItem('theme', 'light')"
agent-browser reload
agent-browser wait 6000
agent-browser eval "JSON.stringify({ theme: document.documentElement.dataset.theme ?? null, bg: getComputedStyle(document.body).backgroundColor })"
agent-browser console
agent-browser errors
agent-browser eval "localStorage.removeItem('theme')"
agent-browser close
```

Expected: `{"theme":"light","bg":"rgb(244, 247, 251)"}` mesmo depois da remontagem do Strict Mode. No console, nenhum erro de hidratação. Um aviso só de dev sobre `<script>` é aceitável: registre o texto no commit. Pare o servidor (receita).

- [ ] **Passo 14: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add src/app/globals.css src/theme/theme-sync.tsx "src/app/[lang]/layout.tsx" src/app/global-error.tsx src/components/hero/hero-grid.tsx src/components/preferences/preferences-menu.tsx
git commit -m "feat: move the colors to light-dark tokens and apply the saved theme before the first paint

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 3: O claro no hero, na logo e nos detalhes

**Files:**
- Create: `public/hero/card-01-light.svg` … `public/hero/card-08-light.svg`
- Modify: `src/data/hero-cards.ts`
- Modify: `src/components/hero/hero-card.tsx`
- Modify: `src/components/hero/hero-grid.module.css`
- Modify: `src/components/hero/hero-title.tsx`
- Modify: `src/components/hero/hero-info.tsx`
- Modify: `src/components/hero/hero-spotlight.tsx`
- Modify: `src/components/ui/button-styles.ts`
- Modify: `src/components/preferences/preferences-menu.tsx` (sombra do painel)
- Modify: `src/components/ui/logo.tsx`
- Modify: `src/app/[lang]/opengraph-image.tsx`

**Interfaces:**
- Consumes: tokens, variantes `light:`/`dark:` e `subtitle-glow` (Tarefa 2); `themeColors.fg` (Tarefa 1).
- Produces:
  - `HeroCardData` com o campo novo `lightSrc: string`.
  - `Logo` com a prop nova `strokeColor?: string` (padrão `"currentColor"`).

- [ ] **Passo 1: Gerar os 8 cards claros**

A receita é a do mockup aprovado. Fundo branco, linhas escuras, linhas de marca a 42%, brilho mais fraco:

```bash
cd /home/martins/projects/martinsportfolio
for i in 01 02 03 04 05 06 07 08; do
  sed -E \
    -e 's/fill="#ffffff" fill-opacity="0.07"/fill="#0b1220" fill-opacity="0.06"/' \
    -e 's/fill="#ffffff"/fill="#0b1220"/g' \
    -e 's/fill="#0c1424"/fill="#ffffff"/' \
    -e 's/#e2e8f0/#334155/g' \
    -e 's/#61dafb/#1b9ccb/g' \
    -e 's/fill-opacity="0.85"/fill-opacity="0.42"/g' \
    -e 's/stop-opacity="0.55"/stop-opacity="0.20"/' \
    -e 's/stop-opacity="0.12"/stop-opacity="0.08"/' \
    "public/hero/card-$i.svg" > "public/hero/card-$i-light.svg"
done
grep -c 'fill="#0c1424"' public/hero/card-0*-light.svg
grep -c 'fill="#ffffff"' public/hero/card-0*-light.svg
```

A ordem dos `-e` importa: os brancos viram escuros **antes** de o fundo virar branco. Expected: `0` em todos na primeira contagem e `1` em todos na segunda (o fundo).

- [ ] **Passo 2: Cada card com a versão clara**

Substitua `src/data/hero-cards.ts` inteiro por:

```ts
export type HeroCardData = {
  /** Versão do tema escuro. */
  src: string;
  /**
   * Versão do tema claro: fundo branco e linhas mais suaves, para não
   * brigar com o subtítulo que fica por cima.
   */
  lightSrc: string;
  /** Nome da área no grid-template-areas (ver hero-grid.module.css). */
  area: string;
  /** Some no celular, onde a grade tem só 2 colunas. */
  desktopOnly?: boolean;
};

// A ordem segue as colunas da esquerda para a direita: a timeline usa
// `stagger: { from: "edges" }`, então as colunas de fora entram primeiro.
export const heroCards: HeroCardData[] = [
  { src: "/hero/card-01.svg", lightSrc: "/hero/card-01-light.svg", area: "a" },
  { src: "/hero/card-02.svg", lightSrc: "/hero/card-02-light.svg", area: "b" },
  { src: "/hero/card-03.svg", lightSrc: "/hero/card-03-light.svg", area: "c" },
  { src: "/hero/card-04.svg", lightSrc: "/hero/card-04-light.svg", area: "d" },
  { src: "/hero/card-05.svg", lightSrc: "/hero/card-05-light.svg", area: "e" },
  { src: "/hero/card-06.svg", lightSrc: "/hero/card-06-light.svg", area: "f" },
  {
    src: "/hero/card-07.svg",
    lightSrc: "/hero/card-07-light.svg",
    area: "g",
    desktopOnly: true,
  },
  {
    src: "/hero/card-08.svg",
    lightSrc: "/hero/card-08-light.svg",
    area: "h",
    desktopOnly: true,
  },
];
```

Substitua `src/components/hero/hero-card.tsx` inteiro por:

```tsx
import Image from "next/image";
import { INTRO } from "@/animations/intro-targets";
import type { HeroCardData } from "@/data/hero-cards";

const SIZES = "(min-width: 48rem) 25vw, 50vw";

export function HeroCard({ card }: { card: HeroCardData }) {
  return (
    <div
      data-intro={INTRO.card}
      style={{ gridArea: card.area }}
      className={`relative overflow-hidden rounded-sm ${card.desktopOnly ? "hidden md:block" : ""}`}
    >
      {/* Uma imagem por tema; o CSS mostra só a do tema atual. As duas
          carregam de cara (eager): a intro espera todas as <img>
          ficarem prontas, e uma imagem "preguiçosa" escondida nunca
          carregaria. São SVGs de 2 KB. */}
      <Image
        src={card.src}
        alt=""
        fill
        loading="eager"
        sizes={SIZES}
        className="object-cover light:hidden"
      />
      <Image
        src={card.lightSrc}
        alt=""
        fill
        loading="eager"
        sizes={SIZES}
        className="object-cover dark:hidden"
      />
    </div>
  );
}
```

- [ ] **Passo 3: Vinheta, título e informações do hero**

Em `src/components/hero/hero-grid.module.css`, troque o `background` do `.grid::after` por:

```css
  /* Névoa atrás do título: escura no escuro, clara no claro. */
  background: radial-gradient(
    ellipse 62% 48% at center,
    light-dark(rgb(244 247 251 / 0.82), rgb(4 7 14 / 0.72)),
    light-dark(rgb(244 247 251 / 0.35), rgb(4 7 14 / 0.3)) 60%,
    transparent
  );
```

Em `src/components/hero/hero-title.tsx`, troque o `<p>` do subtítulo e o `<span>` dele por:

```tsx
      <p
        data-intro={INTRO.subtitle}
        className="mt-[clamp(0.75rem,1.6cqi,1.75rem)] text-[clamp(1.125rem,2.6cqi,2.75rem)] font-medium tracking-[-0.01em] subtitle-glow"
      >
        <span className="bg-linear-to-r from-gradient-from via-gradient-via to-gradient-to bg-clip-text text-transparent">
          {dict.hero.role}
        </span>
      </p>
```

Em `src/components/hero/hero-info.tsx`, troque o `className` do link social por (sai `hover:text-react`, `md:min-h-0` e `md:py-[0.3em]`: os links ficam com 44px de altura também no desktop, o mínimo do AAA 2.5.5):

```tsx
              className="group inline-flex min-h-11 items-center gap-[0.4em] text-fg-muted transition-colors duration-200 hover:text-highlight"
```

- [ ] **Passo 4: Holofote, botão e painel no claro**

Em `src/components/hero/hero-spotlight.tsx`, troque a constante `LAYERS` por:

```ts
// Uma camada por cor. No claro, cor sobre fundo claro pesa mais: as
// camadas têm metade da força.
const LAYERS = [
  "from-ts/35 light:from-ts/18",
  "from-react/22 light:from-react/12",
  "from-node/35 light:from-node/18",
];
```

Substitua `src/components/ui/button-styles.ts` inteiro por:

```ts
// No claro, o brilho verde em volta do botão vira uma sombra verde
// embaixo: brilho sobre fundo claro some, e a sombra dá definição ao
// botão (o verde contra o fundo claro tem pouco contraste).
export const buttonClassName =
  "inline-flex items-center justify-center rounded-full bg-accent px-[1.9em] py-[0.85em] font-semibold tracking-tight text-accent-foreground shadow-[0_0_48px_-12px_var(--color-node-light)] transition duration-200 hover:-translate-y-0.5 hover:bg-accent-strong hover:shadow-[0_0_56px_-8px_var(--color-node-light)] active:translate-y-0 light:shadow-[0_6px_24px_-10px_rgb(63_130_40/0.6)] light:hover:shadow-[0_10px_28px_-10px_rgb(63_130_40/0.7)]";
```

Em `src/components/preferences/preferences-menu.tsx`, no `className` do painel, troque `shadow-2xl shadow-black/40` por `shadow-2xl shadow-black/40 light:shadow-black/10`.

- [ ] **Passo 5: Logo com a cor do contorno por prop**

Em `src/components/ui/logo.tsx`, troque o tipo `LogoProps` e a assinatura do componente por:

```tsx
type LogoProps = ComponentProps<"svg"> & {
  /**
   * Precisa ser único na página: a máscara e o gradiente são encontrados
   * pelo id (`url(#...)`), e duas logos com o mesmo id pegariam as peças
   * uma da outra. Não dá para usar `useId` porque a imagem de Open Graph
   * desenha a logo fora do React, onde hooks não rodam.
   */
  id: string;
  /**
   * Cor dos contornos. Na página, o padrão `currentColor` pega a cor do
   * texto em volta (o `text-fg` do <body>), que muda com o tema. A imagem
   * de Open Graph passa um hex: o Satori, que desenha a imagem, não
   * entende `currentColor`.
   */
  strokeColor?: string;
};

export function Logo({ id, strokeColor = "currentColor", ...props }: LogoProps) {
```

Troque os quatro `stroke={c.fg}` por `stroke={strokeColor}`:

```bash
cd /home/martins/projects/martinsportfolio
sed -i 's/stroke={c\.fg}/stroke={strokeColor}/g' src/components/ui/logo.tsx
grep -c "stroke={strokeColor}" src/components/ui/logo.tsx
grep -n "c\.fg" src/components/ui/logo.tsx || echo "nenhum c.fg sobrando"
```

Expected: `4` e `nenhum c.fg sobrando`.

Em `src/app/[lang]/opengraph-image.tsx`, na `<Logo>`, acrescente a prop:

```tsx
      <Logo
        id="og-logo"
        strokeColor={c.fg}
        width={120}
        height={56}
        style={{ position: "absolute", top: 52, left: 72 }}
      />
```

- [ ] **Passo 6: Lint, testes e build**

```bash
cd /home/martins/projects/martinsportfolio
pnpm lint
pnpm test
pnpm build 2>&1 | tail -25
```

Expected: lint sem erros; testes passando; `/pt-br` e `/en` estáticas.

- [ ] **Passo 7: O escuro e a imagem de OG continuam iguais**

Suba o build de produção (receita). Depois:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media dark reduced-motion
agent-browser set viewport 1440 900 1
agent-browser open http://localhost:3100/pt-br
agent-browser wait 4000
agent-browser screenshot "$SCRATCH/depois3-escuro-1440.png"
agent-browser set viewport 375 812 1
agent-browser reload
agent-browser wait 4000
agent-browser screenshot "$SCRATCH/depois3-escuro-375.png"
node "$SCRATCH/diff.cjs" "$SCRATCH/antes-escuro-1440.png" "$SCRATCH/depois3-escuro-1440.png"
node "$SCRATCH/diff.cjs" "$SCRATCH/antes-escuro-375.png" "$SCRATCH/depois3-escuro-375.png"
OG=$(curl -s http://localhost:3100/en | grep -o 'property="og:image" content="[^"]*"' | head -1 | sed 's/.*content="//;s/"$//;s/&amp;/\&/g')
curl -s "$OG" -o "$SCRATCH/depois-og-en.png"
node "$SCRATCH/diff.cjs" "$SCRATCH/antes-og-en.png" "$SCRATCH/depois-og-en.png"
```

Expected:
- Escuro 375: `idênticas` (no celular, os links sociais já tinham 44px).
- Escuro 1440: só a área dos links sociais muda (canto inferior direito do hero, onde GitHub e LinkedIn ficam empilhados). Confira abrindo as duas imagens com a ferramenta Read.
- OG: `idênticas`.

- [ ] **Passo 8: O claro na tela**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media light reduced-motion
for size in "1440 900" "375 812" "320 640"; do
  set -- $size
  agent-browser set viewport "$1" "$2" 1
  agent-browser open http://localhost:3100/pt-br
  agent-browser wait 4000
  agent-browser screenshot "$SCRATCH/claro-$1.png"
done
agent-browser set viewport 375 812 1
agent-browser open http://localhost:3100/pt-br/nao-existe
agent-browser wait 1500
agent-browser screenshot "$SCRATCH/claro-404-375.png"
agent-browser set media dark reduced-motion
agent-browser reload
agent-browser wait 1500
agent-browser screenshot "$SCRATCH/escuro-404-375.png"
node "$SCRATCH/diff.cjs" "$SCRATCH/antes-404-375.png" "$SCRATCH/escuro-404-375.png"
```

Abra as imagens com a ferramenta Read. Expected: `claro-1440` igual à "Opção 3 com cores AAA" aprovada no mockup (cards claros e calmos, título escuro, subtítulo azul → ciano → verde com halo branco, botão verde com sombra embaixo, logo com contorno escuro). O "Full Stack" não se mistura com as linhas verdes. Em 375 e 320, título e subtítulo legíveis sobre a grade de 2 colunas. O 404 claro com fundo claro e texto escuro. O 404 escuro: `idênticas` na comparação com o de antes.

- [ ] **Passo 9: Contraste AAA medido na tela**

Crie `$SCRATCH/probe-boxes.js` e `$SCRATCH/contrast.cjs` com o conteúdo da seção "Receitas". Depois, para cada combinação de tema e largura, siga a receita "Medir o contraste de uma tela":

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
for tema in light dark; do
  for size in "1440 900" "375 812" "320 640"; do
    set -- $size
    agent-browser set media "$tema" reduced-motion
    agent-browser set viewport "$1" "$2" 1
    agent-browser open http://localhost:3100/pt-br
    agent-browser wait 4000
    NOME="$tema-$1"
    agent-browser eval "$(cat "$SCRATCH/probe-boxes.js")" > "$SCRATCH/boxes-$NOME.txt"
    agent-browser screenshot "$SCRATCH/contraste-$NOME.png"
    echo "== $NOME"
    node "$SCRATCH/contrast.cjs" "$SCRATCH/contraste-$NOME.png" "$SCRATCH/boxes-$NOME.txt"
  done
done
```

Expected: todas as linhas `OK`.

**Se só o `subtitulo` falhar**, meça de novo com o halo pintado (o halo existe para ser o fundo do subtítulo; a medida pessimista some com ele). Na mesma combinação que falhou:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
NOME=halo-light-1440
agent-browser reload
agent-browser wait 4000
agent-browser eval "(() => { const s = document.querySelector('h1 + p span'); const light = getComputedStyle(document.body).backgroundColor === 'rgb(244, 247, 251)'; s.style.backgroundImage = 'none'; s.style.color = light ? '#ffffff' : '#04070e'; const r = s.getBoundingClientRect(); return JSON.stringify([{ label: 'subtitulo', x: r.x, y: r.y, w: r.width, h: r.height, colors: light ? ['#1a5299', '#085975', '#265e1d'] : ['#6aa8f5', '#61dafb', '#8cc84b'], goal: 7 }]); })()" > "$SCRATCH/boxes-$NOME.txt"
agent-browser screenshot "$SCRATCH/contraste-$NOME.png"
node "$SCRATCH/contrast.cjs" "$SCRATCH/contraste-$NOME.png" "$SCRATCH/boxes-$NOME.txt"
```

O texto pintado na cor do halo desenha o halo e fica "invisível" contra ele. Se passar, registre no commit que o subtítulo passa com o halo. Se falhar também assim, reforce a vinheta em `hero-grid.module.css`: claro `0.82 → 0.9` e `0.35 → 0.45`; escuro `0.72 → 0.8` e `0.3 → 0.38`. Depois refaça o build e as medidas deste passo.

**Se outro texto falhar**, pare e reporte: os tokens da spec foram calculados para passar sobre `page` e `surface`, então uma falha fora da colagem indica token errado.

- [ ] **Passo 10: Holofote no claro**

O holofote só liga com mouse de verdade e sem "reduzir movimento":

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media light
agent-browser set viewport 1440 900 1
agent-browser open http://localhost:3100/pt-br
agent-browser wait 9000
agent-browser hover "main h1"
agent-browser wait 1500
agent-browser hover "main a[target=_blank]"
agent-browser wait 1500
agent-browser screenshot "$SCRATCH/claro-holofote.png"
agent-browser close
```

Abra com a ferramenta Read. Expected: uma mancha de cor suave atrás da colagem, perto do ponteiro, sem escurecer a página nem sujar o texto. Se a mancha não aparecer, o `hover:hover` do navegador sem tela pode estar desligado: registre e siga. Se estiver forte demais (a cor domina o fundo), reduza as camadas claras para `ts/12`, `react/8`, `node/12` e refaça. Pare o servidor (receita).

- [ ] **Passo 11: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add public/hero src/data/hero-cards.ts src/components/hero src/components/ui/button-styles.ts src/components/ui/logo.tsx src/components/preferences/preferences-menu.tsx "src/app/[lang]/opengraph-image.tsx"
git commit -m "feat: give the hero, the logo and the button a light version

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 4: Seletor de tema no painel, com fade na troca

**Files:**
- Create: `src/theme/fade-theme-change.ts`
- Create: `src/theme/use-theme.ts`
- Create: `src/components/ui/monitor-icon.tsx`
- Create: `src/components/ui/sun-icon.tsx`
- Create: `src/components/ui/moon-icon.tsx`
- Create: `src/components/preferences/theme-switch.tsx`
- Modify: `src/components/preferences/preferences-menu.tsx`
- Modify: `src/i18n/dictionaries/pt-br.ts`, `src/i18n/dictionaries/en.ts`

**Interfaces:**
- Consumes: `themes`, `Theme`, `readTheme()`, `saveTheme()`, `subscribeTheme()`, `applyTheme()` (Tarefa 1); o CSS do fade e a variante `noscript:` (Tarefa 2).
- Produces:
  - `fadeThemeChange(update: () => void): void`
  - `useTheme(): readonly [Theme, (next: Theme) => void]`
  - `<ThemeSwitch legend={string} labels={Record<Theme, string>} />`
  - `MonitorIcon`, `SunIcon`, `MoonIcon` (`ComponentProps<"svg">`)
  - Dicionário: `preferences.theme` e `preferences.themes.{system,light,dark}`

- [ ] **Passo 1: Textos nos dicionários**

Em `src/i18n/dictionaries/pt-br.ts`, troque o bloco `preferences` por:

```ts
  preferences: {
    open: "Preferências",
    language: "Idioma",
    theme: "Tema",
    themes: {
      system: "Sistema",
      light: "Claro",
      dark: "Escuro",
    },
  },
```

Em `src/i18n/dictionaries/en.ts`, troque o bloco `preferences` por:

```ts
  preferences: {
    open: "Preferences",
    language: "Language",
    theme: "Theme",
    themes: {
      system: "System",
      light: "Light",
      dark: "Dark",
    },
  },
```

- [ ] **Passo 2: O fade da troca**

`src/theme/fade-theme-change.ts`:

```ts
const REDUCED_MOTION = "(prefers-reduced-motion: reduce)";

/**
 * Trocas em andamento. Clicar rápido pode começar uma troca antes de a
 * anterior acabar; o atributo só sai quando a última termina.
 */
let pending = 0;

/**
 * Roda `update` (a troca de tema) com um fade curto. A View Transitions
 * API tira uma "foto" da página, roda `update` e faz um cross-fade da
 * foto para a página nova (a duração está no globals.css).
 *
 * Sem suporte no navegador, ou com "reduzir movimento", troca direto.
 * Nos dois casos, `data-theme-switching` no <html> desliga as transições
 * CSS durante a troca (ver globals.css).
 */
export function fadeThemeChange(update: () => void) {
  const root = document.documentElement;
  pending += 1;
  root.setAttribute("data-theme-switching", "");

  const done = () => {
    pending -= 1;
    if (pending === 0) root.removeAttribute("data-theme-switching");
  };

  if (
    !("startViewTransition" in document) ||
    matchMedia(REDUCED_MOTION).matches
  ) {
    update();
    // Dois quadros: o navegador pinta o tema novo antes de as transições
    // voltarem.
    requestAnimationFrame(() => requestAnimationFrame(done));
    return;
  }

  // Começar uma troca durante outra pula a anterior. `finished` resolve
  // mesmo assim, então o contador sempre volta a zero.
  document.startViewTransition(update).finished.finally(done);
}
```

- [ ] **Passo 3: O hook**

`src/theme/use-theme.ts`:

```ts
import { useSyncExternalStore } from "react";
import { fadeThemeChange } from "./fade-theme-change";
import {
  applyTheme,
  readTheme,
  saveTheme,
  subscribeTheme,
} from "./theme-store";
import type { Theme } from "./themes";

/** O servidor não sabe a escolha: o HTML sai com "Sistema" marcado. */
const serverTheme = (): Theme => "system";

/**
 * Tema escolhido e a função que troca.
 *
 * `useSyncExternalStore` é o jeito do React de ler algo que vive fora
 * dele (aqui, a escolha salva no navegador). No servidor usa
 * `serverTheme`; no navegador, logo depois de hidratar, passa para o
 * valor salvo sem erro de hidratação. `subscribeTheme` avisa o React a
 * cada troca.
 */
export function useTheme() {
  const theme = useSyncExternalStore(subscribeTheme, readTheme, serverTheme);

  function setTheme(next: Theme) {
    fadeThemeChange(() => {
      saveTheme(next);
      applyTheme(next);
    });
  }

  return [theme, setTheme] as const;
}
```

- [ ] **Passo 4: Os ícones**

`src/components/ui/monitor-icon.tsx`:

```tsx
import type { ComponentProps } from "react";

// Ícone "monitor" do Lucide (licença ISC). Fica ao lado do texto
// "Sistema", que já diz o que é, então o SVG fica escondido.
export function MonitorIcon(props: ComponentProps<"svg">) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      <rect width="20" height="14" x="2" y="3" rx="2" />
      <path d="M8 21h8M12 17v4" />
    </svg>
  );
}
```

`src/components/ui/sun-icon.tsx`:

```tsx
import type { ComponentProps } from "react";

// Ícone "sun" do Lucide (licença ISC). Fica ao lado do texto "Claro",
// que já diz o que é, então o SVG fica escondido.
export function SunIcon(props: ComponentProps<"svg">) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      <circle cx="12" cy="12" r="4" />
      <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41" />
    </svg>
  );
}
```

`src/components/ui/moon-icon.tsx`:

```tsx
import type { ComponentProps } from "react";

// Ícone "moon" do Lucide (licença ISC). Fica ao lado do texto "Escuro",
// que já diz o que é, então o SVG fica escondido.
export function MoonIcon(props: ComponentProps<"svg">) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" />
    </svg>
  );
}
```

- [ ] **Passo 5: O seletor**

`src/components/preferences/theme-switch.tsx`:

```tsx
"use client";

import type { ComponentProps, ComponentType } from "react";
import { MonitorIcon } from "@/components/ui/monitor-icon";
import { MoonIcon } from "@/components/ui/moon-icon";
import { SunIcon } from "@/components/ui/sun-icon";
import { type Theme, themes } from "@/theme/themes";
import { useTheme } from "@/theme/use-theme";

const icons: Record<Theme, ComponentType<ComponentProps<"svg">>> = {
  system: MonitorIcon,
  light: SunIcon,
  dark: MoonIcon,
};

type ThemeSwitchProps = {
  /** "Tema" / "Theme". */
  legend: string;
  /** Nome de cada opção no idioma da página. */
  labels: Record<Theme, string>;
};

/**
 * Controle segmentado do tema. Por baixo são três botões de rádio
 * nativos num <fieldset>: o navegador já cuida do teclado (Tab entra no
 * grupo, as setas trocam a opção) e do que o leitor de tela anuncia
 * ("Claro, botão de opção, 2 de 3"). O <input> fica invisível (sr-only)
 * e quem desenha o segmento é o <label>, que reage ao rádio de dentro
 * com has-checked e has-focus-visible.
 */
export function ThemeSwitch({ legend, labels }: ThemeSwitchProps) {
  const [theme, setTheme] = useTheme();

  return (
    <fieldset>
      <legend className="font-mono text-xs uppercase tracking-widest text-fg-muted">
        {legend}
      </legend>
      <div className="mt-2 grid grid-cols-3 gap-0.5 rounded-[0.625rem] bg-page p-[3px] ring-1 ring-line">
        {themes.map((option) => {
          const Icon = icons[option];
          return (
            <label
              key={option}
              className="flex min-h-12 cursor-pointer flex-col items-center justify-center gap-1 rounded-[0.4375rem] text-xs text-fg-muted transition-colors duration-200 hover:text-fg has-checked:bg-surface has-checked:text-fg has-checked:ring-1 has-checked:ring-fg-muted has-focus-visible:outline-2 has-focus-visible:outline-offset-2 has-focus-visible:outline-highlight"
            >
              <input
                type="radio"
                name="theme"
                value={option}
                checked={theme === option}
                onChange={() => setTheme(option)}
                className="peer sr-only"
              />
              <Icon className="size-[1.0625rem] peer-checked:text-highlight" />
              {labels[option]}
            </label>
          );
        })}
      </div>
    </fieldset>
  );
}
```

- [ ] **Passo 6: O grupo "Tema" no painel**

Em `src/components/preferences/preferences-menu.tsx`:

Acrescente o import (depois do import do CSS Module):

```tsx
import { ThemeSwitch } from "./theme-switch";
```

Troque o comentário do componente por:

```tsx
/**
 * Botão da navbar que abre o painel de preferências. Abrir e fechar é
 * trabalho do navegador (atributo `popover`), então isto continua sendo
 * server component. Só os seletores de idioma e de tema rodam no
 * cliente, e recebem os textos por props: os dicionários ficam no
 * servidor.
 */
```

E, dentro do painel, logo depois do `<LanguageSwitch ... />`, acrescente:

```tsx
        {/* noscript:hidden: sem JavaScript, trocar o tema não funciona
            (o site segue o sistema, só com CSS), então o grupo some. */}
        <div className="mt-4 noscript:hidden">
          <ThemeSwitch
            legend={dict.preferences.theme}
            labels={dict.preferences.themes}
          />
        </div>
```

- [ ] **Passo 7: Lint, testes e build**

```bash
cd /home/martins/projects/martinsportfolio
pnpm lint
pnpm test
pnpm build 2>&1 | tail -25
CSS=$(grep -l "color-scheme" $(find .next/static -name "*.css"))
grep -o "scripting:none)[^}]*noscript[^}]*}" $CSS | head -2
```

Expected: lint sem erros; testes passando; `/pt-br` e `/en` estáticas; a regra `@media (scripting:none){.noscript\:hidden{display:none}}` aparece no CSS.

- [ ] **Passo 8: O painel nos dois temas**

Suba o build de produção (receita). Depois:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set viewport 1440 900 1
for tema in dark light; do
  agent-browser set media "$tema" reduced-motion
  agent-browser open http://localhost:3100/pt-br
  agent-browser eval "localStorage.removeItem('theme')"
  agent-browser reload
  agent-browser wait 4000
  agent-browser click "#preferences-button"
  agent-browser wait 400
  agent-browser screenshot "$SCRATCH/painel-$tema.png"
done
```

Abra as duas imagens com a ferramenta Read. Expected: igual ao mockup B aprovado. "Idioma" com a lista, "Tema" com o controle segmentado (monitor, sol, lua + texto), "Sistema" marcado com fundo elevado, borda visível e ícone colorido.

- [ ] **Passo 9: Trocar pelo painel (clique)**

Com o painel aberto e o sistema claro (último estado do passo 8):

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
STATE="JSON.stringify({ theme: document.documentElement.dataset.theme ?? null, saved: localStorage.getItem('theme'), checked: document.querySelector('input[name=theme]:checked')?.value, open: document.getElementById('preferences-panel').matches(':popover-open'), switching: document.documentElement.hasAttribute('data-theme-switching'), bg: getComputedStyle(document.body).backgroundColor, preloader: getComputedStyle(document.querySelector('[data-intro=preloader]')).visibility, url: location.pathname })"
agent-browser click "label:has(input[value=dark])"
agent-browser wait 800
echo "escuro: $(agent-browser eval "$STATE")"
agent-browser click "label:has(input[value=system])"
agent-browser wait 800
echo "sistema: $(agent-browser eval "$STATE")"
```

Expected:
- escuro: `theme` `dark`, `saved` `dark`, `checked` `dark`, `open` `true`, `switching` `false`, `bg` `rgb(7, 12, 23)`, `preloader` `hidden` (a intro não voltou), `url` `/pt-br` (não houve navegação).
- sistema: `theme` `null`, `saved` `null`, `checked` `system`, `open` `true`, `switching` `false`, `bg` `rgb(244, 247, 251)`, `preloader` `hidden`, `url` `/pt-br`.

- [ ] **Passo 10: O fade, e a troca direta com "reduzir movimento"**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media light
agent-browser reload
agent-browser wait 9000
agent-browser click "#preferences-button"
agent-browser wait 400
agent-browser record start "$SCRATCH/troca-com-fade.webm" --fps 60 --contact-sheet
agent-browser click "label:has(input[value=dark])"
agent-browser wait 700
agent-browser record stop
agent-browser set media light reduced-motion
agent-browser click "label:has(input[value=light])"
agent-browser wait 700
agent-browser record start "$SCRATCH/troca-sem-fade.webm" --fps 60 --contact-sheet
agent-browser click "label:has(input[value=dark])"
agent-browser wait 700
agent-browser record stop
```

Abra os dois contact sheets com a ferramenta Read. Expected: no primeiro, quadros intermediários com os dois temas misturados (o cross-fade de ~250ms); no segundo, a troca acontece de um quadro para o outro, sem mistura.

- [ ] **Passo 11: Trocas rápidas seguidas (Review Focus 3)**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
STATE="JSON.stringify({ theme: document.documentElement.dataset.theme ?? null, checked: document.querySelector('input[name=theme]:checked')?.value, switching: document.documentElement.hasAttribute('data-theme-switching') })"
agent-browser set media light
agent-browser eval "for (const v of ['dark', 'light', 'dark', 'system', 'light']) document.querySelector('input[name=theme][value=' + v + ']').click()"
agent-browser wait 1200
echo "depois dos cliques rápidos: $(agent-browser eval "$STATE")"
```

Expected: `{"theme":"light","checked":"light","switching":false}`.

- [ ] **Passo 12: Teclado**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media dark reduced-motion
agent-browser eval "localStorage.removeItem('theme')"
agent-browser reload
agent-browser wait 4000
agent-browser eval "document.getElementById('preferences-button').focus()"
agent-browser press Enter
agent-browser wait 300
agent-browser press Tab
agent-browser press Tab
echo "foco: $(agent-browser eval "document.activeElement.name + '=' + document.activeElement.value")"
agent-browser press ArrowRight
agent-browser wait 600
echo "depois da seta: $(agent-browser eval "JSON.stringify({ foco: document.activeElement.value, theme: document.documentElement.dataset.theme ?? null })")"
agent-browser screenshot "$SCRATCH/teclado-foco.png"
agent-browser press Escape
echo "painel aberto? $(agent-browser eval "document.getElementById('preferences-panel').matches(':popover-open')") | foco: $(agent-browser eval "document.activeElement.id")"
```

Expected: o primeiro Tab vai para o link "English" e o segundo para o rádio marcado: `foco: theme=system`. Depois da seta: `{"foco":"light","theme":"light"}`. A screenshot mostra o anel de foco no segmento "Claro" (abra com Read). Esc fecha o painel e devolve o foco a `preferences-button`.

- [ ] **Passo 13: F5 e troca de idioma mantêm o tema (Review Focus 4)**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set media dark reduced-motion
agent-browser open http://localhost:3100/pt-br
agent-browser wait 4000
agent-browser click "#preferences-button"
agent-browser click "label:has(input[value=light])"
agent-browser wait 800
agent-browser reload
agent-browser wait 4000
agent-browser click "#preferences-button"
agent-browser wait 300
echo "depois do F5: $(agent-browser eval "JSON.stringify({ theme: document.documentElement.dataset.theme ?? null, checked: document.querySelector('input[name=theme]:checked')?.value })")"
agent-browser click "a[hreflang=en]"
agent-browser wait 1500
echo "depois de trocar para inglês: $(agent-browser eval "JSON.stringify({ url: location.pathname, theme: document.documentElement.dataset.theme ?? null, bg: getComputedStyle(document.body).backgroundColor, legend: document.querySelector('legend')?.textContent })")"
agent-browser eval "localStorage.removeItem('theme')"
```

Expected: depois do F5, `{"theme":"light","checked":"light"}` (o rádio certo marcado depois da hidratação). Depois de trocar o idioma: `url` `/en`, `theme` `light`, `bg` `rgb(244, 247, 251)`, `legend` `Theme`.

- [ ] **Passo 14: Painel em telas estreitas**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/14a0f4c1-bc35-4845-8e10-3df6e3b270af/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
for size in "320 640" "375 812"; do
  set -- $size
  agent-browser set viewport "$1" "$2" 1
  agent-browser set media light reduced-motion
  agent-browser open http://localhost:3100/pt-br
  agent-browser wait 4000
  agent-browser click "#preferences-button"
  agent-browser wait 400
  echo "$1px: $(agent-browser eval "(() => { const r = document.getElementById('preferences-panel').getBoundingClientRect(); return JSON.stringify({ semEstouro: document.documentElement.scrollWidth <= innerWidth, painelDentro: r.left >= 0 && r.right <= innerWidth && r.bottom <= innerHeight, alturaSegmento: Math.round(document.querySelector('label:has(input[name=theme])').getBoundingClientRect().height), larguraSegmento: Math.round(document.querySelector('label:has(input[name=theme])').getBoundingClientRect().width) }); })()")"
  agent-browser screenshot "$SCRATCH/painel-$1.png"
done
agent-browser close
```

Expected: nas duas larguras, `semEstouro` `true`, `painelDentro` `true`, `alturaSegmento` pelo menos `48` e `larguraSegmento` pelo menos `44`. Abra as screenshots com a ferramenta Read: os textos "Sistema", "Claro" e "Escuro" cabem sem quebrar. Pare o servidor (receita).

- [ ] **Passo 15: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add src/theme/fade-theme-change.ts src/theme/use-theme.ts src/components/ui/monitor-icon.tsx src/components/ui/sun-icon.tsx src/components/ui/moon-icon.tsx src/components/preferences/theme-switch.tsx src/components/preferences/preferences-menu.tsx src/i18n/dictionaries
git commit -m "feat: add the theme switch to the preferences panel with a short fade

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 5: README e verificação final

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: tudo das tarefas 1 a 4.
- Produces: README atualizado.

- [ ] **Passo 1: Atualizar o README**

Em `README.md`:

1. Logo depois da linha `O site existe em português e inglês: \`/pt-br\` e \`/en\`.`, acrescente uma linha em branco e:

```markdown
Tem tema claro e escuro: segue o sistema operacional ou a escolha feita no painel de preferências.
```

2. Na tabela da Stack, troque a linha do React por:

```markdown
| [React 19](https://react.dev) | Interface. Quase tudo é server component; só a intro, a luz do hero, os seletores de idioma e de tema e as telas de erro rodam no cliente. |
```

3. Troque a linha do Tailwind por:

```markdown
| [Tailwind CSS v4](https://tailwindcss.com) | Estilos. Cores, fontes e animações ficam como tokens no `@theme` do `globals.css`; cada cor que muda com o tema tem os dois valores em `light-dark()`. |
```

4. Troque a linha do `node:test` por:

```markdown
| `node:test` | Testes das funções de idioma e de tema, com o Node rodando TypeScript direto. |
```

5. No bloco "Outros comandos", troque a linha do `pnpm test` por:

```bash
pnpm test       # testes das funções de idioma e de tema (node --test)
```

6. Antes de `## Estrutura`, acrescente a seção:

```markdown
## Tema

O painel de preferências tem três opções: **Sistema** (o padrão), **Claro** e **Escuro**.

- As cores ficam em `src/app/globals.css`. Cada cor que muda com o tema é declarada uma vez, com os dois valores: `light-dark(claro, escuro)`. Quem escolhe o lado é o `color-scheme` do `<html>`: `light dark` segue o sistema; `data-theme="light"` ou `"dark"` força um lado.
- A escolha fica no `localStorage` (chave `theme`). Um script inline no `<head>` (`src/theme/theme-script.ts`) aplica a escolha antes da primeira pintura, então o tema errado não pisca.
- Para o que não é cor, use as variantes `light:` e `dark:` (ex.: `light:hidden`). Elas seguem a mesma regra: escolha forçada primeiro, sistema depois.
- Todo texto passa AAA (7:1) nos dois temas. Ao criar uma cor nova, confira o contraste nos dois.
- Os cards da colagem têm uma versão por tema (`public/hero/card-NN.svg` e `card-NN-light.svg`).
- A imagem de compartilhamento e o manifest são sempre escuros.

```

7. No bloco da estrutura, troque a linha de `preferences/` por:

```
│   ├── preferences/ # botão e painel de preferências (idioma e tema)
```

depois da linha de `i18n/` acrescente:

```
├── theme/           # temas, script que evita piscar, escolha salva e testes
```

e troque a linha de `public/hero/` por:

```
public/hero/         # imagens da colagem (uma versão por tema)
```

8. Em "O que falta", tire a linha `- [ ] Tema sistema/claro/escuro no painel de preferências` do fim e acrescente, logo depois de `- [x] Site em português e inglês, com o idioma escolhido pelo navegador`:

```markdown
- [x] Tema sistema/claro/escuro no painel de preferências, com contraste AAA
```

E troque a linha das imagens da colagem por:

```markdown
- [ ] Imagens reais na colagem: os cards em `public/hero/` ainda são ilustrações provisórias (quando chegarem, decidir se cada uma precisa de versão clara)
```

- [ ] **Passo 2: Verificação completa**

```bash
cd /home/martins/projects/martinsportfolio
pnpm lint
pnpm test
pnpm build 2>&1 | tail -25
git status --short
```

Expected: lint sem erros; testes passando (idioma e tema); `/pt-br` e `/en` estáticas; `git status` mostra só o `README.md`.

Suba o build de produção (receita) e repita, com o código final, o passo 11 da Tarefa 2 (os quatro casos de escolha × sistema) e o passo 9 da Tarefa 3 (contraste nos dois temas, nas três larguras). Expected: os mesmos resultados de lá. Pare o servidor (receita) e feche o navegador (`agent-browser close`).

- [ ] **Passo 3: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add README.md
git commit -m "docs: document the theme tokens, the inline script and the theme switch

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

- [ ] **Passo 4: Depois do deploy (fora desta branch)**

As prévias de PR da Vercel ficam atrás de login, então o PageSpeed só roda depois do merge, em produção: medir `https://smartins.dev/pt-br` e `https://smartins.dev/en` no PageSpeed Insights (não no Lighthouse local) e comparar com a nota de antes do tema. O script inline tem cerca de 200 bytes; não deve mexer na nota.
