# Seção de Projetos com scroll cinematográfico: plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** criar a seção Projetos (`#projects`) com 4 cards de projetos reais. No modo cinema, o scroll vertical faz três coisas numa área fixada: o hero diminui e some enquanto o fundo novo aparece, os cards sobem e a fileira anda na horizontal. Tudo se desfaz ao rolar para cima.

**Architecture:** um client component, `ProjectsStage`, recebe `<Hero />` e `<Projects />` por `children` (os dois continuam server components). Uma variante `cinema:` do Tailwind, definida uma vez no `globals.css`, sobrepõe as duas seções num grid só quando o JS está ligado, o movimento é permitido e a tela é alta o bastante. Fora disso, a seção fica no fluxo normal, com os cards em grade. No modo cinema, um `gsap.matchMedia()` com a mesma condição monta uma timeline com `scrub` e `pin`, com os labels `reveal`, `rise`, `slide` e `slide-end`. As contas da fileira são funções puras, testadas com `node --test`.

**Tech Stack:** Next.js 16.3.5 (App Router, Turbopack), React 19.2.8, TypeScript 5, Tailwind CSS 4 (`@custom-variant`, `light-dark()`), GSAP 3.15.0 + ScrollTrigger + `@gsap/react` 2.1.2, Biome 2.4.2, `node --test` com TypeScript nativo, agent-browser e `sharp` (vem com o Next; só nos scripts de verificação do scratchpad).

**Spec:** `docs/superpowers/specs/2026-09-25-projects-design.md`

## Global Constraints

- Nenhuma dependência nova no `package.json`. O ScrollTrigger já vem no pacote `gsap`.
- As animações de scroll só mexem em `transform` (`scale`, `x`, `y`) e `opacity`. A única outra propriedade é `pointer-events`, que liga e desliga os cliques do hero.
- Não pode ter scroll horizontal na página, em nenhuma largura: `document.documentElement.scrollWidth === document.documentElement.clientWidth`.
- Sem carrossel, sem autoplay e sem `snap`.
- Condição do modo cinema, escrita igual em dois lugares, a variante `cinema` do `globals.css` e o `CINEMA_QUERY` do `projects-stage.tsx`: `(scripting: enabled) and (prefers-reduced-motion: no-preference) and (min-height: 36rem)`.
- A timeline tem os labels `reveal`, `rise`, `slide` e `slide-end`, nessa ordem, exportados como `PHASE` em `src/animations/projects-scroll.ts`.
- O ScrollTrigger do palco usa `pin: true`, `start: "bottom bottom"`, `scrub: 1`, `anticipatePin: 1`, `invalidateOnRefresh: true` e `end` em função.
- O slide usa `ease: "none"`. A distância é `Math.max(0, larguraDaFileira − stage.clientWidth)`, medida com `offsetWidth` e nunca fixa.
- Projetos, na ordem: `almeytour`, `mognus`, `blizzard` e `ecommerce-ddd`. Textos exatamente como na spec (seção 4).
- "current" aparece como "Em andamento" / "Ongoing". O projeto 2 se chama **Mognu's Company**.
- Contraste AAA para todo texto, nos dois temas, medido na tela contra o pior pixel: 7:1 (normal) e 4.5:1 (grande, 24px+ ou 18.66px+ em negrito).
- `/pt-br` e `/en` continuam páginas estáticas no `pnpm build`.
- Arquivos testados com `node --test` (`src/animations/track-travel.ts`, `src/lib/is-plain-click.ts`) não importam nada, e o teste importa o arquivo com `.ts` no final e sem o alias `@/`. O resto do projeto usa `@/` sem extensão.
- Um arquivo, uma responsabilidade. Nomes em kebab-case.
- Comentários em português, curtos, explicando cada conceito novo do GSAP no estilo dos que já existem: ScrollTrigger, `pin`, `scrub`, `start`/`end`, labels, `fromTo` versus `set`, valores em função, `invalidateOnRefresh`, `labelToScroll`, `seek`, `gsap.utils.selector`, `interpolate` e `matchMedia`.
- Mensagens de commit em inglês, no formato do histórico (`feat: ...`), terminando com `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
- `pnpm lint` (Biome) passa em todo commit. Se ele só reclamar de formatação ou da ordem dos imports, rode `pnpm exec biome check --write` e confira o diff.
- Nunca mexer no servidor da porta 3000, que é do Sinval. Os servidores das tarefas usam a porta 3100.
- Mudança visual só vai para commit depois de o Sinval ver os prints e aprovar (passos marcados com **PARAR**).

## Receitas usadas nas tarefas

O estado do shell não persiste entre comandos, então cada bloco repete as variáveis de que precisa. Não use `sleep` solto. Para esperar o servidor, use `curl --retry`; para esperar no navegador, `agent-browser wait <ms>`.

**Scratchpad** (screenshots, scripts e logs; nunca dentro do repo). Se a sessão for outra, troque pelo scratchpad dela:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e1c2c4dc-a002-4ec8-b614-bb669e4103cf/scratchpad
```

**Subir o build de produção na porta 3100:**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e1c2c4dc-a002-4ec8-b614-bb669e4103cf/scratchpad
cd /home/martins/projects/martinsportfolio
pnpm build
setsid pnpm start --port 3100 > "$SCRATCH/server.log" 2>&1 &
curl -s -o /dev/null --retry 30 --retry-delay 1 --retry-connrefused -w "servidor: %{http_code}\n" http://localhost:3100/pt-br
```

**Parar o servidor da porta 3100.** O comando mata o grupo de processos inteiro (`pnpm`, `next` e `next-server`). O PGID vem de quem escuta a porta, e não do `$!`: depois de `setsid ... &`, o `$!` não é o líder do grupo e o `kill` erra em silêncio.

```bash
PID=$(ss -lptnH 'sport = :3100' | grep -o 'pid=[0-9]*' | head -1 | cut -d= -f2)
if [ -n "$PID" ]; then
  PGID=$(ps -o pgid= -p "$PID" | tr -d ' ')
  kill -- -"$PGID"
  timeout 15 tail --pid="$PID" -f /dev/null
fi
ss -ltnH 'sport = :3100' | grep -q . && echo "AINDA RODANDO" || echo "servidor parado"
ps -eo pid,cmd | grep -E "next (start|dev)|next-server" | grep -v grep | grep 3100 || echo "nenhum processo sobrando na 3100"
```

**Sessão do agent-browser.** Cada tarefa usa uma sessão própria, com screenshots em caminho absoluto no scratchpad e escala 1 (1px de CSS = 1px da imagem):

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set viewport 1440 900 1
```

No fim de cada tarefa que usou o navegador: `agent-browser close`.

**Abrir a home e pular a intro.** Qualquer tecla acelera a intro 4×:

```bash
agent-browser open http://localhost:3100/pt-br
agent-browser press Escape
agent-browser wait 3000
```

**Rolar até uma fração do pin.** Não tem nada depois de Projetos, então o fim da página é o fim do pin. Uma fração `F` do scroll máximo é a mesma fração da timeline. Espere 1600ms depois de rolar, porque o `scrub: 1` leva 1s para alcançar o scroll:

```bash
F=0.62
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * $F)"
agent-browser wait 1600
```

Frações de referência, com as durações da Tarefa 3 (reveal 1 + rise 0.84 + slide 1 + respiro 0.15 = 2.99):

| F | O que aparece |
| --- | --- |
| `0` | hero inteiro |
| `0.17` | meio do `reveal`: hero em ~0.87 de escala, meio apagado; fundo novo meio aceso |
| `0.47` | meio do `rise`: título e primeiros cards subindo |
| `0.62` | começo do `slide`: cards no lugar, primeiro card na margem esquerda |
| `0.78` | meio do `slide` |
| `1` | fim: último card na margem direita |

**Script de medida do palco** (`$SCRATCH/stage-probe.js`, criado na Tarefa 3). Ele devolve a sobra de scroll horizontal, as caixas dos cards, o pé do título e o que está embaixo do botão "Ver projetos":

```js
(() => {
  const vw = document.documentElement.clientWidth;
  const round = (r) => ({ left: Math.round(r.left), right: Math.round(r.right), top: Math.round(r.top), bottom: Math.round(r.bottom) });
  const cards = [...document.querySelectorAll('[data-projects="card"]')].map((c) => round(c.getBoundingClientRect()));
  const heading = document.querySelector('[data-projects="heading"]');
  const cta = document.querySelector('[data-projects="hero"] a[href="#projects"]');
  let ctaClickable = null;
  if (cta) {
    const r = cta.getBoundingClientRect();
    ctaClickable = document.elementFromPoint(r.x + r.width / 2, r.y + r.height / 2)?.closest("a") === cta;
  }
  const stage = document.querySelector('[data-projects="hero"]')?.parentElement;
  return JSON.stringify({
    overflowX: document.documentElement.scrollWidth - vw,
    vw,
    vh: innerHeight,
    scrollY: Math.round(scrollY),
    cinema: stage ? getComputedStyle(stage).display === "grid" : null,
    headingBottom: heading ? Math.round(heading.getBoundingClientRect().bottom) : null,
    ctaClickable,
    cards,
  });
})()
```

**Script de comparação de imagens** (`$SCRATCH/diff.cjs`, criado na Tarefa 2):

```js
// Compara dois PNGs do mesmo tamanho: quanto mudou e onde.
// Uso: node diff.cjs antes.png depois.png
const sharp = require(
  require.resolve("sharp", {
    paths: [require.resolve("next", { paths: ["/home/martins/projects/martinsportfolio"] })],
  }),
);
const [a, b] = process.argv.slice(2);
const load = (file) => sharp(file).removeAlpha().raw().toBuffer({ resolveWithObject: true });
(async () => {
  const [A, B] = await Promise.all([load(a), load(b)]);
  if (A.info.width !== B.info.width || A.info.height !== B.info.height) {
    console.log(`tamanhos diferentes: ${A.info.width}x${A.info.height} vs ${B.info.width}x${B.info.height}`);
    process.exit(1);
  }
  const { width, height } = A.info;
  let changed = 0;
  let [minX, minY, maxX, maxY] = [width, height, -1, -1];
  for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
      const i = (y * width + x) * 3;
      const d = Math.max(
        Math.abs(A.data[i] - B.data[i]),
        Math.abs(A.data[i + 1] - B.data[i + 1]),
        Math.abs(A.data[i + 2] - B.data[i + 2]),
      );
      if (d > 10) {
        changed++;
        minX = Math.min(minX, x);
        minY = Math.min(minY, y);
        maxX = Math.max(maxX, x);
        maxY = Math.max(maxY, y);
      }
    }
  }
  const pct = ((changed / (width * height)) * 100).toFixed(3);
  console.log(changed === 0 ? "idênticas" : `${pct}% dos pixels mudaram, na área x ${minX}-${maxX}, y ${minY}-${maxY}`);
})();
```

**Scripts de contraste na tela** (`$SCRATCH/probe-projects.js` e `$SCRATCH/contrast.cjs`, criados na Tarefa 2). O primeiro roda no navegador: esconde os textos da seção que estão na tela e devolve onde eles estavam e a cor de cada um. O segundo lê a screenshot tirada com os textos escondidos e mede o pior contraste entre a cor do texto e os pixels atrás dele. A medida é pessimista: se passar assim, passa de verdade.

`$SCRATCH/probe-projects.js` (as cores são as do `globals.css` de hoje):

```js
(() => {
  const light = getComputedStyle(document.body).backgroundColor === "rgb(244, 247, 251)";
  const c = light
    ? { fg: "#0b1220", muted: "#3b475a", grad: ["#164581", "#074c64", "#205019"] }
    : { fg: "#f3f6fb", muted: "#adb8c9", grad: ["#8ebdf7", "#61dafb", "#8fca50"] };
  const section = document.getElementById("projects");
  const targets = [
    ["titulo", section.querySelector("h2"), [c.fg], 4.5],
    ["contagem", section.querySelector("h2 + span"), [c.muted], 7],
  ];
  section.querySelectorAll('[data-projects="card"]').forEach((card, i) => {
    targets.push(
      [`c${i}-numero`, card.querySelector("span[aria-hidden]"), c.grad, 4.5],
      [`c${i}-ano`, card.querySelector("div > span + span"), [c.muted], 7],
      [`c${i}-nome`, card.querySelector("h3"), [c.fg], 4.5],
      [`c${i}-descricao`, card.querySelector("p"), [c.muted], 7],
      ...[...card.querySelectorAll("ul li")].map((chip, j) => [`c${i}-chip${j}`, chip, [c.muted], 7]),
      [`c${i}-link`, card.querySelector("a"), [c.fg], 7],
    );
  });
  const boxes = [];
  for (const [label, el, colors, goal] of targets) {
    if (!el) continue;
    const r = el.getBoundingClientRect();
    // Só o que está inteiro na tela: um texto cortado pela borda não é
    // medido aqui (ele é medido no print em que aparece inteiro).
    if (r.width === 0 || r.top < 0 || r.bottom > innerHeight || r.left < 0 || r.right > innerWidth) continue;
    boxes.push({ label, x: r.x, y: r.y, w: r.width, h: r.height, colors, goal });
    el.style.visibility = "hidden";
  }
  return JSON.stringify(boxes);
})()
```

`$SCRATCH/contrast.cjs`:

```js
// Pior contraste (WCAG) entre a cor de cada texto e os pixels atrás dele.
// Uso: node contrast.cjs screenshot.png boxes.txt
// boxes.txt é a saída do probe-projects.js. Para gradiente (3 cores),
// cada terço da caixa é comparado com a sua cor (começo, meio, fim).
const fs = require("node:fs");
const sharp = require(
  require.resolve("sharp", {
    paths: [require.resolve("next", { paths: ["/home/martins/projects/martinsportfolio"] })],
  }),
);
const [shot, boxesFile] = process.argv.slice(2);
let boxes = JSON.parse(fs.readFileSync(boxesFile, "utf8").trim());
if (typeof boxes === "string") boxes = JSON.parse(boxes);
const lin = (v) => (v <= 0.04045 ? v / 12.92 : ((v + 0.055) / 1.055) ** 2.4);
const lum = ([r, g, b]) => 0.2126 * lin(r / 255) + 0.7152 * lin(g / 255) + 0.0722 * lin(b / 255);
const hex = (h) => [1, 3, 5].map((i) => Number.parseInt(h.slice(i, i + 2), 16));
const ratio = (a, b) => {
  const [hi, lo] = [lum(a), lum(b)].sort((m, n) => n - m);
  return (hi + 0.05) / (lo + 0.05);
};
(async () => {
  const { data, info } = await sharp(shot).removeAlpha().raw().toBuffer({ resolveWithObject: true });
  let failed = false;
  for (const box of boxes) {
    let worst = Number.POSITIVE_INFINITY;
    for (let y = Math.round(box.y); y < Math.round(box.y + box.h); y++) {
      for (let x = Math.round(box.x); x < Math.round(box.x + box.w); x++) {
        if (x < 0 || y < 0 || x >= info.width || y >= info.height) continue;
        const i = (y * info.width + x) * 3;
        const part = Math.min(box.colors.length - 1, Math.floor(((x - box.x) / box.w) * box.colors.length));
        worst = Math.min(worst, ratio(hex(box.colors[part]), [data[i], data[i + 1], data[i + 2]]));
      }
    }
    const ok = worst >= box.goal;
    if (!ok) failed = true;
    console.log(`${box.label.padEnd(14)} pior ${worst.toFixed(2)}:1 (meta ${box.goal}:1) ${ok ? "OK" : "FALHOU"}`);
  }
  process.exit(failed ? 1 : 0);
})();
```

**Medir o contraste da tela atual.** Precisa do servidor e da sessão do navegador já abertos, com a seção na tela. `NOME` identifica a medida (ex.: `escuro-1440`). O `eval` esconde os textos, então recarregue depois:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e1c2c4dc-a002-4ec8-b614-bb669e4103cf/scratchpad
NOME=escuro-1440
agent-browser eval "$(cat "$SCRATCH/probe-projects.js")" > "$SCRATCH/boxes-$NOME.txt"
agent-browser screenshot "$SCRATCH/contraste-$NOME.png"
node "$SCRATCH/contrast.cjs" "$SCRATCH/contraste-$NOME.png" "$SCRATCH/boxes-$NOME.txt"
```

## Ajustes em relação à spec

Decididos ao escrever o plano, depois de conferir o código e as ferramentas instaladas:

1. **Variante `cinema:` do Tailwind no lugar dos CSS Modules.** A condição do modo cinema fica escrita uma vez no CSS (`@custom-variant cinema` no `globals.css`), e as classes ficam junto do elemento, como no resto do projeto. O `projects-stage.module.css` não existe.
2. **O modo cinema também exige `min-height: 36rem` (576px).** Nas contas do plano, o card mais longo não cabe na altura de um celular deitado (844×390) nem de um 320×568, nem com fonte menor, e a spec proíbe cortar texto. Nessas telas vale o layout estático, com os cards em grade. Dos 576px para cima, a Tarefa 3 confere que todo card cabe inteiro.
3. **O hero usa `pointer-events` com um `fromTo` de duração 0, e não com um `tl.set`.** O `set` lembra o valor de antes na primeira vez que roda. Depois de um refresh no meio do scroll, ele lembraria `none` e o hero ficaria sem clique ao voltar para o topo. O `fromTo` escreve os dois lados.
4. **A camada de Projetos nunca liga `pointer-events`.** Ela fica sempre com `pointer-events: none` no modo cinema, e só os cards ligam o clique (`pointer-events: auto`). Até o `rise`, os cards estão abaixo da tela e cortados pelo `overflow: clip`, então não cobrem o hero. Com isso sobra uma chave só: a do hero.
5. **Todos os tweens da timeline usam `fromTo`.** Com `invalidateOnRefresh`, um `from()` ou um `to()` pode regravar o ponto de partida no meio do caminho. Com os dois lados escritos, nada depende do estado na hora da medida.
6. **A abertura direta em `#projects` usa `tl.seek("slide")`** antes de rolar, e não `getTween().progress(1)`. O tween do `scrub` só existe depois do primeiro evento de scroll, e o `seek` põe a timeline no ponto certo na hora. O endereço de chegada é lido uma vez só, no evento `load` (depois do refresh do ScrollTrigger). Na troca de idioma, que monta a página de novo sem recarregar, ela não pula para os cards.
11. **Sem `scroll-margin-top`.** O padding de cima da seção é o mesmo do hero, então, no layout estático, a navbar fixa cobre só o padding quando a âncora leva a seção ao topo.
7. **O foco só puxa o scroll quando vem do teclado** (`:focus-visible`). Um clique do mouse num card da borda não pode fazer a página pular antes de abrir o link.
8. **Novo `src/lib/is-plain-click.ts`**, com teste. É a regra do "clique simples" (botão esquerdo, sem Ctrl/Cmd/Shift/Alt), que já existe no `language-switch.tsx`. Agora o seletor de idioma e os links `#projects` usam a mesma função.
9. **Chave nova no dicionário: `projects.stack`** ("Tecnologias" / "Technologies"). É o nome da lista de chips para o leitor de tela.
10. **O `(04)` e o "Em andamento" usam `fg-muted`**, e o número do card usa o degradê do subtítulo do hero.

## Review Focus

Entradas que a spec implica e que são fáceis de esquecer. Cada uma tem uma checagem na tarefa indicada.

1. **Resize ou rotação no meio do pin, e depois voltar ao topo:** o hero aparece inteiro, o "Ver projetos" volta a receber clique, a fileira termina alinhada à margem direita e não aparece scroll horizontal. (Tarefa 3, passo 9)
2. **Ctrl/Cmd + clique ou botão do meio em "Ver projetos" ou "Projetos":** o navegador faz o de sempre (nova aba), e o JS não intercepta. (Tarefa 4, testes do `isPlainClick` e passo 9)
3. **Trocar de idioma no meio do slide, com `#projects` na URL:** a página fica onde estava, sem voltar ao topo e sem pular para o começo dos cards. (Tarefa 4, passo 11)
4. **Telas baixas ou pequenas** (320×568, 844×390, 1280×600): ou o modo cinema com todo card inteiro na tela, ou o layout estático. Nunca texto cortado. (Tarefa 3, passo 8)
5. **Clique do mouse num card meio escondido na borda direita:** o link abre sem a página pular. (Tarefa 4, passo 10)

---

### Task 1: Contas da fileira, com testes

**Files:**
- Create: `src/animations/track-travel.ts`
- Test: `src/animations/track-travel.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `trackTravel(trackWidth: number, stageWidth: number): number`
  - `cardProgress(cardLeft: number, firstCardLeft: number, travel: number): number`

- [ ] **Passo 1: Escrever os testes**

Crie `src/animations/track-travel.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, test } from "node:test";
import { cardProgress, trackTravel } from "./track-travel.ts";

describe("trackTravel", () => {
  test("anda o quanto a fileira passa da largura do palco", () => {
    assert.equal(trackTravel(2132, 1425), 707);
  });

  test("não anda quando a fileira cabe no palco", () => {
    assert.equal(trackTravel(1000, 1440), 0);
  });

  test("não anda quando a fileira tem a largura exata do palco", () => {
    assert.equal(trackTravel(1440, 1440), 0);
  });
});

describe("cardProgress", () => {
  // Palco de 1425px: margem de 43px, cards de 490px com 29px entre eles.
  const margin = 43;
  const step = 490 + 29;
  const travel = 707;

  test("o primeiro card já aparece no começo do slide", () => {
    assert.equal(cardProgress(margin, margin, travel), 0);
  });

  test("o segundo card aparece quando a fileira andou um card", () => {
    assert.equal(cardProgress(margin + step, margin, travel), step / travel);
  });

  test("os cards do fim param no fim do slide", () => {
    assert.equal(cardProgress(margin + 2 * step, margin, travel), 1);
    assert.equal(cardProgress(margin + 3 * step, margin, travel), 1);
  });

  test("sem slide (a fileira cabe na tela), todo card fica no começo", () => {
    assert.equal(cardProgress(margin + step, margin, 0), 0);
  });

  test("nunca fica antes do começo", () => {
    assert.equal(cardProgress(0, margin, travel), 0);
  });
});
```

- [ ] **Passo 2: Rodar e ver falhar**

Run: `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test src/animations/track-travel.test.ts`
Expected: FAIL com `ERR_MODULE_NOT_FOUND` (o `track-travel.ts` ainda não existe).

- [ ] **Passo 3: Implementar**

Crie `src/animations/track-travel.ts`:

```ts
/**
 * Contas da fileira de projetos. Só entram e saem números: quem mede o
 * DOM é quem chama. Por isso dá para testar com node --test, sem
 * navegador.
 */

/**
 * Quanto a fileira anda para a esquerda até o último card encostar na
 * margem direita do palco. Nunca é negativo: se a fileira cabe no
 * palco, ela não anda.
 */
export function trackTravel(trackWidth: number, stageWidth: number) {
  return Math.max(0, trackWidth - stageWidth);
}

/**
 * Em que ponto do slide (0 = começo, 1 = fim) um card aparece inteiro.
 *
 * O alvo é o card alinhado à margem esquerda, onde o primeiro card
 * começa. Os últimos cards não chegam lá (a fileira acaba antes), então
 * o resultado para em 1.
 *
 * @param cardLeft distância do começo da fileira até o card (offsetLeft)
 * @param firstCardLeft a mesma medida do primeiro card, que é a margem
 * @param travel quanto a fileira anda (trackTravel)
 */
export function cardProgress(
  cardLeft: number,
  firstCardLeft: number,
  travel: number,
) {
  if (travel <= 0) return 0;
  return Math.min(1, Math.max(0, (cardLeft - firstCardLeft) / travel));
}
```

- [ ] **Passo 4: Rodar e ver passar**

Run: `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test src/animations/track-travel.test.ts`
Expected: PASS, 8 testes.

Run: `pnpm test && pnpm lint`
Expected: todos os testes passam; Biome sem erros.

- [ ] **Passo 5: Commit**

```bash
git add src/animations/track-travel.ts src/animations/track-travel.test.ts
git commit -m "$(cat <<'EOF'
feat: add the track travel math for the projects row, with tests

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Seção de Projetos estática (dados, textos, cards e fundo)

No fim desta tarefa, a seção aparece para todo mundo embaixo do hero, no layout estático: título e cards em grade. O modo cinema vem na Tarefa 3.

**Files:**
- Create: `src/data/projects.ts`
- Create: `src/animations/projects-targets.ts`
- Create: `src/components/projects/projects.tsx`
- Create: `src/components/projects/project-card.tsx`
- Modify: `src/i18n/dictionaries/pt-br.ts` (bloco `projects` depois de `hero`)
- Modify: `src/i18n/dictionaries/en.ts` (bloco `projects` depois de `hero`)
- Modify: `src/app/globals.css` (utility `bg-projects-glow` depois de `bg-stack-glow`)
- Modify: `src/app/[lang]/page.tsx`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `type ProjectId = "almeytour" | "mognus" | "blizzard" | "ecommerce-ddd"`
  - `type ProjectData = { id: ProjectId; year: number | "ongoing"; stack: string[]; link: { href: string; kind: "site" | "code" } }`
  - `projects: ProjectData[]`
  - `PROJECTS = { hero: "hero", section: "section", heading: "heading", track: "track", card: "card" }` e `target(name): string`, que devolve `[data-projects="name"]`
  - `<Projects />` (server): `<section id="projects" data-projects="section">` com `<div data-projects="heading">` (contém o `<h2 id="projects-title" tabIndex={-1}>`) e `<ol data-projects="track">` com um `<li data-projects="card">` por projeto
  - dicionário: `projects.{ title, stack, ongoing, viewSite, viewCode, items[ProjectId].{ name, description } }`
  - utility CSS `bg-projects-glow`

- [ ] **Passo 1: Criar os scripts de verificação e tirar os prints de "antes"**

Crie no scratchpad os arquivos `diff.cjs`, `probe-projects.js` e `contrast.cjs` com o conteúdo das "Receitas". Depois, com o código ainda sem mudanças, tire os prints do hero no topo com "reduzir movimento" (fica estável para comparar pixel a pixel):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e1c2c4dc-a002-4ec8-b614-bb669e4103cf/scratchpad
cd /home/martins/projects/martinsportfolio
pnpm build
setsid pnpm start --port 3100 > "$SCRATCH/server.log" 2>&1 &
curl -s -o /dev/null --retry 30 --retry-delay 1 --retry-connrefused -w "servidor: %{http_code}\n" http://localhost:3100/pt-br
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
for TEMA in dark light; do
  for TAM in "1440 900" "390 844"; do
    set -- $TAM
    agent-browser set viewport "$1" "$2" 1
    agent-browser set media "$TEMA" reduced-motion
    agent-browser open http://localhost:3100/pt-br
    agent-browser wait 4000
    agent-browser screenshot "$SCRATCH/antes-$TEMA-$1.png"
  done
done
```

Depois pare o servidor (receita "Parar o servidor").

- [ ] **Passo 2: Criar os dados**

Crie `src/data/projects.ts`:

```ts
/** Chave de cada projeto em `projects.items` nos dicionários. */
export type ProjectId = "almeytour" | "mognus" | "blizzard" | "ecommerce-ddd";

export type ProjectData = {
  id: ProjectId;
  /** Ano de entrega, ou "ongoing" enquanto o projeto continua. */
  year: number | "ongoing";
  stack: string[];
  /** "site" leva ao projeto no ar; "code", ao repositório. Muda o texto do link. */
  link: { href: string; kind: "site" | "code" };
};

// O que não muda com o idioma; nome e descrição ficam nos dicionários.
// A ordem daqui é a ordem da fileira (e a do número 01, 02...).
export const projects: ProjectData[] = [
  {
    id: "almeytour",
    year: "ongoing",
    stack: [
      "Next.js",
      "TypeScript",
      "Tailwind CSS 4",
      "Framer Motion",
      "Vitest",
      "Playwright",
    ],
    link: { href: "https://almeytour.com/", kind: "site" },
  },
  {
    id: "mognus",
    year: 2025,
    stack: ["Next.js 16", "TypeScript", "Tailwind CSS 4", "next-intl 4", "Zod"],
    link: {
      href: "https://github.com/smartinsdev/mognus-company",
      kind: "code",
    },
  },
  {
    id: "blizzard",
    year: 2024,
    stack: [
      "Next.js 16",
      "TypeScript 7",
      "Tailwind CSS 4",
      "shadcn/ui",
      "React Hook Form",
      "Prisma",
    ],
    link: { href: "https://github.com/smartinsdev/blizzardco", kind: "code" },
  },
  {
    id: "ecommerce-ddd",
    year: "ongoing",
    stack: ["TypeScript", "Node.js", "CI", "Biome"],
    link: {
      href: "https://github.com/smartinsdev/backend-ecommerce-portfolio",
      kind: "code",
    },
  },
];
```

- [ ] **Passo 3: Criar os nomes dos alvos**

Crie `src/animations/projects-targets.ts`:

```ts
/**
 * Nomes dos elementos que a animação de scroll dos projetos mexe.
 *
 * Mesmo padrão do intro-targets.ts: os componentes marcam o elemento
 * com `data-projects={PROJECTS.x}` e a animação encontra com
 * `target(PROJECTS.x)`. Assim o nome existe num lugar só.
 */
export const PROJECTS = {
  /** A <section> do hero: diminui e some. */
  hero: "hero",
  /** A <section> de Projetos: dona do fundo novo. */
  section: "section",
  /** Título "Projetos" com a contagem. */
  heading: "heading",
  /** A fileira (<ol>) que anda na horizontal. */
  track: "track",
  card: "card",
} as const;

export type ProjectsTarget = (typeof PROJECTS)[keyof typeof PROJECTS];

export const target = (name: ProjectsTarget) => `[data-projects="${name}"]`;
```

- [ ] **Passo 4: Acrescentar os textos em português**

Em `src/i18n/dictionaries/pt-br.ts`, logo depois do fechamento do bloco `hero: { ... },`, acrescente:

```ts
  projects: {
    title: "Projetos",
    stack: "Tecnologias",
    ongoing: "Em andamento",
    viewSite: "Ver site",
    viewCode: "Ver código",
    // As chaves são os ids de src/data/projects.ts. Se faltar uma, o
    // TypeScript acusa no project-card.tsx.
    items: {
      almeytour: {
        name: "Almeytour",
        description:
          "Site de marketing e conversão para transfers de aeroporto e tours privados na Europa, com foco em Paris. Apresenta serviços, depoimentos e experiências exclusivas, e leva o visitante a falar com a equipe pelo WhatsApp.",
      },
      mognus: {
        name: "Mognu's Company",
        description:
          "Site institucional multilíngue de uma marcenaria de móveis sob medida: empresa, serviços, projetos selecionados e contato, numa interface responsiva pensada para visitantes de outros países.",
      },
      blizzard: {
        name: "Blizzard Conquer",
        description:
          "Portal de uma comunidade de MMORPG de fantasia: landing page temática, cadastro e login no banco de dados do jogo, rotas protegidas por sessão e central de downloads do cliente.",
      },
      "ecommerce-ddd": {
        name: "Back-end de e-commerce com DDD",
        description:
          "Protótipo de back-end em Node.js e TypeScript que demonstra DDD, Clean Architecture, SOLID, TDD e inversão de dependência, com fronteiras claras entre as camadas.",
      },
    },
  },
```

- [ ] **Passo 5: Acrescentar os textos em inglês**

Em `src/i18n/dictionaries/en.ts`, logo depois do fechamento do bloco `hero: { ... },`, acrescente:

```ts
  projects: {
    title: "Projects",
    stack: "Technologies",
    ongoing: "Ongoing",
    viewSite: "View site",
    viewCode: "View code",
    items: {
      almeytour: {
        name: "Almeytour",
        description:
          "Marketing and conversion website for airport transfers and private tours in Europe, focused on Paris. It presents services, testimonials and exclusive experiences, and guides visitors to reach the team on WhatsApp.",
      },
      mognus: {
        name: "Mognu's Company",
        description:
          "Multilingual corporate website for a custom carpentry and furniture business: company, services, selected projects and contact, in a responsive interface built for international visitors.",
      },
      blizzard: {
        name: "Blizzard Conquer",
        description:
          "Web portal for a fantasy MMORPG community: themed landing page, registration and login against the game database, session-protected routes and a download hub for the game client.",
      },
      "ecommerce-ddd": {
        name: "E-commerce Back-end with DDD",
        description:
          "Node.js and TypeScript back-end prototype that demonstrates DDD, Clean Architecture, SOLID, TDD and dependency inversion, with clear boundaries between layers.",
      },
    },
  },
```

- [ ] **Passo 6: Criar o fundo novo**

Em `src/app/globals.css`, logo depois do bloco `@utility bg-stack-glow { ... }`, acrescente:

```css
/* Fundo da seção de Projetos: a mesma família do bg-stack-glow
   (pontinhos e brilhos de marca), com a luz em outro lugar (ciano do
   React no alto, azul do TS embaixo à direita) para a troca de fundo
   ser percebida quando o hero some. */
@utility bg-projects-glow {
  background-color: var(--color-page);
  background-image:
    radial-gradient(
      60% 45% at 50% 0%,
      light-dark(
        color-mix(in oklab, var(--color-react) 16%, transparent),
        color-mix(in oklab, var(--color-react) 14%, transparent)
      ),
      transparent
    ),
    radial-gradient(
      50% 40% at 100% 100%,
      light-dark(
        color-mix(in oklab, var(--color-ts) 14%, transparent),
        color-mix(in oklab, var(--color-ts) 24%, transparent)
      ),
      transparent
    ),
    radial-gradient(
      circle at 1px 1px,
      light-dark(rgb(11 18 32 / 0.06), rgb(255 255 255 / 0.05)) 1px,
      transparent 0
    );
  background-size:
    auto,
    auto,
    28px 28px;
}
```

- [ ] **Passo 7: Criar o card**

Crie `src/components/projects/project-card.tsx`:

```tsx
import { PROJECTS } from "@/animations/projects-targets";
import type { ProjectData } from "@/data/projects";
import { getDictionary } from "@/i18n/get-dictionary";

type ProjectCardProps = {
  project: ProjectData;
  /** Posição na fileira, a partir de 0. Vira o "01", "02"... */
  index: number;
};

export async function ProjectCard({ project, index }: ProjectCardProps) {
  const dict = await getDictionary();
  const { name, description } = dict.projects.items[project.id];
  const year =
    project.year === "ongoing" ? dict.projects.ongoing : project.year;
  const linkText =
    project.link.kind === "site"
      ? dict.projects.viewSite
      : dict.projects.viewCode;

  return (
    <li
      data-projects={PROJECTS.card}
      className="relative flex flex-col rounded-xl bg-surface p-[clamp(1.25rem,2.2vw,2.5rem)] ring-1 ring-line transition-colors duration-200 has-[a:focus-visible]:ring-highlight has-[a:hover]:ring-highlight"
    >
      <div className="flex items-baseline justify-between gap-4">
        {/* Decorativo: a ordem já vem do <ol>. */}
        <span
          aria-hidden="true"
          className="bg-linear-to-r from-gradient-from via-gradient-via to-gradient-to bg-clip-text font-display text-[clamp(2.5rem,4vw,4.5rem)] font-semibold leading-none text-transparent"
        >
          {String(index + 1).padStart(2, "0")}
        </span>
        <span className="font-mono text-[clamp(0.75rem,0.85vw,1.0625rem)] uppercase tracking-widest text-fg-muted">
          {year}
        </span>
      </div>

      <h3 className="mt-[clamp(1rem,3svh,2.5rem)] font-display text-[clamp(1.5rem,2.2vw,2.75rem)] font-semibold leading-tight tracking-tight">
        {name}
      </h3>
      <p className="mt-[0.75em] text-[clamp(0.9375rem,1vw,1.25rem)] leading-relaxed text-fg-muted">
        {description}
      </p>

      <ul
        aria-label={dict.projects.stack}
        className="mt-auto flex flex-wrap gap-2 pt-[clamp(1.25rem,3svh,2rem)]"
      >
        {project.stack.map((tech) => (
          <li
            key={tech}
            className="rounded-full px-[0.85em] py-[0.3em] font-mono text-[clamp(0.75rem,0.8vw,1rem)] text-fg-muted ring-1 ring-line"
          >
            {tech}
          </li>
        ))}
      </ul>

      <div className="mt-[clamp(1rem,2.5svh,1.75rem)] border-t border-line pt-[clamp(0.5rem,1.5svh,1rem)]">
        {/* O ::after (after:absolute after:inset-0) estica a área de
            clique do link até cobrir o card inteiro, que é o `relative`
            mais próximo. Continua sendo um link só para o leitor de tela,
            com o nome "Ver site, Almeytour (abre em nova aba)". */}
        <a
          href={project.link.href}
          target="_blank"
          rel="noreferrer"
          className="group inline-flex min-h-11 items-center gap-[0.4em] text-[clamp(1rem,1.1vw,1.375rem)] font-medium text-fg transition-colors duration-200 after:absolute after:inset-0 after:rounded-xl hover:text-highlight"
        >
          {linkText}
          <span className="sr-only">, {name}</span>
          <span
            aria-hidden="true"
            className="transition-transform duration-200 group-hover:translate-x-0.5 group-hover:-translate-y-0.5"
          >
            ↗
          </span>
          <span className="sr-only">{dict.hero.newTab}</span>
        </a>
      </div>
    </li>
  );
}
```

- [ ] **Passo 8: Criar a seção**

Crie `src/components/projects/projects.tsx`:

```tsx
import { PROJECTS } from "@/animations/projects-targets";
import { projects } from "@/data/projects";
import { getDictionary } from "@/i18n/get-dictionary";
import { ProjectCard } from "./project-card";

// Margem lateral igual à da navbar: o título e o primeiro card ficam
// alinhados com a logo.
const GUTTER = "px-[clamp(1.25rem,3vw,5rem)]";

export async function Projects() {
  const dict = await getDictionary();
  // "04": a contagem com dois dígitos, como o número dos cards.
  const count = String(projects.length).padStart(2, "0");

  return (
    // O padding de cima é o mesmo do hero: quando a âncora #projects
    // leva o topo da seção ao topo da tela, a navbar fixa cobre só o
    // padding, e não o título.
    <section
      id="projects"
      aria-labelledby="projects-title"
      data-projects={PROJECTS.section}
      className="relative bg-projects-glow pt-[clamp(5.5rem,11svh,8.5rem)] pb-[clamp(4rem,9svh,6.5rem)]"
    >
      <div
        data-projects={PROJECTS.heading}
        className={`flex items-baseline gap-[0.6em] pb-[clamp(1.5rem,4svh,3rem)] ${GUTTER}`}
      >
        {/* tabIndex -1: não entra no Tab, mas pode receber o foco pelo
            código. O link #projects manda o foco para cá. */}
        <h2
          id="projects-title"
          tabIndex={-1}
          className="font-display text-[clamp(2.25rem,4.5vw,4.5rem)] font-semibold leading-none tracking-tight"
        >
          {dict.projects.title}
        </h2>
        {/* O <ol> já diz quantos são para o leitor de tela. */}
        <span
          aria-hidden="true"
          className="font-mono text-[clamp(0.875rem,1.1vw,1.375rem)] text-fg-muted"
        >
          ({count})
        </span>
      </div>

      <ol
        data-projects={PROJECTS.track}
        className={`relative grid gap-[clamp(1rem,2vw,2.5rem)] md:grid-cols-2 ${GUTTER}`}
      >
        {projects.map((project, index) => (
          <ProjectCard key={project.id} project={project} index={index} />
        ))}
      </ol>
    </section>
  );
}
```

- [ ] **Passo 9: Pôr a seção na página**

Em `src/app/[lang]/page.tsx`, troque o arquivo inteiro por:

```tsx
import { Hero } from "@/components/hero/hero";
import { Intro } from "@/components/intro/intro";
import { Navbar } from "@/components/layout/navbar";
import { Projects } from "@/components/projects/projects";

export default function HomePage() {
  return (
    <Intro>
      <Navbar />
      <main>
        <Hero />
        <Projects />
      </main>
    </Intro>
  );
}
```

- [ ] **Passo 10: Checar tipos, lint, testes e build**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build`
Expected: sem erros. No fim do build, `/[lang]` aparece como página estática (SSG) com `/pt-br` e `/en`.

- [ ] **Passo 11: Conferir no navegador**

Suba o build na 3100 (receita). Na mesma sessão do passo 1:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e1c2c4dc-a002-4ec8-b614-bb669e4103cf/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
for TEMA in dark light; do
  for TAM in "1440 900" "390 844"; do
    set -- $TAM
    agent-browser set viewport "$1" "$2" 1
    agent-browser set media "$TEMA" reduced-motion
    agent-browser open http://localhost:3100/pt-br
    agent-browser wait 4000
    agent-browser screenshot "$SCRATCH/depois-$TEMA-$1.png"
    node "$SCRATCH/diff.cjs" "$SCRATCH/antes-$TEMA-$1.png" "$SCRATCH/depois-$TEMA-$1.png"
    agent-browser eval "document.getElementById('projects').scrollIntoView()"
    agent-browser wait 300
    agent-browser screenshot "$SCRATCH/estatico-$TEMA-$1.png"
  done
done
```

Expected:
- O `diff.cjs` de cada par responde `idênticas`: o hero no topo não mudou.
- Abra os `estatico-*.png` com a ferramenta Read. Tem que aparecer:
  - o título "Projetos (04)" alinhado com a logo, sobre o fundo com brilho ciano no alto;
  - em 1440, os cards em 2 colunas; em 390, em 1 coluna;
  - cada card com número em degradê, ano ou "EM ANDAMENTO", nome, descrição, chips e "Ver site ↗" ou "Ver código ↗".

Confira também a versão em inglês e sem JavaScript:

```bash
agent-browser set viewport 1440 900 1
agent-browser set media dark reduced-motion
agent-browser open http://localhost:3100/en
agent-browser wait 4000
agent-browser eval "document.getElementById('projects').scrollIntoView()"
agent-browser wait 300
agent-browser screenshot "$SCRATCH/estatico-en.png"
agent-browser eval "JSON.stringify([...document.querySelectorAll('#projects a')].map((a) => a.textContent))"
```

Expected: "Projects (04)", "E-commerce Back-end with DDD" e "ONGOING". Os links aparecem como `"View site, Almeytour↗(opens in a new tab)"` e parecidos, ou seja, com o nome do projeto e o aviso de nova aba no texto.

- [ ] **Passo 12: Medir o contraste**

Com a sessão aberta, para cada tema (`dark`, `light`) e largura (`1440 900`, `390 844`):
1. `set viewport`, `set media <tema> reduced-motion`, `open` e `wait 4000`;
2. `document.getElementById('projects').scrollIntoView()` e `wait 300`;
3. a receita "Medir o contraste da tela atual" com `NOME=<tema>-<largura>`;
4. recarregue para os textos voltarem (`agent-browser reload`, `wait 4000`), leve o último card à tela (`document.querySelectorAll('[data-projects=card]')[3].scrollIntoView({ block: "end" })`, `wait 300`) e meça de novo com `NOME=<tema>-<largura>-b`. Isso pega os cards de baixo.

Expected: todas as linhas `OK`. Se o `(04)` ou o título falharem no claro por causa do brilho ciano, baixe o `16%` do primeiro gradiente do `bg-projects-glow` para `10%`, refaça o build e meça de novo.

Pare o servidor e feche a sessão (`agent-browser close`).

- [ ] **Passo 13: PARAR e mostrar os prints ao Sinval**

Mostre os `estatico-dark-1440.png`, `estatico-light-1440.png`, `estatico-dark-390.png` e `estatico-light-390.png` e o resultado do contraste. Não faça o commit antes de o Sinval aprovar. Se ele pedir ajuste, ajuste, refaça os passos 10 a 12 e mostre de novo.

- [ ] **Passo 14: Commit**

```bash
git add src/data/projects.ts src/animations/projects-targets.ts src/components/projects/ src/i18n/dictionaries/pt-br.ts src/i18n/dictionaries/en.ts src/app/globals.css "src/app/[lang]/page.tsx"
git commit -m "$(cat <<'EOF'
feat: add the projects section with the four real projects

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Modo cinema (palco, pin e timeline)

**Files:**
- Create: `src/animations/projects-scroll.ts`
- Create: `src/components/projects/projects-stage.tsx`
- Modify: `src/lib/gsap.ts`
- Modify: `src/app/globals.css` (variante `cinema`, depois da variante `light`)
- Modify: `src/components/hero/hero.tsx` (marca `data-projects`)
- Modify: `src/components/projects/projects.tsx` (classes `cinema:`)
- Modify: `src/components/projects/project-card.tsx` (classes `cinema:`)
- Modify: `src/app/[lang]/page.tsx`

**Interfaces:**
- Consumes: `trackTravel` (Tarefa 1); `PROJECTS` e `target` (Tarefa 2); `<Projects />` (Tarefa 2).
- Produces:
  - `PHASE = { reveal: "reveal", rise: "rise", slide: "slide", slideEnd: "slide-end" }`
  - `measureTravel(stage: HTMLElement): number`
  - `createProjectsScroll(stage: HTMLElement): gsap.core.Timeline` (a timeline tem `tl.scrollTrigger`)
  - `<ProjectsStage>{children}</ProjectsStage>` (client), com `CINEMA_QUERY` local
  - `ScrollTrigger` registrado e exportado por `@/lib/gsap`

- [ ] **Passo 1: Registrar o ScrollTrigger**

Troque `src/lib/gsap.ts` inteiro por:

```ts
import { useGSAP } from "@gsap/react";
import gsap from "gsap";
import { DrawSVGPlugin } from "gsap/DrawSVGPlugin";
import { Flip } from "gsap/Flip";
import { ScrollTrigger } from "gsap/ScrollTrigger";

// Todo plugin do GSAP precisa ser registrado uma vez antes de usar.
// Centralizando aqui, o resto do projeto importa sempre de "@/lib/gsap"
// e nunca esquece de registrar.
gsap.registerPlugin(useGSAP, DrawSVGPlugin, Flip, ScrollTrigger);

export { Flip, gsap, ScrollTrigger, useGSAP };
```

- [ ] **Passo 2: Criar a variante `cinema`**

Em `src/app/globals.css`, logo depois do bloco `@custom-variant light { ... }`, acrescente:

```css
/* Modo cinema da seção de Projetos: JS ligado, ninguém pediu menos
   movimento e tela alta o bastante para o card mais longo caber. Fora
   disso vale o layout estático, com a seção no fluxo normal.
   A mesma condição está em CINEMA_QUERY (projects-stage.tsx): o CSS
   monta o layout e o GSAP liga a animação, sempre juntos.
   Uso: cinema:grid, cinema:h-svh... */
@custom-variant cinema {
  @media (scripting: enabled) and (prefers-reduced-motion: no-preference) and (min-height: 36rem) {
    @slot;
  }
}
```

- [ ] **Passo 3: Marcar o hero**

Em `src/components/hero/hero.tsx`, acrescente o import:

```tsx
import { PROJECTS } from "@/animations/projects-targets";
```

e troque a linha de abertura da `<section>`:

```tsx
    <section className="relative isolate flex min-h-svh flex-col items-center justify-center overflow-clip bg-stack-glow px-4 pt-[clamp(5.5rem,11svh,8.5rem)] pb-[clamp(4rem,9svh,6.5rem)]">
```

por:

```tsx
    // data-projects: a passagem para os projetos diminui e apaga esta
    // <section> inteira. A intro mexe só nos filhos, então as duas
    // animações não brigam.
    <section
      data-projects={PROJECTS.hero}
      className="relative isolate flex min-h-svh flex-col items-center justify-center overflow-clip bg-stack-glow px-4 pt-[clamp(5.5rem,11svh,8.5rem)] pb-[clamp(4rem,9svh,6.5rem)]"
    >
```

O comentário `//` fica dentro do `return (`, antes da `<section`, que é o mesmo padrão do `projects.tsx`.

- [ ] **Passo 4: Classes do modo cinema na seção**

Em `src/components/projects/projects.tsx`:

1. Troque o `className` da `<section>`:

```tsx
      className="relative bg-projects-glow pt-[clamp(5.5rem,11svh,8.5rem)] pb-[clamp(4rem,9svh,6.5rem)]"
```

por:

```tsx
      className="relative bg-projects-glow pt-[clamp(5.5rem,11svh,8.5rem)] pb-[clamp(4rem,9svh,6.5rem)] cinema:pointer-events-none cinema:flex cinema:h-svh cinema:flex-col cinema:self-end cinema:pb-[clamp(1.25rem,4svh,3rem)] cinema:opacity-0"
```

2. Troque o `className` do `<ol>`:

```tsx
        className={`relative grid gap-[clamp(1rem,2vw,2.5rem)] md:grid-cols-2 ${GUTTER}`}
```

por:

```tsx
        className={`relative grid gap-[clamp(1rem,2vw,2.5rem)] md:grid-cols-2 ${GUTTER} cinema:my-auto cinema:flex cinema:w-max cinema:will-change-transform`}
```

3. Troque o comentário que abre o `return (` (as três linhas que começam com `// O padding de cima é o mesmo do hero`) por este, que explica os dois layouts:

```tsx
    // Dois layouts, escolhidos pelo CSS (variante cinema: do globals.css):
    // - estático: a seção vem depois do hero, com os cards em grade. O
    //   padding de cima é o do hero, então a navbar fixa cobre só o
    //   padding quando a âncora #projects leva a seção ao topo da tela.
    // - cinema: a seção fica em cima do hero, com a altura da tela, presa
    //   ao pé do palco (self-end), invisível (opacity-0) e sem receber
    //   cliques; a fileira vira uma linha só (flex + w-max), centralizada
    //   na altura que sobra (my-auto). O projects-scroll.ts anima o resto.
```

- [ ] **Passo 5: Classes do modo cinema no card**

Em `src/components/projects/project-card.tsx`, troque o `className` do `<li>`:

```tsx
      className="relative flex flex-col rounded-xl bg-surface p-[clamp(1.25rem,2.2vw,2.5rem)] ring-1 ring-line transition-colors duration-200 has-[a:focus-visible]:ring-highlight has-[a:hover]:ring-highlight"
```

por:

```tsx
      className="relative flex flex-col rounded-xl bg-surface p-[clamp(1.25rem,2.2vw,2.5rem)] ring-1 ring-line transition-colors duration-200 has-[a:focus-visible]:ring-highlight has-[a:hover]:ring-highlight cinema:pointer-events-auto cinema:w-[85vw] cinema:shrink-0 cinema:md:w-[60vw] cinema:lg:w-[34vw]"
```

e acrescente, logo antes do `return (`:

```tsx
  // No modo cinema a largura vem da tela, e não do texto: ~85% no
  // celular, ~60% no tablet e ~34% no desktop. Assim a fileira sempre
  // passa da tela (tem o que andar) e a medida não muda quando as fontes
  // carregam. O pointer-events-auto religa o clique que a seção desliga.
```

- [ ] **Passo 6: Criar a timeline**

Crie `src/animations/projects-scroll.ts`:

```ts
import { gsap } from "@/lib/gsap";
import { PROJECTS, target } from "./projects-targets";
import { trackTravel } from "./track-travel";

/**
 * Labels da timeline, na ordem. O projects-focus.ts usa para descobrir
 * a posição de scroll de cada fase (labelToScroll).
 */
export const PHASE = {
  reveal: "reveal",
  rise: "rise",
  slide: "slide",
  slideEnd: "slide-end",
} as const;

/**
 * Quanto scroll, em alturas de tela, as fases reveal e rise usam juntas.
 * O slide usa, além disso, a distância real que a fileira anda.
 */
const INTRO_SCREENS = 1.5;

/**
 * Durações em "unidades" da timeline. Com scrub, o tempo vira distância
 * de scroll: o que vale é a proporção entre elas. Com estes números, numa
 * tela comum, a fileira anda perto de 1px por pixel rolado. Se o slide
 * parecer rápido ou lento, mexa em `slide`.
 */
const DURATION = { reveal: 1, rise: 0.6, stagger: 0.08, slide: 1, rest: 0.15 };

/**
 * Quanto a fileira anda, medido agora. offsetWidth é a largura de layout:
 * não muda com o transform que a própria animação aplica. clientWidth
 * não conta a barra de rolagem (window.innerWidth conta).
 */
export function measureTravel(stage: HTMLElement) {
  const track = stage.querySelector<HTMLElement>(target(PROJECTS.track));
  return track ? trackTravel(track.offsetWidth, stage.clientWidth) : 0;
}

/**
 * Monta a passagem do hero para os projetos, controlada pelo scroll:
 * reveal (o hero sai, o fundo novo entra) → rise (título e cards sobem)
 * → slide (a fileira anda na horizontal).
 *
 * Precisa rodar dentro de um gsap.matchMedia() (ou useGSAP): a timeline,
 * o ScrollTrigger e o pin entram no contexto e são desfeitos sozinhos.
 */
export function createProjectsScroll(stage: HTMLElement) {
  // selector: um querySelectorAll que só procura dentro do palco.
  const q = gsap.utils.selector(stage);
  const hero = q(target(PROJECTS.hero));
  const section = q(target(PROJECTS.section));
  const risers = [...q(target(PROJECTS.heading)), ...q(target(PROJECTS.card))];
  const track = q(target(PROJECTS.track));

  // Com `scrollTrigger` na timeline, quem move a agulha é o scroll, não
  // o relógio. Fica na timeline, nunca num tween de dentro dela.
  const tl = gsap.timeline({
    // "none" = velocidade constante. Os tweens que querem curva dizem.
    defaults: { ease: "none" },
    scrollTrigger: {
      trigger: stage,
      // pin: segura o palco na tela enquanto a timeline toca. O GSAP põe
      // o palco numa div nova (pin-spacer) com a altura do scroll, para a
      // página não "encolher" enquanto ele está fixo.
      pin: true,
      // "<ponto do palco> <ponto da tela>": começa quando o pé do palco
      // encosta no pé da tela. Com o hero da altura da tela, é o mesmo
      // que "top top"; com o hero mais alto (celular deitado), a pessoa
      // vê o hero inteiro antes do pin.
      start: "bottom bottom",
      // Função: é chamada de novo a cada refresh (resize, rotação), então
      // o tamanho acompanha a largura real da fileira.
      end: () =>
        `+=${window.innerHeight * INTRO_SCREENS + measureTravel(stage)}`,
      // scrub: a timeline segue o scroll. O número é o atraso suave, em
      // segundos, para alcançar o scroll (o "cinema"). Rolar para cima
      // desfaz tudo, sem código extra.
      scrub: 1,
      // Adianta o pin um pouquinho para não dar tranco quando ele engata.
      anticipatePin: 1,
      // No refresh, recalcula os valores em função (x, y, end).
      invalidateOnRefresh: true,
    },
  });

  // 1. reveal: o hero diminui e some; ao mesmo tempo ("<" = junto com o
  //    anterior) a seção de Projetos, dona do fundo novo, aparece.
  //    fromTo em tudo: com os dois lados escritos, um refresh no meio do
  //    caminho não muda o ponto de partida.
  tl.addLabel(PHASE.reveal)
    .fromTo(
      hero,
      { scale: 1, opacity: 1 },
      { scale: 0.75, opacity: 0, duration: DURATION.reveal, ease: "power1.in" },
    )
    .fromTo(
      section,
      { opacity: 0 },
      { opacity: 1, duration: DURATION.reveal, ease: "power1.out" },
      "<",
    )
    // Chave: no fim do reveal o hero invisível para de receber cliques
    // (senão um clique no vazio acertaria o "Ver projetos"). Duração 0 =
    // muda de uma vez naquele ponto; voltando o scroll, volta a "auto".
    // É um fromTo, e não um set, porque o set lembra o valor de antes na
    // primeira vez que roda, e depois de um refresh no meio do scroll ele
    // lembraria "none". immediateRender: false = não aplicar o "auto"
    // agora, só quando a agulha passar por aqui.
    .fromTo(
      hero,
      { pointerEvents: "auto" },
      { pointerEvents: "none", duration: 0, immediateRender: false },
    );

  // 2. rise: título e cards sobem de baixo da tela, um de cada vez
  //    (stagger). O y inicial é uma função: vale a altura da tela de
  //    agora, e o invalidateOnRefresh recalcula no resize.
  tl.addLabel(PHASE.rise).fromTo(
    risers,
    { y: () => window.innerHeight },
    {
      y: 0,
      duration: DURATION.rise,
      ease: "power2.out",
      stagger: DURATION.stagger,
    },
  );

  // 3. slide: a fileira anda até o último card encostar na margem direita.
  //    ease "none" (o padrão desta timeline): a fileira acompanha o scroll
  //    na mesma velocidade do começo ao fim.
  tl.addLabel(PHASE.slide)
    .fromTo(
      track,
      { x: 0 },
      { x: () => -measureTravel(stage), duration: DURATION.slide },
    )
    .addLabel(PHASE.slideEnd)
    // Respiro: um tween vazio. O último card para um instante antes de o
    // pin soltar.
    .to({}, { duration: DURATION.rest });

  return tl;
}
```

- [ ] **Passo 7: Criar o palco e pôr na página**

Crie `src/components/projects/projects-stage.tsx`:

```tsx
"use client";

import type { ReactNode } from "react";
import { useRef } from "react";
import { createProjectsScroll } from "@/animations/projects-scroll";
import { gsap, useGSAP } from "@/lib/gsap";

/**
 * Mesma condição da variante `cinema:` do globals.css. O CSS monta o
 * layout e o matchMedia liga a animação: os dois mudam juntos, até se a
 * pessoa mudar a preferência com a página aberta.
 */
const CINEMA_QUERY =
  "(scripting: enabled) and (prefers-reduced-motion: no-preference) and (min-height: 36rem)";

/**
 * Palco da passagem do hero para os projetos. Recebe as duas seções por
 * `children`, então elas continuam server components (igual ao <Intro>).
 */
export function ProjectsStage({ children }: { children: ReactNode }) {
  const wrapper = useRef<HTMLDivElement>(null);
  const stage = useRef<HTMLDivElement>(null);

  useGSAP(
    () => {
      // matchMedia roda a função só enquanto a condição vale. Se ela
      // deixar de valer (ex.: a pessoa liga "reduzir movimento"), o GSAP
      // desfaz timeline, ScrollTrigger e pin sozinho.
      gsap.matchMedia().add(CINEMA_QUERY, () => {
        if (!stage.current) return;
        createProjectsScroll(stage.current);
      });
    },
    { scope: wrapper },
  );

  return (
    // O pin põe o palco dentro de uma div nova (pin-spacer). O React só
    // tira e põe esta div de fora; o que muda dentro dela é por conta do
    // GSAP, que desfaz o embrulho quando o componente desmonta.
    <div ref={wrapper}>
      {/* No modo cinema: grid com as duas seções na mesma célula
          (col-start-1 + row-start-1 em cada filho), ou seja, uma em cima
          da outra; Projetos vem depois no HTML e fica por cima.
          overflow-clip corta o que sai do palco (cards abaixo e à direita)
          sem criar barra de rolagem. */}
      <div
        ref={stage}
        className="cinema:grid cinema:overflow-clip cinema:*:col-start-1 cinema:*:row-start-1"
      >
        {children}
      </div>
    </div>
  );
}
```

Em `src/app/[lang]/page.tsx`, troque o arquivo inteiro por:

```tsx
import { Hero } from "@/components/hero/hero";
import { Intro } from "@/components/intro/intro";
import { Navbar } from "@/components/layout/navbar";
import { Projects } from "@/components/projects/projects";
import { ProjectsStage } from "@/components/projects/projects-stage";

export default function HomePage() {
  return (
    <Intro>
      <Navbar />
      <main>
        <ProjectsStage>
          <Hero />
          <Projects />
        </ProjectsStage>
      </main>
    </Intro>
  );
}
```

- [ ] **Passo 8: Checar, subir e conferir as fases**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build`
Expected: sem erros, com `/pt-br` e `/en` estáticas.

Crie `$SCRATCH/stage-probe.js` (receita) e suba o build na 3100. Depois:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e1c2c4dc-a002-4ec8-b614-bb669e4103cf/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set viewport 1440 900 1
agent-browser set media dark
agent-browser open http://localhost:3100/pt-br
agent-browser press Escape
agent-browser wait 3000
for F in 0 0.17 0.47 0.62 0.78 1; do
  agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * $F)"
  agent-browser wait 1600
  agent-browser screenshot "$SCRATCH/cinema-dark-1440-$F.png"
  agent-browser eval "$(cat "$SCRATCH/stage-probe.js")" > "$SCRATCH/probe-dark-1440-$F.txt"
done
cat "$SCRATCH"/probe-dark-1440-*.txt
```

Abra os prints com a ferramenta Read. Expected, pela tabela de frações da receita:
- `0`: o hero igual ao de sempre, sem card à vista.
- `0.17`: o hero menor e meio apagado, e o fundo com brilho ciano aparecendo.
- `0.47`: o título e os primeiros cards subindo.
- `0.62`: os cards no lugar, com o primeiro alinhado à logo.
- `0.78`: a fileira no meio do caminho.
- `1`: o último card alinhado à margem direita.

Nos probes:
- `overflowX` é `0` e `cinema` é `true` em todos.
- `ctaClickable` é `true` em `0` e `false` de `0.47` em diante.
- Em `0.62`, `cards[0].left` fica a ±2px de 43 (a margem em 1440).
- Em `1`, `cards[3].right` fica a ±2px de `vw − 43`.
- De `0.62` em diante, todo card tem `top > headingBottom` e `bottom ≤ vh`.

Agora a volta. Role para `F` 0.78, 0.47, 0.17 e 0, nessa ordem, com `wait 1600` entre um e outro, e tire o print do `0`. Expected: igual ao `cinema-dark-1440-0.png`. O ponto verde de "disponível" pisca, então compare de olho, e não com o `diff.cjs`.

Repita a ida (`0.62` e `1`) com os probes nestas telas, com `set viewport <w> <h> 1`, recarregando e pulando a intro a cada troca:

| Tela | Expected |
| --- | --- |
| 390×844 | `cinema: true`; cards com ~85% da largura; tudo cabe (`bottom ≤ vh`); `overflowX: 0` |
| 768×1024 | `cinema: true`; cards com ~60%; tudo cabe |
| 1280×600 | `cinema: true`; tudo cabe |
| 2540×1300 | `cinema: true`; cards com ~34%; o fim alinha com `vw − 76` (±2px); tudo cabe |
| 375×667 | `cinema: true`; tudo cabe |
| 844×390 | `cinema: false`: layout estático, com os cards em grade embaixo do hero; `overflowX: 0` |
| 320×568 | `cinema: false`; `overflowX: 0` |

Se algum card não couber numa tela com `cinema: true`, diminua os tamanhos na ordem abaixo, refaça o build e confira de novo. Nunca corte texto.
1. Descrição: `text-[clamp(0.9375rem,1vw,1.25rem)]` para `text-[clamp(0.875rem,0.95vw,1.25rem)]`.
2. Espaço antes do nome: `mt-[clamp(1rem,3svh,2.5rem)]` para `mt-[clamp(0.5rem,2svh,2.5rem)]`.
3. Número: `text-[clamp(2.5rem,4vw,4.5rem)]` para `text-[clamp(2rem,3.5vw,4.5rem)]`.
4. Se ainda não couber, suba o `36rem` da condição (no `globals.css` e no `CINEMA_QUERY`, os dois iguais) para o menor valor em que a tela que falhou passa a usar o layout estático, e anote no commit.

- [ ] **Passo 9: Resize no meio, "reduzir movimento" e tema claro**

Resize no meio do pin (Review Focus 1):

```bash
agent-browser set viewport 1440 900 1
agent-browser open http://localhost:3100/pt-br
agent-browser press Escape
agent-browser wait 3000
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * 0.78)"
agent-browser wait 1600
agent-browser set viewport 1024 768 1
agent-browser wait 1000
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * 1)"
agent-browser wait 1600
agent-browser eval "$(cat "$SCRATCH/stage-probe.js")"
agent-browser eval "window.scrollTo(0, 0)"
agent-browser wait 1600
agent-browser eval "$(cat "$SCRATCH/stage-probe.js")"
agent-browser screenshot "$SCRATCH/resize-topo.png"
```

Expected:
- No fim, `cards[3].right` fica a ±2px de `vw − 31` (a margem em 1024 é 3vw ≈ 31px) e `overflowX` é `0`.
- No topo, `ctaClickable` é `true`, e o print mostra o hero inteiro, em escala 1 e sem transparência.

"Reduzir movimento" ligado com a página aberta:

```bash
agent-browser set viewport 1440 900 1
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * 0.78)"
agent-browser wait 1600
agent-browser set media dark reduced-motion
agent-browser wait 500
agent-browser eval "JSON.stringify({ probe: JSON.parse($(cat "$SCRATCH/stage-probe.js")), heroStyle: document.querySelector('[data-projects=hero]').getAttribute('style'), spacer: !!document.querySelector('.pin-spacer') })"
agent-browser set media dark
agent-browser wait 500
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * 0.62)"
agent-browser wait 1600
agent-browser eval "$(cat "$SCRATCH/stage-probe.js")"
```

Expected:
- Com "reduzir movimento": `cinema: false`, `heroStyle` vazio ou `null` (o GSAP devolveu o estilo original), `spacer: false` e `overflowX: 0`.
- Depois de desligar: `cinema: true` de novo, com o primeiro card na margem (±2px de 43).

Tema claro:
1. `set media light`, recarregue e pule a intro.
2. Tire os prints em `F` 0.17 e 0.62 (`cinema-light-1440-*.png`).
3. Meça o contraste com o probe em `F=0.62`, em 1440 e 390, nos dois temas (`NOME=cinema-<tema>-<largura>`), recarregando entre as medidas.

Expected: todas as linhas `OK`. No modo cinema, o probe mede só o que está inteiro na tela.

Pare o servidor e feche a sessão.

- [ ] **Passo 10: PARAR e mostrar os prints ao Sinval**

Mostre os `cinema-dark-1440-{0,0.17,0.47,0.62,1}.png`, os `cinema-light-1440-{0.17,0.62}.png`, o 390 em `0.62` e o resultado do contraste. Se der, grave um vídeo da ida e da volta: `agent-browser record start "$SCRATCH/cinema.webm"`, role de 0 a 1 em passos de 0.05 com `wait 300`, volte, e `agent-browser record stop`. Não faça o commit antes de o Sinval aprovar.

- [ ] **Passo 11: Commit**

```bash
git add src/lib/gsap.ts src/app/globals.css src/animations/projects-scroll.ts src/components/projects/ src/components/hero/hero.tsx "src/app/[lang]/page.tsx"
git commit -m "$(cat <<'EOF'
feat: pin the hero and the projects on one stage and drive the passage with ScrollTrigger

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Âncora `#projects`, teclado e clique simples

**Files:**
- Create: `src/lib/is-plain-click.ts`
- Test: `src/lib/is-plain-click.test.ts`
- Create: `src/animations/projects-focus.ts`
- Modify: `src/components/preferences/language-switch.tsx` (usa `isPlainClick`)
- Modify: `src/components/projects/projects-stage.tsx` (liga o foco e a âncora)

**Interfaces:**
- Consumes: `PHASE`, `measureTravel` e `createProjectsScroll` (Tarefa 3); `cardProgress` (Tarefa 1); `PROJECTS` e `target` (Tarefa 2); `<h2 id="projects-title" tabIndex={-1}>` dentro de `[data-projects="heading"]` (Tarefa 2).
- Produces:
  - `isPlainClick(event: Pick<MouseEvent, "button" | "metaKey" | "ctrlKey" | "shiftKey" | "altKey">): boolean`
  - `linkToProjects(stage: HTMLElement, tl: gsap.core.Timeline): () => void`
  - `followFocus(stage: HTMLElement, tl: gsap.core.Timeline): () => void`

- [ ] **Passo 1: Escrever os testes do clique simples**

Crie `src/lib/is-plain-click.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, test } from "node:test";
import { isPlainClick } from "./is-plain-click.ts";

const plain = {
  button: 0,
  metaKey: false,
  ctrlKey: false,
  shiftKey: false,
  altKey: false,
};

describe("isPlainClick", () => {
  test("aceita o clique com o botão esquerdo, sem tecla", () => {
    assert.equal(isPlainClick(plain), true);
  });

  test("recusa o botão do meio e o direito", () => {
    assert.equal(isPlainClick({ ...plain, button: 1 }), false);
    assert.equal(isPlainClick({ ...plain, button: 2 }), false);
  });

  test("recusa o clique com Ctrl, Cmd, Shift ou Alt", () => {
    for (const key of ["ctrlKey", "metaKey", "shiftKey", "altKey"] as const) {
      assert.equal(isPlainClick({ ...plain, [key]: true }), false, key);
    }
  });
});
```

- [ ] **Passo 2: Rodar e ver falhar**

Run: `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test src/lib/is-plain-click.test.ts`
Expected: FAIL com `ERR_MODULE_NOT_FOUND`.

- [ ] **Passo 3: Implementar**

Crie `src/lib/is-plain-click.ts`:

```ts
/** O que importa de um evento de clique para saber se ele é "simples". */
type ClickLike = Pick<
  MouseEvent,
  "button" | "metaKey" | "ctrlKey" | "shiftKey" | "altKey"
>;

/**
 * Clique simples: botão esquerdo, sem Ctrl/Cmd/Shift/Alt. Os outros
 * (abrir em nova aba, em nova janela, baixar) são do navegador, e o site
 * não intercepta.
 */
export function isPlainClick(event: ClickLike) {
  return (
    event.button === 0 &&
    !event.metaKey &&
    !event.ctrlKey &&
    !event.shiftKey &&
    !event.altKey
  );
}
```

Run: `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test src/lib/is-plain-click.test.ts`
Expected: PASS, 3 testes.

- [ ] **Passo 4: Usar no seletor de idioma**

Em `src/components/preferences/language-switch.tsx`, acrescente o import:

```tsx
import { isPlainClick } from "@/lib/is-plain-click";
```

e troque:

```tsx
    if (
      event.button !== 0 ||
      event.metaKey ||
      event.ctrlKey ||
      event.shiftKey ||
      event.altKey
    ) {
      return;
    }
```

por:

```tsx
    if (!isPlainClick(event)) return;
```

O comentário de cima ("Ctrl/Cmd/Shift/Alt + clique ou botão do meio...") continua.

- [ ] **Passo 5: Criar o foco e a âncora**

Crie `src/animations/projects-focus.ts`:

```ts
import { gsap } from "@/lib/gsap";
import { isPlainClick } from "@/lib/is-plain-click";
import { measureTravel, PHASE } from "./projects-scroll";
import { PROJECTS, target } from "./projects-targets";
import { cardProgress } from "./track-travel";

const HASH = "#projects";

// Variável de módulo: sobrevive à troca de idioma, que monta a página de
// novo sem recarregar. O endereço de chegada (digitado ou de um link de
// fora) é lido uma vez só, no "load"; depois da troca de idioma, mesmo
// com #projects na URL, a página fica onde estava.
let initialHashHandled = false;

/**
 * No modo cinema, a seção de Projetos fica em cima do hero, no topo do
 * palco: o pulo normal da âncora #projects iria para o topo da página e
 * nada aconteceria. Aqui, os links para #projects e o #projects no
 * endereço levam ao ponto em que os cards acabaram de chegar.
 *
 * Devolve a limpeza dos listeners.
 */
export function linkToProjects(stage: HTMLElement, tl: gsap.core.Timeline) {
  const st = tl.scrollTrigger;
  const title = stage.querySelector<HTMLElement>(
    `${target(PROJECTS.heading)} h2`,
  );
  if (!st || !title) return () => {};

  // labelToScroll: a posição de scroll (em px) em que a agulha passa
  // pelo label. É o scroll que corresponde ao começo do slide.
  const cardsTop = () => st.labelToScroll(PHASE.slide);

  // Um listener só no documento pega todos os links para #projects (o da
  // navbar e o "Ver projetos"): o clique "sobe" até o document.
  const onClick = (event: MouseEvent) => {
    if (event.defaultPrevented || !isPlainClick(event)) return;
    if (!(event.target instanceof Element)) return;
    if (!event.target.closest(`a[href="${HASH}"]`)) return;

    event.preventDefault();
    // O que a âncora faria: o #projects na URL (e no histórico)...
    history.pushState(null, "", HASH);
    // ...e o foco na seção, para quem usa teclado ou leitor de tela.
    // preventScroll: quem rola é a linha de baixo, com animação.
    title.focus({ preventScroll: true });
    // Saindo do topo, a rolagem suave passa pela transição inteira.
    window.scrollTo({ top: cardsTop(), behavior: "smooth" });
  };
  document.addEventListener("click", onClick);

  // Chegou com #projects no endereço: vai direto para os cards. Espera o
  // "load" porque o ScrollTrigger refaz as medidas nessa hora. A marca de
  // "já li o endereço" só muda quando a leitura acontece de verdade: em
  // desenvolvimento, o React monta e desmonta os efeitos uma vez a mais
  // (StrictMode), e a limpeza tira o listener antes do "load".
  const handleInitialHash = () => {
    initialHashHandled = true;
    if (location.hash !== HASH) return;
    // seek: põe a agulha no label na hora. Sem isso, o scrub levaria 1s
    // para alcançar o scroll e a pessoa veria a transição passar.
    tl.seek(PHASE.slide);
    window.scrollTo({ top: cardsTop(), behavior: "instant" });
  };
  if (!initialHashHandled) {
    if (document.readyState === "complete") handleInitialHash();
    else window.addEventListener("load", handleInitialHash, { once: true });
  }

  return () => {
    document.removeEventListener("click", onClick);
    window.removeEventListener("load", handleInitialHash);
  };
}

/**
 * Leva a página até o que recebeu o foco pelo teclado. O overflow: clip
 * do palco impede o navegador de rolar para o lado sozinho: sem isto, o
 * Tab iria para um card fora da tela.
 *
 * Devolve a limpeza do listener.
 */
export function followFocus(stage: HTMLElement, tl: gsap.core.Timeline) {
  const st = tl.scrollTrigger;
  const hero = stage.querySelector(target(PROJECTS.hero));
  // toArray com escopo: os cards de dentro do palco, como array.
  const cards = gsap.utils.toArray<HTMLElement>(target(PROJECTS.card), stage);
  if (!st || !hero || cards.length === 0) return () => {};

  const onFocusIn = (event: FocusEvent) => {
    const element = event.target;
    // Só foco de teclado (:focus-visible). O clique do mouse também foca
    // o link, e a página não pode pular antes de ele abrir.
    if (!(element instanceof Element) || !element.matches(":focus-visible")) {
      return;
    }

    // Voltou para o hero (Shift+Tab): volta para antes do reveal.
    if (hero.contains(element)) {
      if (window.scrollY > st.start) {
        window.scrollTo({ top: st.start, behavior: "instant" });
      }
      return;
    }

    const card = element.closest<HTMLElement>(target(PROJECTS.card));
    if (!card) return;

    // offsetLeft: onde o card está dentro da fileira, sem contar o
    // transform. O primeiro card começa na margem.
    const progress = cardProgress(
      card.offsetLeft,
      cards[0].offsetLeft,
      measureTravel(stage),
    );
    // interpolate: o ponto entre o começo e o fim do slide que corresponde
    // ao progresso (0 = começo, 1 = fim). O scrub anima o caminho.
    const top = gsap.utils.interpolate(
      st.labelToScroll(PHASE.slide),
      st.labelToScroll(PHASE.slideEnd),
      progress,
    );
    window.scrollTo({ top, behavior: "instant" });
  };

  stage.addEventListener("focusin", onFocusIn);
  return () => stage.removeEventListener("focusin", onFocusIn);
}
```

- [ ] **Passo 6: Ligar no palco**

Em `src/components/projects/projects-stage.tsx`, acrescente o import:

```tsx
import { followFocus, linkToProjects } from "@/animations/projects-focus";
```

e troque:

```tsx
      gsap.matchMedia().add(CINEMA_QUERY, () => {
        if (!stage.current) return;
        createProjectsScroll(stage.current);
      });
```

por:

```tsx
      gsap.matchMedia().add(CINEMA_QUERY, () => {
        if (!stage.current) return;
        const tl = createProjectsScroll(stage.current);
        const stopLinks = linkToProjects(stage.current, tl);
        const stopFocus = followFocus(stage.current, tl);

        // Os listeners não são do GSAP: a função devolvida é a limpeza
        // deles, e o matchMedia chama quando desfaz tudo.
        return () => {
          stopLinks();
          stopFocus();
        };
      });
```

- [ ] **Passo 7: Checar tipos, lint, testes e build**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build`
Expected: sem erros. O `pnpm test` roda também `track-travel.test.ts` e `is-plain-click.test.ts`.

- [ ] **Passo 8: Conferir a âncora e a abertura direta**

Suba o build na 3100. Depois:

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e1c2c4dc-a002-4ec8-b614-bb669e4103cf/scratchpad
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser set viewport 1440 900 1
agent-browser set media dark
agent-browser open http://localhost:3100/pt-br
agent-browser press Escape
agent-browser wait 3000
agent-browser click 'header nav a[href="#projects"]'
agent-browser wait 2500
agent-browser eval "JSON.stringify({ hash: location.hash, focus: document.activeElement.id, probe: JSON.parse($(cat "$SCRATCH/stage-probe.js")) })"
agent-browser screenshot "$SCRATCH/ancora-navbar.png"
agent-browser eval "window.scrollTo(0, 0)"
agent-browser wait 1600
agent-browser click '[data-projects="hero"] a[href="#projects"]'
agent-browser wait 2500
agent-browser eval "JSON.stringify({ hash: location.hash, focus: document.activeElement.id, probe: JSON.parse($(cat "$SCRATCH/stage-probe.js")) })"
```

Expected, nos dois cliques: `hash: "#projects"`, `focus: "projects-title"` e `cards[0].left` a ±2px de 43, ou seja, os cards acabaram de chegar. O print mostra os cards no lugar.

Abertura direta:

```bash
agent-browser open http://localhost:3100/pt-br#projects
agent-browser press Escape
agent-browser wait 3000
agent-browser eval "$(cat "$SCRATCH/stage-probe.js")"
agent-browser screenshot "$SCRATCH/abertura-direta.png"
```

Expected: `cards[0].left` a ±2px de 43, e o print com os cards no lugar, sem hero.

Recarregar no meio do slide:

```bash
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * 0.78)"
agent-browser wait 1600
agent-browser eval "Math.round(scrollY)"
agent-browser reload
agent-browser press Escape
agent-browser wait 3000
agent-browser eval "$(cat "$SCRATCH/stage-probe.js")"
```

Expected: a URL ainda tem `#projects`, então a página abre no começo do slide (`cards[0].left` ±2px de 43). É a regra da abertura direta. Sem hash (abra `/pt-br`, role para 0.78 e recarregue), o `scrollY` depois do reload fica a ±50px do de antes.

- [ ] **Passo 9: Clique com Ctrl não é interceptado (Review Focus 2)**

```bash
agent-browser open http://localhost:3100/pt-br
agent-browser press Escape
agent-browser wait 3000
agent-browser eval "(() => { let prevented = null; document.addEventListener('click', (e) => { prevented = e.defaultPrevented; e.preventDefault(); }, { once: true }); document.querySelector('header nav a[href=\"#projects\"]').dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true, ctrlKey: true, button: 0 })); return JSON.stringify({ prevented, scrollY }); })()"
```

Expected: `prevented: false`, porque o nosso listener deixou o Ctrl+clique passar, e `scrollY: 0`. O listener de teste, registrado depois do nosso, lê o `defaultPrevented` e cancela a navegação do evento sintético.

- [ ] **Passo 10: Teclado e clique do mouse na borda (Review Focus 5)**

Teclado, do hero para os cards e de volta:

```bash
agent-browser eval "window.scrollTo(0, 0)"
agent-browser wait 1600
agent-browser focus '[data-projects="hero"] ul li:last-child a'
for I in 1 2 3 4; do
  agent-browser press Tab
  agent-browser wait 1600
  agent-browser eval "JSON.stringify({ focus: document.activeElement.textContent, card: (() => { const r = document.activeElement.closest('[data-projects=card]')?.getBoundingClientRect(); return r && { left: Math.round(r.left), right: Math.round(r.right) }; })(), vw: document.documentElement.clientWidth })"
  agent-browser screenshot "$SCRATCH/tab-$I.png"
done
for I in 1 2 3 4; do agent-browser press Shift+Tab; done
agent-browser wait 1600
agent-browser eval "JSON.stringify({ focus: document.activeElement.textContent, scrollY: Math.round(scrollY) })"
```

Expected:
- A cada Tab, o card focado aparece inteiro (`card.left ≥ 0` e `card.right ≤ vw`), e o print mostra o anel de foco.
- Depois dos 4 Shift+Tab, o foco volta ao "LinkedIn↗(abre em nova aba)" com `scrollY: 0`, e o hero aparece de novo.

Clique do mouse num card meio escondido:

```bash
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * 0.62)"
agent-browser wait 1600
agent-browser eval "(() => { const r = document.querySelectorAll('[data-projects=card]')[2].getBoundingClientRect(); return JSON.stringify({ x: Math.round(Math.min(r.left + 40, innerWidth - 10)), y: Math.round(r.top + r.height / 2), scrollY: Math.round(scrollY) }); })()"
```

Com o `x` e o `y` que voltaram:

```bash
agent-browser mouse move <x> <y>
agent-browser mouse down
agent-browser mouse up
agent-browser wait 1000
agent-browser tab list
agent-browser eval "Math.round(scrollY)"
```

Expected: uma aba nova com o repositório do Blizzard Conquer, e o `scrollY` igual ao de antes do clique. Volte para a primeira aba com `agent-browser tab 0` se precisar.

- [ ] **Passo 11: Troca de idioma no meio do slide (Review Focus 3)**

```bash
agent-browser open http://localhost:3100/pt-br
agent-browser press Escape
agent-browser wait 3000
agent-browser click 'header nav a[href="#projects"]'
agent-browser wait 2500
agent-browser eval "window.scrollTo(0, (document.documentElement.scrollHeight - innerHeight) * 0.78)"
agent-browser wait 1600
agent-browser eval "Math.round(scrollY)"
agent-browser find role button click --name "Preferências"
agent-browser find role link click --name "English"
agent-browser wait 2500
agent-browser eval "JSON.stringify({ path: location.pathname, hash: location.hash, scrollY: Math.round(scrollY), probe: JSON.parse($(cat "$SCRATCH/stage-probe.js")) })"
```

Expected: `path: "/en"`, `hash: "#projects"`, `scrollY` a ±50px do de antes e os cards no mesmo ponto do slide. A página não vai para o topo nem para o começo dos cards.

**Se o `scrollY` voltar para 0**, é o pin sendo desfeito antes do palco novo existir: a página encolhe e o navegador puxa o scroll para o topo. A correção é guardar o scroll antes de o GSAP desfazer o pin e voltar a ele no palco novo. Em `src/components/projects/projects-stage.tsx`:

1. Troque `import { useRef } from "react";` por `import { useLayoutEffect, useRef } from "react";`.
2. Acima do `export function ProjectsStage`, acrescente:

```tsx
// Scroll de quando o palco foi desmontado (troca de idioma: a página é
// montada de novo sem recarregar). Variável de módulo, então sobrevive.
let scrollBeforeUnmount: number | null = null;
```

3. Logo antes do `useGSAP(`, acrescente:

```tsx
  // Declarado antes do useGSAP de propósito: na desmontagem, o React
  // roda as limpezas na ordem em que os efeitos foram declarados, então
  // esta guarda o scroll antes de o GSAP desfazer o pin.
  useLayoutEffect(
    () => () => {
      scrollBeforeUnmount = window.scrollY;
    },
    [],
  );
```

4. Dentro do `gsap.matchMedia().add(...)`, logo depois de `const stopFocus = followFocus(stage.current, tl);`, acrescente:

```tsx
        if (scrollBeforeUnmount !== null) {
          const top = scrollBeforeUnmount;
          scrollBeforeUnmount = null;
          tl.progress(
            gsap.utils.clamp(
              0,
              1,
              gsap.utils.normalize(tl.scrollTrigger?.start ?? 0, tl.scrollTrigger?.end ?? 1, top),
            ),
          );
          window.scrollTo({ top, behavior: "instant" });
        }
```

Refaça o passo 7 e este passo 11. Expected: agora passa.

Pare o servidor e feche a sessão.

- [ ] **Passo 12: Commit**

```bash
git add src/lib/is-plain-click.ts src/lib/is-plain-click.test.ts src/animations/projects-focus.ts src/components/preferences/language-switch.tsx src/components/projects/projects-stage.tsx
git commit -m "$(cat <<'EOF'
feat: send the #projects links and keyboard focus to the right point of the passage

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: README e verificação final

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: tudo das tarefas 1 a 4.
- Produces: README atualizado.

- [ ] **Passo 1: Atualizar o README**

Em `README.md`:

1. No fim do primeiro parágrafo, logo depois de `...as informações de contato.`, acrescente:

```markdown
 Rolando a página, o hero diminui e some enquanto o fundo muda, os cards de projetos sobem, e o scroll vertical passa a mover a fileira de cards para o lado.
```

2. Troque a linha `> Em construção: hoje existe só o hero da home. Veja [o que falta](#o-que-falta).` por:

```markdown
> Em construção: hoje a home tem o hero e a seção de Projetos. Veja [o que falta](#o-que-falta).
```

3. Na tabela da Stack, troque a linha do React por:

```markdown
| [React 19](https://react.dev) | Interface. Quase tudo é server component; só a intro, a luz do hero, o palco dos projetos, os seletores de idioma e de tema e as telas de erro rodam no cliente. |
```

4. Troque a linha do GSAP por:

```markdown
| [GSAP](https://gsap.com) + [`@gsap/react`](https://gsap.com/resources/React) | Animações: timeline da intro, desenho da logo (`DrawSVGPlugin`), voo da logo até a navbar (`Flip`), luz que segue o mouse (`quickTo`), passagem do hero para os projetos (`ScrollTrigger`, com `pin` e `scrub`) e versão reduzida para quem ativa "reduzir movimento" (`matchMedia`). |
```

5. Troque a linha do `node:test` por:

```markdown
| `node:test` | Testes das funções de idioma, de tema, das contas da fileira de projetos e do "clique simples", com o Node rodando TypeScript direto. |
```

6. No bloco "Outros comandos", troque a linha do `pnpm test` por:

```bash
pnpm test       # testes das funções puras (node --test)
```

7. Antes de `## Estrutura`, acrescente a seção:

````markdown
## Projetos

A lista fica em `src/data/projects.ts` (ano, stack e link) e nos dicionários (`projects.items`: nome e descrição em cada idioma). Para acrescentar um projeto, crie o id no tipo `ProjectId`, o item na lista e o texto nos dois dicionários; o TypeScript acusa se faltar um deles. A ordem da lista é a ordem da fileira.

A seção tem dois layouts, escolhidos pelo CSS com a variante `cinema:` (definida no `globals.css`):

- **Cinema** (JS ligado, sem "reduzir movimento" e tela com pelo menos 36rem de altura): o hero e os Projetos ficam empilhados num palco fixado pelo ScrollTrigger (`src/components/projects/projects-stage.tsx`). A timeline (`src/animations/projects-scroll.ts`) tem três fases: `reveal` (o hero diminui e some, o fundo novo aparece), `rise` (os cards sobem) e `slide` (a fileira anda na horizontal, pela largura real dela). Os links `#projects` e o Tab levam ao ponto certo da timeline (`src/animations/projects-focus.ts`).
- **Estático** (o resto, inclusive sem JavaScript): a seção vem depois do hero, com os cards em grade.

A condição do modo cinema está escrita em dois lugares, e os dois precisam ser iguais: a variante `cinema` no `globals.css` e o `CINEMA_QUERY` no `projects-stage.tsx`.
````

8. Na árvore da Estrutura:
   - troque `│   ├── hero/        # partes do hero: colagem, título, informações, luz` por:

```
│   ├── hero/        # partes do hero: colagem, título, informações, luz
│   ├── projects/    # seção de projetos, card e o palco que liga o scroll
```

   - troque `├── data/            # o que não muda com o idioma (nome, redes, ids das seções) e cards da colagem` por:

```
├── data/            # o que não muda com o idioma (nome, redes, ids das seções), cards da colagem e projetos
```

9. Troque o parágrafo `Os componentes marcam o que a intro anima com \`data-intro\`...` por:

```markdown
Os componentes marcam o que a intro anima com `data-intro`, e a timeline encontra esses elementos pelos nomes definidos em `src/animations/intro-targets.ts`. A passagem para os projetos faz o mesmo com `data-projects` e `src/animations/projects-targets.ts`. Assim a animação não depende de classes CSS nem de refs espalhadas.
```

10. Em "O que falta":
    - troque `- [ ] Seção **Projetos** (\`#projects\`): os links da navbar e o botão "Ver projetos" ainda não levam a lugar nenhum` por:

```markdown
- [x] Seção **Projetos** (`#projects`) com scroll cinematográfico
- [ ] Imagens dos projetos nos cards
```

    - troque `- [ ] Animações de scroll nas novas seções (ScrollTrigger)` por:

```markdown
- [ ] Animações de scroll nas seções Sobre e Contato
```

    - troque `- [ ] Textos definitivos em \`src/i18n/dictionaries/\` (hoje são provisórios)` por:

```markdown
- [ ] Textos definitivos do hero em `src/i18n/dictionaries/` (hoje são provisórios)
```

- [ ] **Passo 2: Verificação final**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build`
Expected: sem erros; `/pt-br` e `/en` estáticas.

Suba o build na 3100 e repita, rapidamente, em 1440×900 e 390×844, no tema escuro:
- `F` 0, 0.62 e 1 com o probe: `overflowX: 0` e cards no lugar;
- a volta até o 0: hero inteiro e `ctaClickable: true`;
- o clique no "Projetos" da navbar leva ao começo do slide;
- `/en`: textos em inglês na seção;
- rolar durante a intro: abra `/pt-br` e, sem apertar tecla, logo em seguida role para `F` 0.62 e espere 4000ms. A intro acelera e termina, e os cards ficam no lugar. Voltando para o 0, o hero aparece inteiro, com título e informações;
- trocar de tema no meio do slide: em `F` 0.78, abra "Preferências" (`agent-browser find role button click --name "Preferências"`) e escolha "Claro". A página não sai do lugar (`scrollY` igual, ±2px) e os cards continuam no mesmo ponto.

Pare o servidor, confira que nada ficou rodando na 3100 e feche a sessão.

- [ ] **Passo 3: Commit**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs: document the projects section, its two layouts and the scroll passage

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Passo 4: Depois do push (com o Sinval)**

O push e o PR só acontecem quando o Sinval pedir. No preview da Vercel, medir o PageSpeed no mobile e no desktop: o Lighthouse local dá número falso no mobile. Comparar com a nota da `main`.
