# Internacionalização (PT-BR / EN): plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** publicar o portfólio em `/pt-br` e `/en`, com o idioma escolhido pelo cookie, depois pelo navegador e por fim `en`, e um painel de preferências que troca de idioma sem repetir a intro.

**Architecture:** as rotas vão para `app/[lang]/`. Um `proxy.ts` redireciona endereços sem idioma usando funções puras testadas com `node --test`. Os textos saem dos componentes para dicionários `.ts` tipados, lidos no servidor com `next/root-params`. O painel usa o atributo `popover` do HTML; só o seletor de idioma é client component. Uma variável de módulo faz a intro pular para o fim quando a página é montada de novo na troca de idioma.

**Tech Stack:** Next.js 16.3 (App Router, Turbopack, `proxy.ts`, `next/root-params`), React 19.2, TypeScript 5.9, Tailwind CSS 4.3, GSAP 3.15, Biome 2.4, Node 24 (`node --test` com TypeScript nativo), agent-browser.

**Spec:** `docs/superpowers/specs/2026-09-24-i18n-design.md`

## Global Constraints

- Nenhuma dependência nova em `package.json` (nem de desenvolvimento).
- Idiomas: `pt-br` e `en`; padrão `en`; cookie `locale`; URLs `/pt-br` e `/en`.
- `<html lang>`: `pt-BR` / `en`. `og:locale`: `pt_BR` / `en_US`.
- Redirecionamento do proxy: **307**, mantendo a query string.
- Âncoras das seções: `#projects`, `#about`, `#contact` nos dois idiomas.
- Seletor de idioma só com texto ("Português", "English"), sem bandeira.
- Trocar de idioma não repete a intro; F5 repete.
- `/pt-br` e `/en` continuam páginas estáticas no `pnpm build`.
- Arquivos testados com `node --test` (`src/i18n/locales.ts`, `negotiate-locale.ts`, `localize-path.ts`) se importam com `.ts` no final e sem o alias `@/`. O resto do projeto continua usando `@/` sem extensão.
- Um arquivo, uma responsabilidade. Nomes em kebab-case.
- Comentários em português, curtos, explicando o conceito novo (proxy, `next/root-params`, `popover`, `tl.progress`, Cookie Store API), no estilo dos que já existem no projeto.
- Mensagens de commit em inglês, no formato do histórico (`feat: ...`), terminando com `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
- `pnpm lint` (Biome) precisa passar em todo commit. Se reclamar só de formatação ou da ordem dos imports, rode `pnpm exec biome check --write` e confira o diff.

## Receitas usadas nas tarefas

O estado do shell não persiste entre comandos, então cada bloco repete as variáveis de que precisa.

**Scratchpad** (saídas temporárias, screenshots, logs; nunca dentro do repo):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
```

**Subir o build de produção na porta 3100** (a 3000 pode ser do servidor do Sinval, que nunca deve ser mexido):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
cd /home/martins/projects/martinsportfolio
pnpm build
setsid pnpm start --port 3100 > "$SCRATCH/server.log" 2>&1 &
echo $! > "$SCRATCH/server.pgid"
for i in $(seq 1 30); do curl -s -o /dev/null http://localhost:3100/en && break; sleep 1; done
curl -s -o /dev/null -w "servidor: %{http_code}\n" http://localhost:3100/en
```

**Parar o servidor** (mata o grupo de processos inteiro: `pnpm`, `next` e `next-server`):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
kill -- -"$(cat "$SCRATCH/server.pgid")"
for i in $(seq 1 10); do curl -s -o /dev/null http://localhost:3100 || break; sleep 1; done
curl -s -o /dev/null http://localhost:3100 && echo "AINDA RODANDO" || echo "servidor parado"
```

**Sessão do agent-browser** (sempre uma sessão própria; screenshots com caminho absoluto no scratchpad):

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
```

No fim de cada tarefa que usou o navegador: `agent-browser close`.

## Ajustes em relação à spec

Decididos ao escrever o plano, depois de conferir as APIs:

1. **Seletor de idioma usa `<a>` + `router.replace`, não `<Link>`.** O `href` de um `<Link>` é calculado na renderização, e a âncora (`#about`) muda sem renderizar de novo. Com `<a>`, o clique lê `location.hash` na hora. Sem JavaScript, o `<a>` continua sendo um link normal.
2. **O idioma atual é um `<span aria-current="true">`, não um link.** Clicar no idioma em que já se está não faria nada.
3. **O cookie é gravado com a Cookie Store API** (`src/i18n/remember-locale.ts`). O Biome acusa `document.cookie` (`lint/suspicious/noDocumentCookie`). Se o navegador não tiver a API, a troca funciona do mesmo jeito, só não fica lembrada.
4. **O painel não tem `role` nem `aria-label`.** Com `role="dialog"`, o Biome pede `<dialog>`, e `<dialog popover>` briga com o CSS padrão do navegador (`dialog:not([open]) { display: none }`). Quem dá nome à lista é o texto "Idioma", via `aria-labelledby`; o botão tem `aria-label`.
5. **A chave `preferences.title` saiu.** O painel não tem título; o botão já diz "Preferências".
6. **Arquivos a mais:** `src/i18n/dictionaries/index.ts` (junta os dicionários), `src/i18n/remember-locale.ts`, `src/components/ui/sliders-icon.tsx` e `src/components/preferences/preferences-menu.module.css` (transição do painel, como o `hero-grid.module.css`).
7. **`hreflangPaths` mora em `locales.ts`**, usado pelos metadados e pelo sitemap.
8. **Node 22.18 ou mais novo** para o `pnpm test`: é a versão em que o Node passou a rodar TypeScript sem flag. O README hoje diz "Node 20"; a Tarefa 8 atualiza.

## Review Focus

Entradas que a spec implica, mas que é fácil esquecer. Cada uma tem teste ou checagem na tarefa indicada.

1. **O proxy não pode redirecionar arquivos.** `/robots.txt`, `/sitemap.xml`, `/manifest.webmanifest`, `/favicon.ico`, `/logo.svg`, `/hero/card-01.svg` e `/_next/...` devem responder 200, não 307. Se um deles for redirecionado, a imagem, o ícone ou o SEO quebram em silêncio. (Tarefa 2, passo 7)
2. **`Accept-Language` estranho** (`*`, `q=0`, `q=abc`, maiúsculas e espaços, `constructor`, `__proto__`) devolve um idioma válido e nunca quebra. (Tarefa 1, testes)
3. **A query string sobrevive ao redirecionamento.** `/?utm_source=linkedin` vira `/en?utm_source=linkedin`; o rastreio de onde veio a visita não se perde. (Tarefa 2, passo 7)
4. **A âncora sobrevive à troca de idioma.** Em `/pt-br#about`, escolher English leva para `/en#about`. (Tarefa 7, passo 5)
5. **Ctrl/Cmd + clique num idioma** segue o comportamento normal de link (nova aba) e não troca o idioma da aba atual. (Tarefa 6, passo 11)

---

### Tarefa 1: Idiomas e funções puras de idioma, com testes

**Files:**
- Create: `src/i18n/locales.ts`
- Create: `src/i18n/negotiate-locale.ts`
- Create: `src/i18n/localize-path.ts`
- Test: `src/i18n/negotiate-locale.test.ts`
- Test: `src/i18n/localize-path.test.ts`
- Modify: `package.json` (script `test`)
- Modify: `tsconfig.json` (`allowImportingTsExtensions`)

**Interfaces:**
- Consumes: nada.
- Produces:
  - `locales: readonly ["pt-br", "en"]`
  - `type Locale = "pt-br" | "en"`
  - `defaultLocale: Locale` (`"en"`)
  - `LOCALE_COOKIE: "locale"`
  - `hasLocale(value: string): value is Locale`
  - `localeInfo: Record<Locale, { htmlLang: string; ogLocale: string; name: string }>`
  - `hreflangPaths: Record<string, string>` (`{ "pt-BR": "/pt-br", en: "/en" }`)
  - `negotiateLocale(input: { cookie?: string; acceptLanguage?: string | null }): Locale`
  - `pathLocale(pathname: string): Locale | undefined`
  - `localizePath(pathname: string, locale: Locale): string`

- [ ] **Passo 1: Ligar os testes no projeto**

Em `package.json`, dentro de `"scripts"`, depois de `"format"`:

```json
    "format": "biome format --write",
    "test": "node --test \"src/**/*.test.ts\""
```

As aspas em volta do padrão fazem o próprio Node expandir o `**` (o `sh` não expande `**` recursivo).

Em `tsconfig.json`, logo depois de `"noEmit": true,`:

```json
    "noEmit": true,
    "allowImportingTsExtensions": true,
```

- [ ] **Passo 2: Escrever os testes que vão falhar**

`src/i18n/negotiate-locale.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, test } from "node:test";
import { negotiateLocale } from "./negotiate-locale.ts";

describe("negotiateLocale", () => {
  test("português do Brasil vira pt-br", () => {
    assert.equal(
      negotiateLocale({ acceptLanguage: "pt-BR,pt;q=0.9" }),
      "pt-br",
    );
  });

  test("inglês vira en", () => {
    assert.equal(negotiateLocale({ acceptLanguage: "en-US,en;q=0.9" }), "en");
  });

  test("português de Portugal também vira pt-br", () => {
    assert.equal(negotiateLocale({ acceptLanguage: "pt-PT" }), "pt-br");
  });

  test("pula idiomas sem suporte e pega o primeiro suportado", () => {
    assert.equal(
      negotiateLocale({ acceptLanguage: "de-DE,pt;q=0.5" }),
      "pt-br",
    );
  });

  test("entre os suportados, vale a ordem de preferência", () => {
    assert.equal(
      negotiateLocale({ acceptLanguage: "de-DE,en;q=0.8,pt;q=0.5" }),
      "en",
    );
  });

  test("vale o peso q, não a posição no cabeçalho", () => {
    assert.equal(
      negotiateLocale({ acceptLanguage: "en;q=0.5,pt;q=0.9" }),
      "pt-br",
    );
  });

  test("nenhum idioma suportado cai no padrão", () => {
    assert.equal(negotiateLocale({ acceptLanguage: "de" }), "en");
  });

  test("cabeçalho vazio ou ausente cai no padrão", () => {
    assert.equal(negotiateLocale({ acceptLanguage: "" }), "en");
    assert.equal(negotiateLocale({ acceptLanguage: null }), "en");
    assert.equal(negotiateLocale({}), "en");
  });

  test("cookie válido vence o navegador", () => {
    assert.equal(
      negotiateLocale({ cookie: "pt-br", acceptLanguage: "en-US" }),
      "pt-br",
    );
  });

  test("cookie inválido é ignorado", () => {
    assert.equal(
      negotiateLocale({ cookie: "fr", acceptLanguage: "pt-BR" }),
      "pt-br",
    );
  });

  test("q=0 quer dizer 'não quero este idioma'", () => {
    assert.equal(
      negotiateLocale({ acceptLanguage: "pt;q=0,en;q=0.1" }),
      "en",
    );
  });

  test("maiúsculas e espaços não atrapalham", () => {
    assert.equal(
      negotiateLocale({ acceptLanguage: "  PT-br ; q=0.8 , EN;q=0.2" }),
      "pt-br",
    );
  });

  test("q malformado é tratado como recusado", () => {
    assert.equal(negotiateLocale({ acceptLanguage: "pt;q=abc,en" }), "en");
  });

  test("curinga e nomes herdados de Object não viram idioma", () => {
    assert.equal(
      negotiateLocale({ acceptLanguage: "*, constructor, __proto__" }),
      "en",
    );
  });
});
```

`src/i18n/localize-path.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, test } from "node:test";
import { localizePath, pathLocale } from "./localize-path.ts";

describe("pathLocale", () => {
  test("lê o idioma do começo do caminho", () => {
    assert.equal(pathLocale("/en/x"), "en");
    assert.equal(pathLocale("/pt-br"), "pt-br");
  });

  test("prefixo parecido não conta como idioma", () => {
    assert.equal(pathLocale("/english"), undefined);
  });

  test("home e caminho vazio não têm idioma", () => {
    assert.equal(pathLocale("/"), undefined);
    assert.equal(pathLocale(""), undefined);
  });
});

describe("localizePath", () => {
  test("troca o idioma da home", () => {
    assert.equal(localizePath("/pt-br", "en"), "/en");
    assert.equal(localizePath("/en", "pt-br"), "/pt-br");
  });

  test("troca o idioma e mantém o resto do caminho", () => {
    assert.equal(localizePath("/pt-br/x", "en"), "/en/x");
  });

  test("acrescenta o idioma em caminho sem idioma", () => {
    assert.equal(localizePath("/x", "en"), "/en/x");
    assert.equal(localizePath("/", "pt-br"), "/pt-br");
  });

  test("não confunde prefixo parecido com idioma", () => {
    assert.equal(localizePath("/english", "pt-br"), "/pt-br/english");
  });

  test("a barra no fim da home some", () => {
    assert.equal(localizePath("/pt-br/", "en"), "/en");
  });
});
```

- [ ] **Passo 3: Rodar e ver falhar**

Run: `cd /home/martins/projects/martinsportfolio && pnpm test`
Expected: FAIL com `ERR_MODULE_NOT_FOUND` apontando para `negotiate-locale.ts` e `localize-path.ts`.

- [ ] **Passo 4: Escrever `src/i18n/locales.ts`**

```ts
// Fonte única dos idiomas do site. É importado pelo proxy, pelo servidor,
// pelo cliente e pelos testes (`pnpm test`).
//
// Os arquivos de i18n testados com `node --test` (este, negotiate-locale e
// localize-path) se importam com ".ts" no final e sem o alias "@/": o Node
// roda TypeScript direto, mas não conhece nenhum desses dois atalhos.

export const locales = ["pt-br", "en"] as const;

export type Locale = (typeof locales)[number];

/** Vale para quem não tem nem português nem inglês no navegador. */
export const defaultLocale: Locale = "en";

/** Cookie gravado quando a pessoa escolhe o idioma no painel. */
export const LOCALE_COOKIE = "locale";

/** Type guard: depois de `if (hasLocale(x))`, o TypeScript trata x como Locale. */
export function hasLocale(value: string): value is Locale {
  return (locales as readonly string[]).includes(value);
}

type LocaleInfo = {
  /** Vai no <html lang> e no hreflang. */
  htmlLang: string;
  /** Vai no og:locale (formato com "_"). */
  ogLocale: string;
  /** Nome no seletor, escrito no próprio idioma. */
  name: string;
};

export const localeInfo: Record<Locale, LocaleInfo> = {
  "pt-br": { htmlLang: "pt-BR", ogLocale: "pt_BR", name: "Português" },
  en: { htmlLang: "en", ogLocale: "en_US", name: "English" },
};

/**
 * Caminho de cada idioma no formato do hreflang:
 * { "pt-BR": "/pt-br", en: "/en" }. Usado nos metadados e no sitemap.
 */
export const hreflangPaths: Record<string, string> = Object.fromEntries(
  locales.map((locale) => [localeInfo[locale].htmlLang, `/${locale}`]),
);
```

- [ ] **Passo 5: Escrever `src/i18n/negotiate-locale.ts`**

```ts
import { defaultLocale, hasLocale, type Locale } from "./locales.ts";

type NegotiateInput = {
  /** Valor do cookie `locale`, se existir. */
  cookie?: string;
  /** Cabeçalho Accept-Language, ex.: "pt-BR,pt;q=0.9,en;q=0.8". */
  acceptLanguage?: string | null;
};

// Código principal -> idioma do site: "pt-BR" e "pt-PT" começam com "pt".
// Map, e não objeto: num objeto, um cabeçalho com "constructor" acharia
// a função herdada de Object e devolveria lixo.
const SUPPORTED = new Map<string, Locale>([
  ["pt", "pt-br"],
  ["en", "en"],
]);

/**
 * Escolhe o idioma de quem chegou sem idioma na URL: primeiro a escolha
 * salva no cookie, depois o idioma do navegador, por fim o padrão.
 */
export function negotiateLocale({
  cookie,
  acceptLanguage,
}: NegotiateInput): Locale {
  if (cookie && hasLocale(cookie)) return cookie;

  for (const tag of preferredLanguages(acceptLanguage ?? "")) {
    const locale = SUPPORTED.get(tag.split("-")[0]);
    if (locale) return locale;
  }

  return defaultLocale;
}

/**
 * "en;q=0.5, pt-BR" -> ["pt-br", "en"]: do mais preferido para o menos.
 * `q` é o peso de cada idioma (0 a 1, padrão 1); q=0 quer dizer "não".
 */
function preferredLanguages(header: string): string[] {
  return (
    header
      .split(",")
      .map((part, index) => {
        const [tag = "", ...params] = part.split(";");
        const q = params
          .map((param) => param.trim())
          .find((param) => param.startsWith("q="));
        const weight = q === undefined ? 1 : Number(q.slice(2));

        return {
          tag: tag.trim().toLowerCase(),
          weight: Number.isNaN(weight) ? 0 : weight,
          index,
        };
      })
      .filter(({ tag, weight }) => tag !== "" && weight > 0)
      // Maior peso primeiro; no empate, vale a ordem do cabeçalho.
      .sort((a, b) => b.weight - a.weight || a.index - b.index)
      .map(({ tag }) => tag)
  );
}
```

- [ ] **Passo 6: Escrever `src/i18n/localize-path.ts`**

```ts
import { hasLocale, type Locale } from "./locales.ts";

/** Idioma no começo do caminho: "/en/x" -> "en"; "/english" -> undefined. */
export function pathLocale(pathname: string): Locale | undefined {
  // "/en/x".split("/") = ["", "en", "x"]: o primeiro segmento é o [1].
  const first = pathname.split("/")[1] ?? "";
  return hasLocale(first) ? first : undefined;
}

/** Troca (ou acrescenta) o idioma no começo do caminho: "/pt-br/x" -> "/en/x". */
export function localizePath(pathname: string, locale: Locale): string {
  const current = pathLocale(pathname);
  // Tira "/pt-br" do começo, se tiver. Sobra "" (era a home), "/" ou "/x".
  const rest = current ? pathname.slice(current.length + 1) : pathname;
  return rest === "" || rest === "/" ? `/${locale}` : `/${locale}${rest}`;
}
```

- [ ] **Passo 7: Rodar e ver passar**

Run: `cd /home/martins/projects/martinsportfolio && pnpm test`
Expected: PASS, `ℹ fail 0`, 22 testes (14 de `negotiateLocale`, 8 de `pathLocale`/`localizePath`).

- [ ] **Passo 8: Lint e tipos**

Run: `cd /home/martins/projects/martinsportfolio && pnpm lint && pnpm exec tsc --noEmit`
Expected: sem erros. Se o Biome reclamar só de formatação, rode `pnpm exec biome check --write` e repita.

- [ ] **Passo 9: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add package.json tsconfig.json src/i18n
git commit -m "$(cat <<'EOF'
feat: add the locale list and the pure locale helpers with tests

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Tarefa 2: Rotas em `app/[lang]` e proxy

Nesta tarefa o site ainda fica todo em português nas duas URLs. O objetivo é deixar rotas, redirecionamentos e o 404 funcionando.

**Files:**
- Move: `src/app/layout.tsx` → `src/app/[lang]/layout.tsx`
- Move: `src/app/page.tsx` → `src/app/[lang]/page.tsx`
- Move: `src/app/not-found.tsx` → `src/app/[lang]/not-found.tsx`
- Move: `src/app/error.tsx` → `src/app/[lang]/error.tsx`
- Move: `src/app/opengraph-image.tsx` → `src/app/[lang]/opengraph-image.tsx`
- Create: `src/app/[lang]/[...rest]/page.tsx`
- Create: `src/proxy.ts`

**Interfaces:**
- Consumes (Tarefa 1): `locales`, `hasLocale`, `localeInfo`, `LOCALE_COOKIE`, `negotiateLocale`, `pathLocale`, `localizePath`.
- Produces: rota raiz `app/[lang]` com o parâmetro `lang`, que gera a função `lang()` em `next/root-params` e o tipo `LayoutProps<"/[lang]">`.

- [ ] **Passo 1: Mover os arquivos**

```bash
cd /home/martins/projects/martinsportfolio
mkdir -p 'src/app/[lang]/[...rest]'
git mv src/app/layout.tsx 'src/app/[lang]/layout.tsx'
git mv src/app/page.tsx 'src/app/[lang]/page.tsx'
git mv src/app/not-found.tsx 'src/app/[lang]/not-found.tsx'
git mv src/app/error.tsx 'src/app/[lang]/error.tsx'
git mv src/app/opengraph-image.tsx 'src/app/[lang]/opengraph-image.tsx'
```

Ficam em `src/app/`: `global-error.tsx`, `globals.css`, `robots.ts`, `sitemap.ts`, `manifest.ts`, `favicon.ico`, `apple-icon.png`.

- [ ] **Passo 2: Ajustar o layout**

Em `src/app/[lang]/layout.tsx`:

1. Troque `import "./globals.css";` por `import "../globals.css";`.
2. Acrescente os imports `import { notFound } from "next/navigation";` e `import { hasLocale, localeInfo, locales } from "@/i18n/locales";`.
3. Troque o componente `RootLayout` inteiro por:

```tsx
// Gera /pt-br e /en no build, como páginas estáticas.
export function generateStaticParams() {
  return locales.map((lang) => ({ lang }));
}

export default async function RootLayout({
  children,
  params,
}: LayoutProps<"/[lang]">) {
  const { lang } = await params;
  // O proxy só deixa passar idiomas válidos. Isto cobre o que ele ignora
  // (caminhos com ponto, como /foo.png).
  if (!hasLocale(lang)) notFound();

  return (
    <html
      lang={localeInfo[lang].htmlLang}
      className={`${fontVariables} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col bg-ink-900 font-sans text-fg">
        {children}
      </body>
    </html>
  );
}
```

`metadata` e `viewport` ficam como estão; a Tarefa 3 troca `metadata` por `generateMetadata`. **Não** acrescente `dynamicParams = false`: ele vale também para as rotas filhas e faria o `[...rest]` devolver o 404 genérico do Next.

- [ ] **Passo 3: Tirar o canonical fixo da home**

`src/app/[lang]/page.tsx` fica assim (o canonical `/` estaria errado nas duas URLs; a Tarefa 3 põe o certo no layout):

```tsx
import { Hero } from "@/components/hero/hero";
import { Intro } from "@/components/intro/intro";
import { Navbar } from "@/components/layout/navbar";

export default function HomePage() {
  return (
    <Intro>
      <Navbar />
      <main>
        <Hero />
      </main>
    </Intro>
  );
}
```

- [ ] **Passo 4: Criar a rota pega-tudo do 404**

`src/app/[lang]/[...rest]/page.tsx`:

```tsx
import { notFound } from "next/navigation";

/**
 * Pega qualquer URL que não existe dentro de um idioma (ex.: /en/xyz) e
 * mostra o 404 daquele idioma (app/[lang]/not-found.tsx), dentro do layout
 * certo. Sem esta rota, o Next não acharia um not-found para URLs
 * desconhecidas, porque o layout raiz fica dentro de [lang].
 */
export default function CatchAllPage() {
  notFound();
}
```

- [ ] **Passo 5: Criar o proxy**

`src/proxy.ts`:

```ts
import { type NextRequest, NextResponse } from "next/server";
import { LOCALE_COOKIE } from "@/i18n/locales";
import { localizePath, pathLocale } from "@/i18n/localize-path";
import { negotiateLocale } from "@/i18n/negotiate-locale";

/**
 * Proxy é o novo nome do middleware no Next 16: roda antes de qualquer
 * rota. Aqui ele só garante que toda URL tenha idioma, e "/" vira
 * "/pt-br" ou "/en". Ele não renderiza nada, então as páginas continuam
 * estáticas.
 */
export function proxy(request: NextRequest) {
  const { pathname } = request.nextUrl;
  if (pathLocale(pathname)) return NextResponse.next();

  const locale = negotiateLocale({
    cookie: request.cookies.get(LOCALE_COOKIE)?.value,
    acceptLanguage: request.headers.get("accept-language"),
  });

  // clone() leva junto a query string (?utm_source=...).
  const url = request.nextUrl.clone();
  url.pathname = localizePath(pathname, locale);

  // redirect() usa 307 (temporário) por padrão, e precisa ser temporário:
  // o destino depende de quem visita. Um 301/308 ficaria guardado no
  // navegador e prenderia a pessoa no idioma antigo.
  return NextResponse.redirect(url);
}

export const config = {
  // Tudo, menos os arquivos internos do Next (_next) e caminhos com ponto
  // (imagens, robots.txt, sitemap.xml, manifest, ícones).
  matcher: ["/((?!_next|.*\\..*).*)"],
};
```

- [ ] **Passo 6: Build**

Rode a receita "Subir o build de produção na porta 3100".
Expected no `pnpm build`: sem erros, e a tabela de rotas mostra `/[lang]` como SSG (●) com `/pt-br` e `/en` embaixo, e `ƒ Proxy (Middleware)`. `/[lang]/[...rest]` pode aparecer como dinâmica (ƒ): ela só serve 404.

- [ ] **Passo 7: Conferir redirecionamentos, arquivos e 404**

```bash
B=http://localhost:3100
r() { curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" "$@"; }
echo "--- redirecionamentos (esperado: 307 + destino)"
r -H "Accept-Language: pt-BR,pt;q=0.9" $B/          # 307 .../pt-br
r -H "Accept-Language: en-US,en;q=0.9" $B/          # 307 .../en
r -H "Accept-Language: de" $B/                      # 307 .../en
r -b "locale=pt-br" -H "Accept-Language: en-US" $B/ # 307 .../pt-br
r -H "Accept-Language: en" "$B/?utm_source=linkedin" # 307 .../en?utm_source=linkedin
echo "--- páginas (esperado: 200)"
r $B/pt-br; r $B/en
echo "--- 404 (esperado: 404; o /xyz primeiro redireciona)"
r $B/en/xyz; r -L -H "Accept-Language: en" $B/xyz
echo "--- arquivos (esperado: 200, nunca 307)"
for p in /robots.txt /sitemap.xml /manifest.webmanifest /favicon.ico /logo.svg /hero/card-01.svg /apple-icon.png; do printf "%s " "$p"; r $B$p; done
echo "--- ícones no <head> de /en (esperado: link rel=icon e apple-touch-icon)"
curl -s $B/en | grep -o '<link rel="\(icon\|apple-touch-icon\)[^>]*>'
echo "--- imagem de compartilhamento (esperado: 200 image/png)"
OG=$(curl -s $B/en | grep -o 'property="og:image" content="[^"]*"' | head -1 | sed 's/.*content="//;s/"$//' | sed "s#^https\?://[^/]*#$B#")
echo "$OG"; curl -s -o /dev/null -w "%{http_code} %{content_type}\n" "$OG"
```

Expected: exatamente o que está nos comentários. Se algum arquivo responder 307, o `matcher` está errado. Se `/apple-icon.png` responder 404, veja no HTML de `/en` qual é o caminho real (pode ter hash) e confira esse.

- [ ] **Passo 8: Parar o servidor e rodar o lint**

Rode a receita "Parar o servidor". Depois: `cd /home/martins/projects/martinsportfolio && pnpm lint && pnpm test`
Expected: sem erros.

- [ ] **Passo 9: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add -A src/app src/proxy.ts
git commit -m "$(cat <<'EOF'
feat: move the routes under [lang] and redirect bare URLs by locale

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Tarefa 3: Dicionários, textos do site e metadados

**Files:**
- Create: `src/i18n/dictionaries/pt-br.ts`
- Create: `src/i18n/dictionaries/en.ts`
- Create: `src/i18n/dictionaries/index.ts`
- Create: `src/i18n/get-dictionary.ts`
- Modify: `src/data/site.ts`
- Modify: `src/components/layout/navbar.tsx`
- Modify: `src/components/hero/hero-title.tsx`
- Modify: `src/components/hero/hero-info.tsx`
- Modify: `src/app/[lang]/not-found.tsx`
- Modify: `src/app/[lang]/layout.tsx`
- Modify: `src/app/[lang]/opengraph-image.tsx`

**Interfaces:**
- Consumes (Tarefa 1): `Locale`, `locales`, `hasLocale`, `defaultLocale`, `localeInfo`, `hreflangPaths`. (Tarefa 2): `lang()` de `next/root-params`.
- Produces:
  - `type Dictionary` com `meta.{title,description}`, `nav.{label,projects,about,contact}`, `preferences.{open,language}`, `hero.{role,tagline: string[],cta,newTab}`, `notFound.{title,description,back}`
  - `dictionaries: Record<Locale, Dictionary>` (`@/i18n/dictionaries`)
  - `getLocale(): Promise<Locale>` e `getDictionary(): Promise<Dictionary>` (`@/i18n/get-dictionary`, só servidor)
  - `site.nav: readonly ["projects", "about", "contact"]`, `site.ctaHref: "#projects"`

- [ ] **Passo 1: Escrever o dicionário em português**

`src/i18n/dictionaries/pt-br.ts`:

```ts
// Textos do site em português. O formato daqui define o tipo Dictionary:
// o en.ts precisa ter exatamente as mesmas chaves.
//
// Sem `as const` de propósito: assim cada valor tem tipo `string` (e não
// o texto exato), e o inglês pode ter textos diferentes nas mesmas chaves.
export const ptBr = {
  meta: {
    title: "Portfólio",
    description: "Desenvolvedor Full Stack. Portfólio de Sinval Martins.",
  },
  nav: {
    label: "Principal",
    projects: "Projetos",
    about: "Sobre",
    contact: "Contato",
  },
  preferences: {
    open: "Preferências",
    language: "Idioma",
  },
  hero: {
    role: "Desenvolvedor Full Stack",
    tagline: ["Disponível para", "novos projetos"],
    cta: "Ver projetos",
    newTab: "(abre em nova aba)",
  },
  notFound: {
    title: "Página não encontrada",
    description: "O endereço pode ter mudado ou nunca ter existido.",
    back: "Voltar para o início",
  },
};

export type Dictionary = typeof ptBr;
```

- [ ] **Passo 2: Escrever o dicionário em inglês faltando uma chave (o "teste que falha")**

O teste de paridade dos dicionários é o próprio TypeScript. Para ver que ele funciona, comece **sem** `newTab`:

`src/i18n/dictionaries/en.ts`:

```ts
import type { Dictionary } from "./pt-br";

// Anotar o tipo (`: Dictionary`) é o que faz o TypeScript acusar chave
// faltando ou sobrando em relação ao português.
export const en: Dictionary = {
  meta: {
    title: "Portfolio",
    description: "Full Stack Developer. Portfolio of Sinval Martins.",
  },
  nav: {
    label: "Main",
    projects: "Projects",
    about: "About",
    contact: "Contact",
  },
  preferences: {
    open: "Preferences",
    language: "Language",
  },
  hero: {
    role: "Full Stack Developer",
    tagline: ["Available for", "new projects"],
    cta: "View projects",
  },
  notFound: {
    title: "Page not found",
    description: "This address may have changed or never existed.",
    back: "Back to home",
  },
};
```

Run: `cd /home/martins/projects/martinsportfolio && pnpm exec next typegen && pnpm exec tsc --noEmit`
Expected: FAIL com `Property 'newTab' is missing in type ... but required in type ...` em `en.ts`.

- [ ] **Passo 3: Completar o dicionário em inglês**

Em `src/i18n/dictionaries/en.ts`, no bloco `hero`, depois de `cta`:

```ts
    cta: "View projects",
    newTab: "(opens in a new tab)",
```

Run: `pnpm exec tsc --noEmit`
Expected: sem erros.

- [ ] **Passo 4: Juntar os dicionários e criar o leitor do servidor**

`src/i18n/dictionaries/index.ts`:

```ts
import type { Locale } from "../locales";
import { en } from "./en";
import { type Dictionary, ptBr } from "./pt-br";

export type { Dictionary };

/** Dicionário de cada idioma. Só código de servidor importa isto. */
export const dictionaries: Record<Locale, Dictionary> = {
  "pt-br": ptBr,
  en,
};
```

`src/i18n/get-dictionary.ts`:

```ts
import { notFound } from "next/navigation";
import { lang } from "next/root-params";
import { type Dictionary, dictionaries } from "./dictionaries";
import { hasLocale, type Locale } from "./locales";

// `lang` vem de next/root-params: o Next gera uma função para cada
// parâmetro dinâmico acima do layout raiz (aqui, o [lang] de app/[lang]).
// Qualquer server component chama e descobre o idioma da página atual,
// sem ninguém passar `lang` por props. Não roda em client components (o
// build acusa) nem em rotas de imagem, como a de Open Graph.

/** Idioma da página atual. */
export async function getLocale(): Promise<Locale> {
  const value = await lang();
  if (!hasLocale(value)) notFound();
  return value;
}

/** Textos da página atual. */
export async function getDictionary(): Promise<Dictionary> {
  return dictionaries[await getLocale()];
}
```

- [ ] **Passo 5: Deixar em `site.ts` só o que não muda com o idioma**

`src/data/site.ts` inteiro:

```ts
export const site = {
  name: "Sinval Martins",
  // Ids das seções da home, na ordem da navbar. São iguais nos dois
  // idiomas: trocar de idioma mantém a âncora da URL (#about continua
  // #about). Os rótulos ficam nos dicionários (src/i18n/dictionaries).
  nav: ["projects", "about", "contact"],
  ctaHref: "#projects",
  socials: [
    { label: "GitHub", href: "https://github.com/smartinsdev" },
    { label: "LinkedIn", href: "https://www.linkedin.com/in/smartins-dev" },
  ],
} as const;
```

- [ ] **Passo 6: Navbar lendo o dicionário**

`src/components/layout/navbar.tsx` inteiro:

```tsx
import { INTRO } from "@/animations/intro-targets";
import { Logo } from "@/components/ui/logo";
import { site } from "@/data/site";
import { getDictionary, getLocale } from "@/i18n/get-dictionary";

export async function Navbar() {
  // Promise.all busca os dois ao mesmo tempo, e não um depois do outro.
  const [locale, dict] = await Promise.all([getLocale(), getDictionary()]);

  return (
    <header className="fixed inset-x-0 top-0 z-40 flex items-center justify-between px-[clamp(1.25rem,3vw,5rem)] py-[clamp(1rem,2.6svh,2.5rem)]">
      {/* Na intro, a logo do preloader voa até aqui e é trocada por esta.
          O header não pode ser animado: a logo mudaria de lugar.
          O link vai para a home do idioma atual, e não para "/", para não
          passar pelo redirecionamento do proxy. */}
      <a href={`/${locale}`} data-intro={INTRO.navLogo} className="inline-flex">
        <Logo
          id="navbar-logo"
          className="h-[clamp(2rem,2.5vw,3.5rem)] w-auto"
        />
        <span className="sr-only">{site.name}</span>
      </a>

      <nav data-intro={INTRO.nav} aria-label={dict.nav.label}>
        <ul className="flex gap-[clamp(0.25rem,1.2vw,2rem)] text-[clamp(0.875rem,0.85vw,1.125rem)] font-medium">
          {site.nav.map((section) => (
            <li key={section}>
              <a
                href={`#${section}`}
                className="inline-flex min-h-11 items-center px-[0.6em] text-fg-muted transition-colors duration-200 hover:text-fg"
              >
                {dict.nav[section]}
              </a>
            </li>
          ))}
        </ul>
      </nav>
    </header>
  );
}
```

- [ ] **Passo 7: Hero lendo o dicionário**

Em `src/components/hero/hero-title.tsx`:

1. Acrescente `import { getDictionary } from "@/i18n/get-dictionary";`.
2. Troque `export function HeroTitle() {` por:

```tsx
export async function HeroTitle() {
  const dict = await getDictionary();

```

3. Troque `{site.role}` por `{dict.hero.role}`.

`src/components/hero/hero-info.tsx`:

1. Acrescente `import { getDictionary } from "@/i18n/get-dictionary";`.
2. Troque o começo da função:

```tsx
export async function HeroInfo() {
  const dict = await getDictionary();
  const [firstLine, ...otherLines] = dict.hero.tagline;
```

3. Troque `<ButtonLink href={site.cta.href}>{site.cta.label}</ButtonLink>` por `<ButtonLink href={site.ctaHref}>{dict.hero.cta}</ButtonLink>`.
4. Troque `<span className="sr-only">(abre em nova aba)</span>` por `<span className="sr-only">{dict.hero.newTab}</span>`.

- [ ] **Passo 8: 404 lendo o dicionário**

`src/app/[lang]/not-found.tsx` inteiro:

```tsx
import type { Metadata } from "next";
import { StatusPage } from "@/components/status/status-page";
import { ButtonLink } from "@/components/ui/button-link";
import { getDictionary, getLocale } from "@/i18n/get-dictionary";

// O not-found não recebe `params`, mas next/root-params não precisa deles:
// getDictionary() descobre o idioma sozinho.
export async function generateMetadata(): Promise<Metadata> {
  const dict = await getDictionary();
  return { title: dict.notFound.title };
}

export default async function NotFound() {
  const [locale, dict] = await Promise.all([getLocale(), getDictionary()]);

  return (
    <StatusPage
      code="404"
      title={dict.notFound.title}
      description={dict.notFound.description}
    >
      <ButtonLink href={`/${locale}`}>{dict.notFound.back}</ButtonLink>
    </StatusPage>
  );
}
```

O passo 12 confere se o Next aplica esse `generateMetadata` (a documentação só garante isso para o `global-not-found`). Se não aplicar, o plano B está lá.

- [ ] **Passo 9: Metadados por idioma no layout**

Em `src/app/[lang]/layout.tsx`:

1. Apague as constantes `title` e `description` e o `export const metadata`.
2. Ajuste os imports: acrescente `import { dictionaries } from "@/i18n/dictionaries";` e troque o import de locales por `import { hasLocale, hreflangPaths, localeInfo, locales } from "@/i18n/locales";`. O import de `site` continua.
3. Acrescente, antes do `export const viewport`:

```tsx
type LayoutParams = { params: Promise<{ lang: string }> };

export async function generateMetadata({
  params,
}: LayoutParams): Promise<Metadata> {
  const { lang } = await params;
  if (!hasLocale(lang)) notFound();

  const { meta } = dictionaries[lang];
  const title = `${site.name} — ${meta.title}`;

  return {
    metadataBase: siteUrl,
    title: { default: title, template: `%s — ${site.name}` },
    description: meta.description,
    alternates: {
      canonical: `/${lang}`,
      // hreflang: avisa o Google que /pt-br e /en são a mesma página em
      // dois idiomas. x-default é para quem não lê nenhum dos dois: "/"
      // deixa o proxy escolher.
      languages: { ...hreflangPaths, "x-default": "/" },
    },
    openGraph: {
      type: "website",
      locale: localeInfo[lang].ogLocale,
      alternateLocale: locales
        .filter((other) => other !== lang)
        .map((other) => localeInfo[other].ogLocale),
      siteName: title,
      title,
      description: meta.description,
    },
    twitter: { card: "summary_large_image" },
  };
}
```

- [ ] **Passo 10: Imagem de compartilhamento com o cargo no idioma**

Em `src/app/[lang]/opengraph-image.tsx`:

1. Acrescente os imports `import { dictionaries } from "@/i18n/dictionaries";` e `import { defaultLocale, hasLocale } from "@/i18n/locales";`.
2. Troque a linha do `export const alt` (que usa `site.role`) por `export const alt = site.name;`. A Tarefa 5 faz um alt por idioma.
3. Troque a assinatura e o começo da função:

```tsx
// Uma imagem por idioma. Rotas de imagem não rodam next/root-params, então
// o idioma vem de `params`, como numa página comum.
export default async function OpengraphImage({
  params,
}: {
  params: Promise<{ lang: string }>;
}) {
  const { lang } = await params;
  const { role } = dictionaries[hasLocale(lang) ? lang : defaultLocale].hero;

  return new ImageResponse(
```

4. Troque `{site.role}` por `{role}`.

- [ ] **Passo 11: Tipos, lint e build**

Run: `cd /home/martins/projects/martinsportfolio && pnpm exec tsc --noEmit && pnpm lint && pnpm test`
Expected: sem erros. Um `site.role` ou `site.cta` esquecido aparece aqui.

Depois, rode a receita "Subir o build de produção na porta 3100".
Expected: build sem erros; `/pt-br` e `/en` continuam SSG (●).

- [ ] **Passo 12: Conferir textos, metadados e 404**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
B=http://localhost:3100
EN="$SCRATCH/en.html"; PT="$SCRATCH/pt.html"
curl -s $B/en > "$EN"; curl -s $B/pt-br > "$PT"
echo "--- /en"
grep -o '<html lang="[^"]*"' "$EN"                              # <html lang="en"
grep -o '<title>[^<]*</title>' "$EN"                           # Sinval Martins — Portfolio
grep -c 'Full Stack Developer\|View projects\|opens in a new tab\|aria-label="Main"' "$EN"  # >= 1
grep -c 'Ver projetos\|Desenvolvedor' "$EN"                    # 0
grep -io 'rel="canonical" href="[^"]*"' "$EN"                  # .../en
grep -io 'hreflang="[^"]*" href="[^"]*"' "$EN"                 # pt-BR -> /pt-br, en -> /en, x-default -> /
grep -o 'property="og:locale[^>]*>' "$EN"                      # og:locale en_US e og:locale:alternate pt_BR
echo "--- /pt-br"
grep -o '<html lang="[^"]*"' "$PT"                              # <html lang="pt-BR"
grep -o '<title>[^<]*</title>' "$PT"                           # Sinval Martins — Portfólio
grep -c 'Desenvolvedor Full Stack\|Ver projetos\|href="#projects"' "$PT"  # >= 1
echo "--- 404"
curl -s -o /dev/null -w "%{http_code}\n" $B/en/xyz             # 404
curl -s $B/en/xyz | grep -o '<title>[^<]*</title>\|Page not found\|href="/en"' | sort -u
curl -s $B/pt-br/xyz | grep -o '<title>[^<]*</title>\|Página não encontrada\|href="/pt-br"' | sort -u
rm -f "$EN" "$PT"
```

Expected: o que está nos comentários. No 404 em inglês, o título deve ser `Page not found — Sinval Martins`.

**Plano B do título do 404:** se o `<title>` do 404 vier como `Sinval Martins — Portfolio` (o `generateMetadata` do not-found foi ignorado), ou se o build acusar erro nesse export, troque o `generateMetadata` em `not-found.tsx` por um título neutro, que serve aos dois idiomas:

```tsx
// O Next não aplica generateMetadata no not-found.tsx, e "404" dispensa
// tradução.
export const metadata: Metadata = { title: "404" };
```

Depois, rebuild e confira de novo (esperado: `404 — Sinval Martins`).

- [ ] **Passo 13: Ver a imagem de compartilhamento em inglês**

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
B=http://localhost:3100
OG=$(curl -s $B/en | grep -o 'property="og:image" content="[^"]*"' | head -1 | sed 's/.*content="//;s/"$//' | sed "s#^https\?://[^/]*#$B#")
curl -s "$OG" -o "$SCRATCH/og-en.png" && file "$SCRATCH/og-en.png"
```

Abra `$SCRATCH/og-en.png` com a ferramenta Read. Expected: "Sinval Martins" e, embaixo, "Full Stack Developer".

- [ ] **Passo 14: Parar o servidor e fazer commit**

Rode a receita "Parar o servidor".

```bash
cd /home/martins/projects/martinsportfolio
git add -A src/i18n src/data/site.ts src/components src/app
git commit -m "$(cat <<'EOF'
feat: move the site copy into typed PT-BR and EN dictionaries

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Tarefa 4: Telas de erro traduzidas

**Files:**
- Create: `src/i18n/error-texts.ts`
- Create: `src/i18n/use-path-locale.ts`
- Modify: `src/components/status/error-screen.tsx`
- Modify: `src/app/global-error.tsx`

**Interfaces:**
- Consumes (Tarefa 1): `Locale`, `defaultLocale`, `localeInfo`, `pathLocale`.
- Produces:
  - `errorTexts: Record<Locale, { code: string; title: string; description: string; retry: string; back: string }>`
  - `usePathLocale(): Locale` (hook de client component)

- [ ] **Passo 1: Textos das telas de erro**

`src/i18n/error-texts.ts`:

```ts
import type { Locale } from "./locales";

type ErrorTexts = {
  code: string;
  title: string;
  description: string;
  retry: string;
  back: string;
};

/**
 * Textos das telas de erro. Ficam fora dos dicionários porque error
 * boundaries são client components: importar o dicionário inteiro
 * mandaria todos os textos do site para o JavaScript do navegador.
 */
export const errorTexts: Record<Locale, ErrorTexts> = {
  "pt-br": {
    code: "Erro",
    title: "Algo deu errado",
    description:
      "Um erro inesperado impediu esta página de carregar. Tente de novo; se continuar, volte para o início.",
    retry: "Tentar de novo",
    back: "Voltar para o início",
  },
  en: {
    code: "Error",
    title: "Something went wrong",
    description:
      "An unexpected error stopped this page from loading. Try again; if it keeps happening, go back to the home page.",
    retry: "Try again",
    back: "Back to home",
  },
};
```

- [ ] **Passo 2: Hook que lê o idioma da URL no cliente**

`src/i18n/use-path-locale.ts`:

```ts
import { usePathname } from "next/navigation";
import { defaultLocale, type Locale } from "./locales";
import { pathLocale } from "./localize-path";

/**
 * Idioma da página atual em client components, lido do começo da URL.
 * No servidor, use getLocale(): next/root-params não roda no cliente.
 */
export function usePathLocale(): Locale {
  return pathLocale(usePathname()) ?? defaultLocale;
}
```

- [ ] **Passo 3: Tela de erro traduzida**

Em `src/components/status/error-screen.tsx`:

1. Acrescente `import { errorTexts } from "@/i18n/error-texts";` e `import { usePathLocale } from "@/i18n/use-path-locale";`.
2. Troque o corpo da função `ErrorScreen` por:

```tsx
export function ErrorScreen({ error, retry }: ErrorScreenProps) {
  const locale = usePathLocale();
  const texts = errorTexts[locale];

  useEffect(() => {
    // Por enquanto só no console. Um serviço de monitoramento (ex.:
    // Sentry) receberia o erro aqui.
    console.error(error);
  }, [error]);

  return (
    <StatusPage
      code={texts.code}
      title={texts.title}
      description={texts.description}
    >
      <Button onClick={() => retry()}>{texts.retry}</Button>
      <a
        href={`/${locale}`}
        className="text-fg-muted underline-offset-4 transition-colors duration-200 hover:text-fg hover:underline"
      >
        {texts.back}
      </a>
    </StatusPage>
  );
}
```

- [ ] **Passo 4: Erro global traduzido**

`src/app/global-error.tsx` inteiro:

```tsx
"use client"; // Error boundaries precisam ser client components.

import { ErrorScreen } from "@/components/status/error-screen";
import { errorTexts } from "@/i18n/error-texts";
import { localeInfo } from "@/i18n/locales";
import { usePathLocale } from "@/i18n/use-path-locale";
import { fontVariables } from "@/lib/fonts";
import "./globals.css";

/**
 * Substitui o layout raiz quando ele mesmo quebra. Fica fora de [lang] e
 * desenha o próprio <html>, então descobre o idioma pela URL.
 */
export default function GlobalError(props: {
  error: Error & { digest?: string };
  retry: () => void;
}) {
  const locale = usePathLocale();

  return (
    <html
      lang={localeInfo[locale].htmlLang}
      className={`${fontVariables} h-full antialiased`}
    >
      <body className="min-h-full bg-ink-900 font-sans text-fg">
        <title>{errorTexts[locale].title}</title>
        <ErrorScreen {...props} />
      </body>
    </html>
  );
}
```

- [ ] **Passo 5: Tipos e lint**

Run: `cd /home/martins/projects/martinsportfolio && pnpm exec tsc --noEmit && pnpm lint`
Expected: sem erros.

- [ ] **Passo 6: Provocar os erros e conferir no navegador**

Acrescente **temporariamente** duas linhas (serão desfeitas no passo 7):

- em `src/components/hero/hero-title.tsx`, primeira linha dentro de `HeroTitle`:
  `if (process.env.I18N_ERROR_CHECK) throw new Error("i18n error check");`
- em `src/app/[lang]/layout.tsx`, primeira linha dentro de `RootLayout`:
  `if (process.env.I18N_GLOBAL_ERROR_CHECK) throw new Error("i18n global error check");`

Erro de página (`error.tsx`), com o servidor de desenvolvimento (o `global-error` também aparece em dev desde o Next 15.2):

```bash
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
cd /home/martins/projects/martinsportfolio
setsid env I18N_ERROR_CHECK=1 pnpm dev --port 3100 > "$SCRATCH/server.log" 2>&1 &
echo $! > "$SCRATCH/server.pgid"
for i in $(seq 1 60); do curl -s -o /dev/null http://localhost:3100/en && break; sleep 1; done
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
for l in en pt-br; do
  agent-browser open http://localhost:3100/$l >/dev/null
  agent-browser wait 3000 >/dev/null
  agent-browser eval '({ lang: document.documentElement.lang, text: ["Something went wrong","Try again","Algo deu errado","Tentar de novo"].filter(t => document.body.innerText.includes(t)), back: document.querySelector("main a[href^=\"/\"]")?.getAttribute("href") })'
done
```

Expected: em `/en`: `text: ["Something went wrong","Try again"]`, `back: "/en"`. Em `/pt-br`: `text: ["Algo deu errado","Tentar de novo"]`, `back: "/pt-br"`.

Rode a receita "Parar o servidor". Repita o bloco acima trocando `I18N_ERROR_CHECK=1` por `I18N_GLOBAL_ERROR_CHECK=1`.
Expected: o mesmo, e agora também `lang: "en"` em `/en` e `lang: "pt-BR"` em `/pt-br` (quem desenha o `<html>` é o `global-error`).

Rode a receita "Parar o servidor" e `agent-browser close`.

- [ ] **Passo 7: Desfazer as linhas temporárias**

```bash
cd /home/martins/projects/martinsportfolio
git diff --stat -- src/components/hero/hero-title.tsx 'src/app/[lang]/layout.tsx'   # só 1 linha em cada
git checkout -- src/components/hero/hero-title.tsx 'src/app/[lang]/layout.tsx'
git diff --stat -- src/components/hero/hero-title.tsx 'src/app/[lang]/layout.tsx'   # vazio
grep -rn "I18N_.*ERROR_CHECK" src || echo "limpo"
```

Expected: `limpo`.

- [ ] **Passo 8: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add src/i18n/error-texts.ts src/i18n/use-path-locale.ts src/components/status/error-screen.tsx src/app/global-error.tsx
git commit -m "$(cat <<'EOF'
feat: translate the error screens from the URL locale

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Tarefa 5: SEO por idioma (alt da imagem, sitemap e manifest)

**Files:**
- Modify: `src/app/[lang]/opengraph-image.tsx`
- Modify: `src/app/sitemap.ts`
- Modify: `src/app/manifest.ts`

**Interfaces:**
- Consumes (Tarefa 1): `locales`, `hasLocale`, `defaultLocale`, `hreflangPaths`. (Tarefa 3): `dictionaries`.
- Produces: nada que outras tarefas usem.

- [ ] **Passo 1: Alt da imagem por idioma**

Em `src/app/[lang]/opengraph-image.tsx`:

1. Apague `export const alt = site.name;`.
2. Acrescente, antes da função padrão:

```tsx
function roleFor(lang: string) {
  return dictionaries[hasLocale(lang) ? lang : defaultLocale].hero.role;
}

// `export const alt` seria um texto só para as duas imagens.
// generateImageMetadata descreve cada imagem da rota e recebe `params`,
// então dá para ter um alt por idioma. Aqui é uma imagem só (id "og").
export function generateImageMetadata({
  params,
}: {
  params: { lang: string };
}) {
  return [
    {
      id: "og",
      alt: `${site.name} — ${roleFor(params.lang)}`,
      size,
      contentType,
    },
  ];
}
```

3. Dentro da função padrão, troque `const { role } = dictionaries[hasLocale(lang) ? lang : defaultLocale].hero;` por `const role = roleFor(lang);`.

- [ ] **Passo 2: Sitemap com os dois idiomas**

`src/app/sitemap.ts` inteiro:

```ts
import type { MetadataRoute } from "next";
import { hreflangPaths, locales } from "@/i18n/locales";
import { siteUrl } from "@/lib/site-url";

const absolute = (path: string) => new URL(path, siteUrl).href;

// Uma entrada por idioma. As duas repetem o par pt-BR/en em
// `alternates.languages`: é assim que o Google entende que são a mesma
// página traduzida.
export default function sitemap(): MetadataRoute.Sitemap {
  const languages = Object.fromEntries(
    Object.entries(hreflangPaths).map(([hreflang, path]) => [
      hreflang,
      absolute(path),
    ]),
  );

  return locales.map((locale) => ({
    url: absolute(`/${locale}`),
    lastModified: new Date(),
    alternates: { languages },
  }));
}
```

- [ ] **Passo 3: Manifest neutro**

Em `src/app/manifest.ts`:

1. Troque o comentário do topo por:

```ts
// Usado quando alguém instala o site na tela inicial do Android. Os ícones
// ficam em public/ porque o Next não tem convenção de arquivo para eles.
// É um manifest só para os dois idiomas: nome neutro, e start_url "/"
// deixa o proxy escolher o idioma quando o app abre.
```

2. Troque a linha do `name` (hoje com "— Portfólio") por `name: site.name,`.

- [ ] **Passo 4: Build e conferência**

Rode a receita "Subir o build de produção na porta 3100".

```bash
B=http://localhost:3100
echo "--- alt por idioma"
curl -s $B/en | grep -o 'property="og:image:alt" content="[^"]*"'     # Sinval Martins — Full Stack Developer
curl -s $B/pt-br | grep -o 'property="og:image:alt" content="[^"]*"'  # Sinval Martins — Desenvolvedor Full Stack
echo "--- imagem responde"
for l in en pt-br; do
  OG=$(curl -s $B/$l | grep -o 'property="og:image" content="[^"]*"' | head -1 | sed 's/.*content="//;s/"$//' | sed "s#^https\?://[^/]*#$B#")
  echo "$OG"; curl -s -o /dev/null -w "%{http_code} %{content_type}\n" "$OG"   # 200 image/png
done
echo "--- sitemap"
curl -s $B/sitemap.xml
echo; echo "--- manifest"
curl -s $B/manifest.webmanifest | grep -o '"name":"[^"]*"\|"short_name":"[^"]*"\|"start_url":"[^"]*"'
```

Expected:
- alt conforme os comentários;
- as duas imagens respondem `200 image/png`;
- o sitemap tem dois `<url>` (`/pt-br` e `/en`), cada um com `<xhtml:link rel="alternate" hreflang="pt-BR" ...>` e `hreflang="en"`;
- manifest com `"name":"Sinval Martins"`, `"short_name":"Sinval Martins"`, `"start_url":"/"`.

**Se o alt do `/pt-br` vier em inglês,** o `params` chegou como Promise nesta versão. Troque a função por:

```tsx
export async function generateImageMetadata({
  params,
}: {
  params: Promise<{ lang: string }>;
}) {
  const { lang } = await params;
  return [
    { id: "og", alt: `${site.name} — ${roleFor(lang)}`, size, contentType },
  ];
}
```

**Se o build quebrar por causa do `generateImageMetadata`,** ou a imagem deixar de responder 200: apague o `generateImageMetadata`, volte `export const alt = site.name;` e escreva na mensagem do commit que o alt ficou neutro e por quê. É o plano B da spec.

- [ ] **Passo 5: Parar o servidor, lint e commit**

Rode a receita "Parar o servidor".

```bash
cd /home/martins/projects/martinsportfolio
pnpm lint && pnpm test
git add 'src/app/[lang]/opengraph-image.tsx' src/app/sitemap.ts src/app/manifest.ts
git commit -m "$(cat <<'EOF'
feat: localize the share image alt and list both locales in the sitemap

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Tarefa 6: Painel de preferências com o seletor de idioma

**Files:**
- Create: `src/i18n/remember-locale.ts`
- Create: `src/components/ui/sliders-icon.tsx`
- Create: `src/components/preferences/preferences-menu.tsx`
- Create: `src/components/preferences/preferences-menu.module.css`
- Create: `src/components/preferences/language-switch.tsx`
- Modify: `src/components/layout/navbar.tsx`

**Interfaces:**
- Consumes (Tarefa 1): `Locale`, `locales`, `localeInfo`, `LOCALE_COOKIE`, `localizePath`. (Tarefa 3): `getLocale`, `getDictionary`, `dict.preferences.{open,language}`, `dict.nav.label`.
- Produces:
  - `rememberLocale(locale: Locale): void`
  - `<SlidersIcon {...svgProps} />`
  - `<PreferencesMenu />` (async server component)
  - `<LanguageSwitch current={Locale} labelledBy={string} />` (client). A Tarefa 7 acrescenta uma chamada dentro de `switchTo`.
  - ids no DOM: painel `#preferences-panel`; botão `button[popovertarget="preferences-panel"]`

- [ ] **Passo 1: Gravar a escolha no cookie**

`src/i18n/remember-locale.ts`:

```ts
import { LOCALE_COOKIE, type Locale } from "./locales";

const ONE_YEAR_MS = 365 * 24 * 60 * 60 * 1000;

/**
 * Grava a escolha de idioma no cookie que o proxy lê na próxima visita a
 * "/". Usa a Cookie Store API: o jeito novo de mexer em cookies, com
 * objeto e promessa em vez de montar a string de `document.cookie` na mão.
 *
 * Num navegador sem a API, a troca de idioma funciona igual; só não fica
 * lembrada. Por isso uma falha aqui é ignorada.
 */
export function rememberLocale(locale: Locale) {
  if (!("cookieStore" in window)) return;

  window.cookieStore
    .set({
      name: LOCALE_COOKIE,
      value: locale,
      path: "/",
      expires: Date.now() + ONE_YEAR_MS,
      sameSite: "lax",
    })
    .catch(() => undefined);
}
```

- [ ] **Passo 2: Ícone do botão**

`src/components/ui/sliders-icon.tsx`:

```tsx
import type { ComponentProps } from "react";

// Ícone "sliders-horizontal" do Lucide (licença ISC). O botão que usa o
// ícone é quem tem o nome acessível, então o SVG fica escondido.
export function SlidersIcon(props: ComponentProps<"svg">) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      <path d="M21 4h-7M10 4H3M21 12h-9M8 12H3M21 20h-5M12 20H3M14 2v4M8 10v4M16 18v4" />
    </svg>
  );
}
```

- [ ] **Passo 3: Seletor de idioma (client)**

`src/components/preferences/language-switch.tsx`:

```tsx
"use client";

import { usePathname, useRouter } from "next/navigation";
import type { MouseEvent } from "react";
import { type Locale, localeInfo, locales } from "@/i18n/locales";
import { localizePath } from "@/i18n/localize-path";
import { rememberLocale } from "@/i18n/remember-locale";

type LanguageSwitchProps = {
  current: Locale;
  /** id do texto "Idioma", que dá nome à lista para leitores de tela. */
  labelledBy: string;
};

export function LanguageSwitch({ current, labelledBy }: LanguageSwitchProps) {
  const router = useRouter();
  const pathname = usePathname();

  function switchTo(event: MouseEvent<HTMLAnchorElement>, locale: Locale) {
    // Ctrl/Cmd/Shift/Alt + clique ou botão do meio: o navegador abre em
    // outra aba ou janela, como em qualquer link. Só o clique simples
    // troca o idioma desta aba.
    if (
      event.button !== 0 ||
      event.metaKey ||
      event.ctrlKey ||
      event.shiftKey ||
      event.altKey
    ) {
      return;
    }

    event.preventDefault();
    rememberLocale(locale);
    // Navegação do Next no cliente, sem recarregar a página.
    // replace: o botão voltar não vira uma alternância entre idiomas.
    // scroll: false: a página fica onde estava. O hash (#about) vai junto,
    // lido agora, porque ele muda sem o componente renderizar de novo.
    router.replace(`${localizePath(pathname, locale)}${window.location.hash}`, {
      scroll: false,
    });
  }

  return (
    <ul aria-labelledby={labelledBy} className="mt-1 flex flex-col">
      {locales.map((locale) => {
        const { htmlLang, name } = localeInfo[locale];

        // O idioma atual não é link: clicar nele não faria nada.
        if (locale === current) {
          return (
            <li key={locale}>
              <span
                aria-current="true"
                lang={htmlLang}
                className="flex min-h-11 items-center gap-3 text-fg"
              >
                <span
                  aria-hidden="true"
                  className="size-1.5 rounded-full bg-accent"
                />
                {name}
              </span>
            </li>
          );
        }

        // Um link de verdade: sem JavaScript, vira uma navegação normal.
        return (
          <li key={locale}>
            <a
              href={localizePath(pathname, locale)}
              hrefLang={htmlLang}
              lang={htmlLang}
              onClick={(event) => switchTo(event, locale)}
              className="flex min-h-11 items-center gap-3 text-fg-muted transition-colors duration-200 hover:text-fg"
            >
              <span aria-hidden="true" className="size-1.5" />
              {name}
            </a>
          </li>
        );
      })}
    </ul>
  );
}
```

- [ ] **Passo 4: Transição do painel**

`src/components/preferences/preferences-menu.module.css`:

```css
/* Transição de abrir e fechar o painel. Fica em CSS Module porque
   @starting-style e allow-discrete são bem mais legíveis aqui do que em
   classes Tailwind. */

.panel {
  opacity: 0;
  translate: 0 -0.5rem;
  /* display e overlay mudam de uma vez (não dá para "meio display").
     allow-discrete segura essa troca até o fim da transição: sem isso, o
     painel sumiria na hora ao fechar, sem o fade. */
  transition:
    opacity 0.2s ease-out,
    translate 0.2s ease-out,
    display 0.2s allow-discrete,
    overlay 0.2s allow-discrete;
}

.panel:popover-open {
  opacity: 1;
  translate: 0 0;
}

/* Ao abrir, o painel sai de display: none e não tem um "antes" para
   animar. @starting-style diz de onde a transição parte. */
@starting-style {
  .panel:popover-open {
    opacity: 0;
    translate: 0 -0.5rem;
  }
}

@media (prefers-reduced-motion: reduce) {
  .panel {
    transition: none;
  }
}
```

- [ ] **Passo 5: Botão e painel (server)**

`src/components/preferences/preferences-menu.tsx`:

```tsx
import { SlidersIcon } from "@/components/ui/sliders-icon";
import { getDictionary, getLocale } from "@/i18n/get-dictionary";
import { LanguageSwitch } from "./language-switch";
import styles from "./preferences-menu.module.css";

const PANEL_ID = "preferences-panel";
const LANGUAGE_LABEL_ID = "preferences-language";

/**
 * Botão da navbar que abre o painel de preferências. Abrir e fechar é
 * trabalho do navegador (atributo `popover`), então isto continua sendo
 * server component. Só o seletor de idioma roda no cliente.
 */
export async function PreferencesMenu() {
  const [locale, dict] = await Promise.all([getLocale(), getDictionary()]);

  return (
    <>
      {/* popoverTarget liga o botão ao painel pelo id. O navegador também
          informa aos leitores de tela se o painel está aberto. */}
      <button
        type="button"
        popoverTarget={PANEL_ID}
        aria-label={dict.preferences.open}
        className="inline-flex size-11 cursor-pointer items-center justify-center rounded-full text-fg-muted transition-colors duration-200 hover:text-fg"
      >
        <SlidersIcon className="size-5" />
      </button>

      {/* popover="auto": fecha com Esc, com clique fora e devolve o foco ao
          botão. Aberto, vai para a "top layer": fica acima de tudo e é
          posicionado pela janela, então a animação da intro no elemento
          pai não o desloca. */}
      <div
        id={PANEL_ID}
        popover="auto"
        className={`${styles.panel} fixed inset-auto top-[calc(clamp(1rem,2.6svh,2.5rem)+3.25rem)] right-[clamp(1.25rem,3vw,5rem)] m-0 w-56 rounded-xl border-0 bg-ink-800 p-4 text-fg shadow-2xl shadow-black/40 ring-1 ring-line`}
      >
        <p
          id={LANGUAGE_LABEL_ID}
          className="font-mono text-xs uppercase tracking-widest text-fg-muted"
        >
          {dict.preferences.language}
        </p>
        <LanguageSwitch current={locale} labelledBy={LANGUAGE_LABEL_ID} />
      </div>
    </>
  );
}
```

- [ ] **Passo 6: Botão na navbar**

Em `src/components/layout/navbar.tsx`:

1. Acrescente `import { PreferencesMenu } from "@/components/preferences/preferences-menu";`.
2. Troque o bloco `<nav ...>...</nav>` inteiro por:

```tsx
      {/* Links e preferências entram juntos na intro. O botão fica fora do
          <nav> porque não é navegação. Abaixo de 360px o espaço entre os
          links diminui para o botão caber (medido antes: sobravam 7px em
          320px, e o botão precisa de 44px). */}
      <div data-intro={INTRO.nav} className="flex items-center">
        <nav aria-label={dict.nav.label}>
          <ul className="flex gap-[clamp(0.25rem,1.2vw,2rem)] text-[clamp(0.875rem,0.85vw,1.125rem)] font-medium max-[22.5rem]:gap-0">
            {site.nav.map((section) => (
              <li key={section}>
                <a
                  href={`#${section}`}
                  className="inline-flex min-h-11 items-center px-[0.6em] text-fg-muted transition-colors duration-200 hover:text-fg max-[22.5rem]:px-[0.2em]"
                >
                  {dict.nav[section]}
                </a>
              </li>
            ))}
          </ul>
        </nav>
        <PreferencesMenu />
      </div>
```

- [ ] **Passo 7: Tipos, lint e build**

Run: `cd /home/martins/projects/martinsportfolio && pnpm exec tsc --noEmit && pnpm lint`
Expected: sem erros.

Rode a receita "Subir o build de produção na porta 3100". Expected: `/pt-br` e `/en` continuam SSG (●).

- [ ] **Passo 8: Navbar sem estouro em telas estreitas**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
for w in 320 360 375 1440; do
  agent-browser set viewport $w 800 >/dev/null
  agent-browser open http://localhost:3100/pt-br >/dev/null
  agent-browser wait '[data-intro="nav"][style*="opacity: 1"]' >/dev/null
  agent-browser eval '(() => { const h = document.querySelector("header"); const cs = getComputedStyle(h); const inner = h.clientWidth - parseFloat(cs.paddingLeft) - parseFloat(cs.paddingRight); const used = [...h.children].reduce((s, el) => s + el.getBoundingClientRect().width, 0); return { viewport: innerWidth, free: Math.round(inner - used), overflow: document.documentElement.scrollWidth > innerWidth }; })()'
done
```

Expected: em todas as larguras, `free >= 0` e `overflow: false`. Se 320px estourar, diminua `max-[22.5rem]:px-[0.2em]` para `max-[22.5rem]:px-[0.1em]` e meça de novo. Não mexa nas larguras de 375px para cima.

- [ ] **Passo 9: Painel abre, fecha e funciona pelo teclado**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
BTN='button[popovertarget="preferences-panel"]'
IS_OPEN='document.querySelector("#preferences-panel").matches(":popover-open")'
agent-browser set viewport 1440 900 >/dev/null
agent-browser open http://localhost:3100/pt-br >/dev/null
agent-browser wait '[data-intro="nav"][style*="opacity: 1"]' >/dev/null
agent-browser click "$BTN" >/dev/null; agent-browser wait 300 >/dev/null
agent-browser eval "$IS_OPEN"                                          # true
agent-browser screenshot "$SCRATCH/preferences-open.png" >/dev/null
agent-browser press Escape >/dev/null; agent-browser wait 300 >/dev/null
agent-browser eval "$IS_OPEN"                                          # false
agent-browser eval 'document.activeElement?.getAttribute("popovertarget")'  # "preferences-panel"
agent-browser click "$BTN" >/dev/null; agent-browser wait 300 >/dev/null
agent-browser click 'main' >/dev/null; agent-browser wait 300 >/dev/null
agent-browser eval "$IS_OPEN"                                          # false (clique fora)
agent-browser focus "$BTN" >/dev/null; agent-browser press Enter >/dev/null; agent-browser wait 300 >/dev/null
agent-browser press Tab >/dev/null
agent-browser eval 'document.activeElement?.textContent'               # "English"
agent-browser press Escape >/dev/null
```

Expected: os valores dos comentários. Abra `$SCRATCH/preferences-open.png` com Read e confira o visual: painel escuro com borda sutil, "IDIOMA" em mono, "● Português" em destaque e "English" apagado, alinhado à direita, abaixo da navbar.

- [ ] **Passo 10: Trocar de idioma pelo painel**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser cookies clear >/dev/null
agent-browser open http://localhost:3100/ >/dev/null
agent-browser eval 'location.pathname'                                 # "/en": o Chrome de teste pede inglês
agent-browser open http://localhost:3100/en >/dev/null
agent-browser wait '[data-intro="nav"][style*="opacity: 1"]' >/dev/null
agent-browser eval 'window.__preloader = document.querySelector("[data-intro=\"preloader\"]"); true'
agent-browser click 'button[popovertarget="preferences-panel"]' >/dev/null; agent-browser wait 300 >/dev/null
agent-browser click '#preferences-panel a[hreflang="pt-BR"]' >/dev/null; agent-browser wait 1000 >/dev/null
agent-browser eval '({ path: location.pathname, lang: document.documentElement.lang, role: document.querySelector("[data-intro=\"subtitle\"]")?.textContent, remounted: document.querySelector("[data-intro=\"preloader\"]") !== window.__preloader, preloaderVisibility: getComputedStyle(document.querySelector("[data-intro=\"preloader\"]")).visibility })'
agent-browser cookies get
agent-browser open http://localhost:3100/ >/dev/null
agent-browser eval 'location.pathname'                                 # "/pt-br": só o cookie explica
```

Expected:
- a primeira visita a `/`, sem cookie, vai para `/en`;
- depois da troca: `path: "/pt-br"`, `lang: "pt-BR"`, `role: "Desenvolvedor Full Stack"`;
- `cookies get` mostra `locale=pt-br`;
- a última visita a `/` vai para `/pt-br`. O navegador de teste pede inglês, então só o cookie explica esse destino.

Se a primeira visita a `/` já cair em `/pt-br`, o navegador de teste está em português. Nesse caso, inverta a checagem: comece em `/pt-br`, troque para `a[hreflang="en"]` e espere `/en` no fim.

**Anote `remounted` e `preloaderVisibility`**, porque a Tarefa 7 depende disso. Com `remounted: true`, a intro montou de novo. Nesse caso é esperado que o preloader esteja visível ou que a intro esteja tocando de novo: é o problema que a Tarefa 7 resolve.

- [ ] **Passo 11: Ctrl + clique não troca o idioma desta aba**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser open http://localhost:3100/pt-br >/dev/null
agent-browser wait '[data-intro="nav"][style*="opacity: 1"]' >/dev/null
cat <<'EOF' | agent-browser eval --stdin
(() => {
  const link = document.querySelector('#preferences-panel a[hreflang="en"]');
  let preventedByUs;
  // O React escuta cliques no document; este listener na window roda
  // depois dele. Ele registra se o nosso onClick cancelou o link e depois
  // cancela a navegação, para o teste não sair da página.
  window.addEventListener(
    "click",
    (event) => {
      preventedByUs = event.defaultPrevented;
      event.preventDefault();
    },
    { once: true },
  );
  link.dispatchEvent(
    new MouseEvent("click", { bubbles: true, cancelable: true, ctrlKey: true, button: 0 }),
  );
  return { preventedByUs, path: location.pathname };
})()
EOF
agent-browser wait 800 >/dev/null
agent-browser eval 'location.pathname'                                 # "/pt-br"
agent-browser close
```

Expected: `{ preventedByUs: false, path: "/pt-br" }` e depois `"/pt-br"`. O `false` mostra que o `switchTo` deixou o Ctrl + clique com o navegador.

- [ ] **Passo 12: Parar o servidor e fazer commit**

Rode a receita "Parar o servidor".

```bash
cd /home/martins/projects/martinsportfolio
git add src/i18n/remember-locale.ts src/components/ui/sliders-icon.tsx src/components/preferences src/components/layout/navbar.tsx
git commit -m "$(cat <<'EOF'
feat: add a preferences popover with the language switch

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Tarefa 7: Trocar de idioma sem repetir a intro

**Files:**
- Create: `src/components/intro/intro-played.ts`
- Modify: `src/components/intro/intro.tsx`
- Modify: `src/components/preferences/language-switch.tsx`

**Interfaces:**
- Consumes (Tarefa 6): `switchTo` dentro de `LanguageSwitch`. (Existente): `createIntroTimeline`, `releaseIntro`, `speedUpOnInput`.
- Produces: `hasIntroPlayed(): boolean`, `markIntroPlayed(): void`.

- [ ] **Passo 1: Escrever a checagem que vai falhar**

Com o build da Tarefa 6 (suba com a receita, se não estiver no ar), meça **logo depois** da troca. Neste passo a checagem roda antes do código novo:

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
cat > "$SCRATCH/switch-check.sh" <<'EOF'
agent-browser open http://localhost:3100/pt-br >/dev/null
agent-browser wait '[data-intro="nav"][style*="opacity: 1"]' >/dev/null
agent-browser eval 'history.replaceState(null, "", "#about"); true' >/dev/null
agent-browser click 'button[popovertarget="preferences-panel"]' >/dev/null
agent-browser wait 300 >/dev/null
agent-browser click '#preferences-panel a[hreflang="en"]' >/dev/null
agent-browser wait 400 >/dev/null
agent-browser eval '({ url: location.pathname + location.hash, lang: document.documentElement.lang, preloader: getComputedStyle(document.querySelector("[data-intro=\"preloader\"]")).visibility, title: getComputedStyle(document.querySelector("[data-intro=\"title-word\"]")).opacity, nav: getComputedStyle(document.querySelector("[data-intro=\"nav\"]")).opacity })'
EOF
bash "$SCRATCH/switch-check.sh"
```

O esperado depois da Tarefa 7 é `{ url: "/en#about", lang: "en", preloader: "hidden", title: "1", nav: "1" }`.
Expected agora: se a Tarefa 6 anotou `remounted: true`, pelo menos um desses campos sai diferente (preloader `visible` ou título/nav abaixo de `1`). Se anotou `remounted: false`, a checagem já pode passar: a Tarefa 7 vira uma proteção, e o comentário do passo 2 registra isso.

- [ ] **Passo 2: Guardar se a intro já tocou**

`src/components/intro/intro-played.ts`:

```ts
/**
 * Lembra se a intro já tocou nesta aba. É uma variável de módulo: ela
 * sobrevive às navegações do Next no cliente (como trocar de idioma) e
 * zera quando a página recarrega (F5), que é quando a intro deve tocar
 * de novo.
 *
 * Na troca de idioma, o React monta a <Intro> de novo (medido com o
 * agent-browser), então sem isto a intro repetiria.
 */
let played = false;

export function hasIntroPlayed() {
  return played;
}

export function markIntroPlayed() {
  played = true;
}
```

Esse último parágrafo vale para `remounted: true`, o resultado esperado. **Se o passo 10 da Tarefa 6 deu `remounted: false`,** troque o parágrafo por:

```ts
 * Na troca de idioma, o React reaproveita a <Intro> (medido com o
 * agent-browser). Isto protege as navegações futuras de volta à home.
```

- [ ] **Passo 3: A intro pula para o fim quando já tocou**

Em `src/components/intro/intro.tsx`:

1. Acrescente `import { hasIntroPlayed, markIntroPlayed } from "./intro-played";` junto dos imports locais.
2. Troque o callback do `gsap.matchMedia().add(...)` inteiro por:

```tsx
      gsap.matchMedia().add(MOTION_QUERIES, (context) => {
        const tl = createIntroTimeline({
          reducedMotion: Boolean(context.conditions?.reducedMotion),
          onComplete: () => {
            markIntroPlayed();
            setIntroDone(true);
          },
        });
        timeline.current = tl;

        // A intro já tocou nesta aba (ex.: a pessoa trocou de idioma).
        // Em vez de repetir, a timeline salta para o fim: progress(1) =
        // 100% do caminho. Antes sai a pausa das imagens, senão o salto
        // pararia nela. O useGSAP roda antes de o navegador pintar a
        // tela, então o preloader nem aparece. O salto não suprime os
        // callbacks, então o onComplete dispara e a luz do mouse liga.
        if (hasIntroPlayed()) {
          releaseIntro(tl);
          tl.progress(1);
          return;
        }

        // Se a preferência mudar depois de as imagens carregarem, a
        // timeline nova não pode ficar parada esperando por elas.
        if (imagesLoaded.current) releaseIntro(tl);

        // Clicar, tocar, rolar ou apertar uma tecla acelera o resto da
        // intro. A função devolvida aqui é a limpeza: o matchMedia chama
        // ela quando desfaz tudo (ao desmontar ou mudar a preferência).
        return speedUpOnInput(tl);
      });
```

- [ ] **Passo 4: Marcar na troca de idioma**

Em `src/components/preferences/language-switch.tsx`:

1. Acrescente `import { markIntroPlayed } from "@/components/intro/intro-played";`.
2. Em `switchTo`, logo depois de `rememberLocale(locale);`:

```tsx
    rememberLocale(locale);
    // A página do outro idioma pode montar a intro de novo: isto avisa
    // que ela já tocou e deve aparecer pronta.
    markIntroPlayed();
```

- [ ] **Passo 5: Rodar a checagem e ver passar**

Run: `cd /home/martins/projects/martinsportfolio && pnpm exec tsc --noEmit && pnpm lint`. Depois, receita "Parar o servidor" (se estiver no ar), receita "Subir o build de produção na porta 3100" e:

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
bash "$SCRATCH/switch-check.sh"
```

Expected: `{ url: "/en#about", lang: "en", preloader: "hidden", title: "1", nav: "1" }`.

- [ ] **Passo 6: F5 toca a intro de novo**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
agent-browser reload >/dev/null
agent-browser eval 'getComputedStyle(document.querySelector("[data-intro=\"preloader\"]")).visibility'   # "visible"
agent-browser wait '[data-intro="nav"][style*="opacity: 1"]' >/dev/null
agent-browser eval 'getComputedStyle(document.querySelector("[data-intro=\"preloader\"]")).visibility'   # "hidden"
```

Expected: `"visible"` logo depois do reload (a intro está tocando) e `"hidden"` no fim.

- [ ] **Passo 7: O mesmo com "reduzir movimento"**

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
agent-browser set media reduced-motion >/dev/null
bash "$SCRATCH/switch-check.sh"
agent-browser close
rm -f "$SCRATCH/switch-check.sh"
```

Expected: `{ url: "/en#about", lang: "en", preloader: "hidden", title: "1", nav: "1" }`. Feche o navegador aqui: a emulação de "reduzir movimento" não pode vazar para outras checagens.

- [ ] **Passo 8: Parar o servidor e fazer commit**

Rode a receita "Parar o servidor".

```bash
cd /home/martins/projects/martinsportfolio
git add src/components/intro/intro-played.ts src/components/intro/intro.tsx src/components/preferences/language-switch.tsx
git commit -m "$(cat <<'EOF'
feat: skip the intro when switching languages

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

---

### Tarefa 8: README e verificação final

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: tudo das tarefas anteriores.
- Produces: nada.

- [ ] **Passo 1: Atualizar o README**

Em `README.md`:

1. Depois do primeiro parágrafo (o que termina em "informações de contato."), acrescente:

```md
O site existe em português e inglês: `/pt-br` e `/en`.
```

2. Na tabela da Stack, troque a linha do React por:

```md
| [React 19](https://react.dev) | Interface. Quase tudo é server component; só a intro, a luz do hero, o seletor de idioma e as telas de erro rodam no cliente. |
```

e acrescente, depois da linha do `next/og`:

```md
| `proxy.ts` + `next/root-params` | Idiomas sem biblioteca: o proxy manda `/` para `/pt-br` ou `/en`, e os server components descobrem o idioma sem receber props. |
| `node:test` | Testes das funções de idioma, com o Node rodando TypeScript direto. |
```

3. Em "Como rodar", troque `Precisa de Node.js 20 ou mais novo e pnpm.` por:

```md
Precisa de Node.js 22.18 ou mais novo (o `pnpm test` roda TypeScript direto no Node) e pnpm.
```

e, no bloco "Outros comandos", depois de `pnpm format`:

```bash
pnpm test       # testes das funções de idioma (node --test)
```

4. Depois da seção "Variáveis de ambiente", acrescente:

```md
## Idiomas

Quem abre `/` é redirecionado pelo `src/proxy.ts`. Vale primeiro o idioma escolhido no painel de preferências (cookie `locale`), depois o idioma do navegador e, por último, o inglês.

- Os textos ficam em `src/i18n/dictionaries/`. O `pt-br.ts` define o formato, e o `en.ts` precisa ter as mesmas chaves: o TypeScript acusa se faltar ou sobrar alguma.
- Server components leem os textos com `getDictionary()` (`src/i18n/get-dictionary.ts`), sem receber o idioma por props.
- As telas de erro rodam no navegador e usam `src/i18n/error-texts.ts`.
- Para divulgar o site, use `smartins.dev/pt-br` ou `smartins.dev/en`: o endereço sem idioma passa por um redirecionamento.
```

5. Na árvore da seção "Estrutura", troque as duas linhas do `app/`:

```
├── app/             # rotas, layout raiz, CSS global, 404, erro, robots,
│                    # sitemap, manifest, ícones e imagem de Open Graph
```

por:

```
├── proxy.ts         # manda endereços sem idioma para /pt-br ou /en
├── app/
│   ├── [lang]/      # layout raiz, home, 404, erro e imagem de Open Graph de cada idioma
│   └── ...          # erro global, robots, sitemap, manifest, ícones e CSS global
```

Troque também `│   └── ui/          # peças genéricas (logo, botões, indicador de status)` por:

```
│   ├── preferences/ # botão e painel de preferências (idioma)
│   └── ui/          # peças genéricas (logo, ícones, botões, indicador de status)
```

e troque `├── data/            # textos do site e lista de cards da colagem` por:

```
├── data/            # o que não muda com o idioma (nome, redes, ids das seções) e cards da colagem
```

e, depois da linha de `hooks/`, acrescente:

```
├── i18n/            # idiomas, dicionários, escolha do idioma e testes
```

6. Em "O que falta":
   - depois de `- [x] Favicon, ícone da Apple e imagem de compartilhamento próprios`, acrescente `- [x] Site em português e inglês, com o idioma escolhido pelo navegador`;
   - troque `(`#projetos`)` por `(`#projects`)`, `(`#sobre`)` por `(`#about`)` e `(`#contato`)` por `(`#contact`)`;
   - troque `- [ ] Textos definitivos em `src/data/site.ts` (hoje são provisórios)` por `- [ ] Textos definitivos em `src/i18n/dictionaries/` (hoje são provisórios)`;
   - acrescente no fim `- [ ] Tema sistema/claro/escuro no painel de preferências`.

- [ ] **Passo 2: Verificação completa**

Run: `cd /home/martins/projects/martinsportfolio && pnpm test && pnpm lint && pnpm build`
Expected: testes com `fail 0`; lint sem erros; build com `/pt-br` e `/en` como SSG (●).

- [ ] **Passo 3: A primeira visita continua igual**

Rode a receita "Subir o build de produção na porta 3100" e:

```bash
export AGENT_BROWSER_SESSION="$(agent-browser session id --scope worktree --prefix task)"
SCRATCH=/tmp/claude-1000/-home-martins-projects-martinsportfolio/e4e92e0d-c509-41ae-b1e0-0d0972675c8d/scratchpad
for size in "1440 900" "375 812"; do
  set -- $size
  for l in pt-br en; do
    agent-browser set viewport $1 $2 >/dev/null
    agent-browser open http://localhost:3100/$l >/dev/null
    agent-browser wait '[data-intro="nav"][style*="opacity: 1"]' >/dev/null
    agent-browser screenshot "$SCRATCH/final-$l-$1.png" >/dev/null
  done
done
agent-browser close
```

Abra os quatro PNGs com Read. Expected: o hero igual ao de antes da branch, com os textos no idioma certo e o botão de preferências à direita dos links. No celular, sem nada cortado nem sobreposto.

Rode a receita "Parar o servidor".

- [ ] **Passo 4: Commit**

```bash
cd /home/martins/projects/martinsportfolio
git add README.md
git commit -m "$(cat <<'EOF'
docs: document the locales, the proxy and the test command

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Passo 5: Avisar o que fica para depois do deploy**

Na mensagem final para o Sinval, liste o que só dá para conferir em produção (não é tarefa deste plano):

- medir `/pt-br` e `/en` no PageSpeed (não no Lighthouse local) e comparar com a nota de hoje;
- abrir `smartins.dev/` com o navegador em português e em inglês e ver o redirecionamento;
- trocar os links divulgados (LinkedIn, currículo, GitHub) para `/pt-br` ou `/en`.
